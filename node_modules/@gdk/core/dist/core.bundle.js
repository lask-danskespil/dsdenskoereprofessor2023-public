/*! core 3.6.4 Wed May 31 2023 13:10:04 GMT+0000 (Coordinated Universal Time) */
module.exports =
/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 147);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports, __webpack_require__) {

var NATIVE_BIND = __webpack_require__(59);

var FunctionPrototype = Function.prototype;
var call = FunctionPrototype.call;
var uncurryThisWithBind = NATIVE_BIND && FunctionPrototype.bind.bind(call, call);

module.exports = NATIVE_BIND ? uncurryThisWithBind : function (fn) {
  return function () {
    return call.apply(fn, arguments);
  };
};


/***/ }),
/* 1 */
/***/ (function(module, exports) {

module.exports = function (exec) {
  try {
    return !!exec();
  } catch (error) {
    return true;
  }
};


/***/ }),
/* 2 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var getOwnPropertyDescriptor = __webpack_require__(51).f;
var createNonEnumerableProperty = __webpack_require__(36);
var defineBuiltIn = __webpack_require__(16);
var defineGlobalProperty = __webpack_require__(81);
var copyConstructorProperties = __webpack_require__(107);
var isForced = __webpack_require__(90);

/*
  options.target         - name of the target object
  options.global         - target is the global object
  options.stat           - export as static methods of target
  options.proto          - export as prototype methods of target
  options.real           - real prototype method for the `pure` version
  options.forced         - export even if the native feature is available
  options.bind           - bind methods to the target, required for the `pure` version
  options.wrap           - wrap constructors to preventing global pollution, required for the `pure` version
  options.unsafe         - use the simple assignment of property instead of delete + defineProperty
  options.sham           - add a flag to not completely full polyfills
  options.enumerable     - export as enumerable property
  options.dontCallGetSet - prevent calling a getter on target
  options.name           - the .name of the function if it does not match the key
*/
module.exports = function (options, source) {
  var TARGET = options.target;
  var GLOBAL = options.global;
  var STATIC = options.stat;
  var FORCED, target, key, targetProperty, sourceProperty, descriptor;
  if (GLOBAL) {
    target = global;
  } else if (STATIC) {
    target = global[TARGET] || defineGlobalProperty(TARGET, {});
  } else {
    target = (global[TARGET] || {}).prototype;
  }
  if (target) for (key in source) {
    sourceProperty = source[key];
    if (options.dontCallGetSet) {
      descriptor = getOwnPropertyDescriptor(target, key);
      targetProperty = descriptor && descriptor.value;
    } else targetProperty = target[key];
    FORCED = isForced(GLOBAL ? key : TARGET + (STATIC ? '.' : '#') + key, options.forced);
    // contained in target
    if (!FORCED && targetProperty !== undefined) {
      if (typeof sourceProperty == typeof targetProperty) continue;
      copyConstructorProperties(sourceProperty, targetProperty);
    }
    // add a flag to not completely full polyfills
    if (options.sham || (targetProperty && targetProperty.sham)) {
      createNonEnumerableProperty(sourceProperty, 'sham', true);
    }
    defineBuiltIn(target, key, sourceProperty, options);
  }
};


/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

var $documentAll = __webpack_require__(99);

var documentAll = $documentAll.all;

// `IsCallable` abstract operation
// https://tc39.es/ecma262/#sec-iscallable
module.exports = $documentAll.IS_HTMLDDA ? function (argument) {
  return typeof argument == 'function' || argument === documentAll;
} : function (argument) {
  return typeof argument == 'function';
};


/***/ }),
/* 4 */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {var check = function (it) {
  return it && it.Math == Math && it;
};

// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
module.exports =
  // eslint-disable-next-line es/no-global-this -- safe
  check(typeof globalThis == 'object' && globalThis) ||
  check(typeof window == 'object' && window) ||
  // eslint-disable-next-line no-restricted-globals -- safe
  check(typeof self == 'object' && self) ||
  check(typeof global == 'object' && global) ||
  // eslint-disable-next-line no-new-func -- fallback
  (function () { return this; })() || this || Function('return this')();

/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(148)))

/***/ }),
/* 5 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var shared = __webpack_require__(31);
var hasOwn = __webpack_require__(7);
var uid = __webpack_require__(82);
var NATIVE_SYMBOL = __webpack_require__(33);
var USE_SYMBOL_AS_UID = __webpack_require__(98);

var Symbol = global.Symbol;
var WellKnownSymbolsStore = shared('wks');
var createWellKnownSymbol = USE_SYMBOL_AS_UID ? Symbol['for'] || Symbol : Symbol && Symbol.withoutSetter || uid;

module.exports = function (name) {
  if (!hasOwn(WellKnownSymbolsStore, name)) {
    WellKnownSymbolsStore[name] = NATIVE_SYMBOL && hasOwn(Symbol, name)
      ? Symbol[name]
      : createWellKnownSymbol('Symbol.' + name);
  } return WellKnownSymbolsStore[name];
};


/***/ }),
/* 6 */
/***/ (function(module, exports, __webpack_require__) {

var NATIVE_BIND = __webpack_require__(59);

var call = Function.prototype.call;

module.exports = NATIVE_BIND ? call.bind(call) : function () {
  return call.apply(call, arguments);
};


/***/ }),
/* 7 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var toObject = __webpack_require__(15);

var hasOwnProperty = uncurryThis({}.hasOwnProperty);

// `HasOwnProperty` abstract operation
// https://tc39.es/ecma262/#sec-hasownproperty
// eslint-disable-next-line es/no-object-hasown -- safe
module.exports = Object.hasOwn || function hasOwn(it, key) {
  return hasOwnProperty(toObject(it), key);
};


/***/ }),
/* 8 */
/***/ (function(module, exports, __webpack_require__) {

var fails = __webpack_require__(1);

// Detect IE8's incomplete defineProperty implementation
module.exports = !fails(function () {
  // eslint-disable-next-line es/no-object-defineproperty -- required for testing
  return Object.defineProperty({}, 1, { get: function () { return 7; } })[1] != 7;
});


/***/ }),
/* 9 */
/***/ (function(module, exports, __webpack_require__) {

var isObject = __webpack_require__(13);

var $String = String;
var $TypeError = TypeError;

// `Assert: Type(argument) is Object`
module.exports = function (argument) {
  if (isObject(argument)) return argument;
  throw $TypeError($String(argument) + ' is not an object');
};


/***/ }),
/* 10 */
/***/ (function(module, exports, __webpack_require__) {

var TO_STRING_TAG_SUPPORT = __webpack_require__(79);
var defineBuiltIn = __webpack_require__(16);
var toString = __webpack_require__(151);

// `Object.prototype.toString` method
// https://tc39.es/ecma262/#sec-object.prototype.tostring
if (!TO_STRING_TAG_SUPPORT) {
  defineBuiltIn(Object.prototype, 'toString', toString, { unsafe: true });
}


/***/ }),
/* 11 */
/***/ (function(module, exports, __webpack_require__) {

var classof = __webpack_require__(65);

var $String = String;

module.exports = function (argument) {
  if (classof(argument) === 'Symbol') throw TypeError('Cannot convert a Symbol value to a string');
  return $String(argument);
};


/***/ }),
/* 12 */
/***/ (function(module, exports, __webpack_require__) {

var DESCRIPTORS = __webpack_require__(8);
var IE8_DOM_DEFINE = __webpack_require__(100);
var V8_PROTOTYPE_DEFINE_BUG = __webpack_require__(101);
var anObject = __webpack_require__(9);
var toPropertyKey = __webpack_require__(61);

var $TypeError = TypeError;
// eslint-disable-next-line es/no-object-defineproperty -- safe
var $defineProperty = Object.defineProperty;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var ENUMERABLE = 'enumerable';
var CONFIGURABLE = 'configurable';
var WRITABLE = 'writable';

// `Object.defineProperty` method
// https://tc39.es/ecma262/#sec-object.defineproperty
exports.f = DESCRIPTORS ? V8_PROTOTYPE_DEFINE_BUG ? function defineProperty(O, P, Attributes) {
  anObject(O);
  P = toPropertyKey(P);
  anObject(Attributes);
  if (typeof O === 'function' && P === 'prototype' && 'value' in Attributes && WRITABLE in Attributes && !Attributes[WRITABLE]) {
    var current = $getOwnPropertyDescriptor(O, P);
    if (current && current[WRITABLE]) {
      O[P] = Attributes.value;
      Attributes = {
        configurable: CONFIGURABLE in Attributes ? Attributes[CONFIGURABLE] : current[CONFIGURABLE],
        enumerable: ENUMERABLE in Attributes ? Attributes[ENUMERABLE] : current[ENUMERABLE],
        writable: false
      };
    }
  } return $defineProperty(O, P, Attributes);
} : $defineProperty : function defineProperty(O, P, Attributes) {
  anObject(O);
  P = toPropertyKey(P);
  anObject(Attributes);
  if (IE8_DOM_DEFINE) try {
    return $defineProperty(O, P, Attributes);
  } catch (error) { /* empty */ }
  if ('get' in Attributes || 'set' in Attributes) throw $TypeError('Accessors not supported');
  if ('value' in Attributes) O[P] = Attributes.value;
  return O;
};


/***/ }),
/* 13 */
/***/ (function(module, exports, __webpack_require__) {

var isCallable = __webpack_require__(3);
var $documentAll = __webpack_require__(99);

var documentAll = $documentAll.all;

module.exports = $documentAll.IS_HTMLDDA ? function (it) {
  return typeof it == 'object' ? it !== null : isCallable(it) || it === documentAll;
} : function (it) {
  return typeof it == 'object' ? it !== null : isCallable(it);
};


/***/ }),
/* 14 */
/***/ (function(module, exports) {

module.exports = false;


/***/ }),
/* 15 */
/***/ (function(module, exports, __webpack_require__) {

var requireObjectCoercible = __webpack_require__(21);

var $Object = Object;

// `ToObject` abstract operation
// https://tc39.es/ecma262/#sec-toobject
module.exports = function (argument) {
  return $Object(requireObjectCoercible(argument));
};


/***/ }),
/* 16 */
/***/ (function(module, exports, __webpack_require__) {

var isCallable = __webpack_require__(3);
var definePropertyModule = __webpack_require__(12);
var makeBuiltIn = __webpack_require__(103);
var defineGlobalProperty = __webpack_require__(81);

module.exports = function (O, key, value, options) {
  if (!options) options = {};
  var simple = options.enumerable;
  var name = options.name !== undefined ? options.name : key;
  if (isCallable(value)) makeBuiltIn(value, name, options);
  if (options.global) {
    if (simple) O[key] = value;
    else defineGlobalProperty(key, value);
  } else {
    try {
      if (!options.unsafe) delete O[key];
      else if (O[key]) simple = true;
    } catch (error) { /* empty */ }
    if (simple) O[key] = value;
    else definePropertyModule.f(O, key, {
      value: value,
      enumerable: false,
      configurable: !options.nonConfigurable,
      writable: !options.nonWritable
    });
  } return O;
};


/***/ }),
/* 17 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var isCallable = __webpack_require__(3);

var aFunction = function (argument) {
  return isCallable(argument) ? argument : undefined;
};

module.exports = function (namespace, method) {
  return arguments.length < 2 ? aFunction(global[namespace]) : global[namespace] && global[namespace][method];
};


/***/ }),
/* 18 */
/***/ (function(module, exports, __webpack_require__) {

var isCallable = __webpack_require__(3);
var tryToString = __webpack_require__(34);

var $TypeError = TypeError;

// `Assert: IsCallable(argument) is true`
module.exports = function (argument) {
  if (isCallable(argument)) return argument;
  throw $TypeError(tryToString(argument) + ' is not a function');
};


/***/ }),
/* 19 */
/***/ (function(module, exports, __webpack_require__) {

var $ = __webpack_require__(2);
var toObject = __webpack_require__(15);
var nativeKeys = __webpack_require__(71);
var fails = __webpack_require__(1);

var FAILS_ON_PRIMITIVES = fails(function () { nativeKeys(1); });

// `Object.keys` method
// https://tc39.es/ecma262/#sec-object.keys
$({ target: 'Object', stat: true, forced: FAILS_ON_PRIMITIVES }, {
  keys: function keys(it) {
    return nativeKeys(toObject(it));
  }
});


/***/ }),
/* 20 */
/***/ (function(module, exports, __webpack_require__) {

// toObject with fallback for non-array-like ES3 strings
var IndexedObject = __webpack_require__(67);
var requireObjectCoercible = __webpack_require__(21);

module.exports = function (it) {
  return IndexedObject(requireObjectCoercible(it));
};


/***/ }),
/* 21 */
/***/ (function(module, exports, __webpack_require__) {

var isNullOrUndefined = __webpack_require__(32);

var $TypeError = TypeError;

// `RequireObjectCoercible` abstract operation
// https://tc39.es/ecma262/#sec-requireobjectcoercible
module.exports = function (it) {
  if (isNullOrUndefined(it)) throw $TypeError("Can't call method on " + it);
  return it;
};


/***/ }),
/* 22 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);

var toString = uncurryThis({}.toString);
var stringSlice = uncurryThis(''.slice);

module.exports = function (it) {
  return stringSlice(toString(it), 8, -1);
};


/***/ }),
/* 23 */
/***/ (function(module, exports, __webpack_require__) {

var toLength = __webpack_require__(84);

// `LengthOfArrayLike` abstract operation
// https://tc39.es/ecma262/#sec-lengthofarraylike
module.exports = function (obj) {
  return toLength(obj.length);
};


/***/ }),
/* 24 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EEventType = void 0;
var EEventType;
exports.EEventType = EEventType;
(function (EEventType) {
  EEventType["Click"] = "click";
  EEventType["Error"] = "error";
  EEventType["Game"] = "game";
  EEventType["Slide"] = "slide";
  EEventType["View"] = "view";
  EEventType["Settings"] = "settings";
  EEventType["GetContext"] = "getContext";
  EEventType["Execute"] = "execute";
  EEventType["Rendering"] = "rendering";
  EEventType["Scratch"] = "scratch";
})(EEventType || (exports.EEventType = EEventType = {}));

/***/ }),
/* 25 */
/***/ (function(module, exports) {

module.exports = typeof navigator != 'undefined' && String(navigator.userAgent) || '';


/***/ }),
/* 26 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);

module.exports = uncurryThis({}.isPrototypeOf);


/***/ }),
/* 27 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var DOMIterables = __webpack_require__(104);
var DOMTokenListPrototype = __webpack_require__(105);
var forEach = __webpack_require__(152);
var createNonEnumerableProperty = __webpack_require__(36);

var handlePrototype = function (CollectionPrototype) {
  // some Chrome versions have non-configurable methods on DOMTokenList
  if (CollectionPrototype && CollectionPrototype.forEach !== forEach) try {
    createNonEnumerableProperty(CollectionPrototype, 'forEach', forEach);
  } catch (error) {
    CollectionPrototype.forEach = forEach;
  }
};

for (var COLLECTION_NAME in DOMIterables) {
  if (DOMIterables[COLLECTION_NAME]) {
    handlePrototype(global[COLLECTION_NAME] && global[COLLECTION_NAME].prototype);
  }
}

handlePrototype(DOMTokenListPrototype);


/***/ }),
/* 28 */
/***/ (function(module, exports, __webpack_require__) {

var trunc = __webpack_require__(153);

// `ToIntegerOrInfinity` abstract operation
// https://tc39.es/ecma262/#sec-tointegerorinfinity
module.exports = function (argument) {
  var number = +argument;
  // eslint-disable-next-line no-self-compare -- NaN check
  return number !== number || number === 0 ? 0 : trunc(number);
};


/***/ }),
/* 29 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var toIndexedObject = __webpack_require__(20);
var addToUnscopables = __webpack_require__(123);
var Iterators = __webpack_require__(55);
var InternalStateModule = __webpack_require__(35);
var defineProperty = __webpack_require__(12).f;
var defineIterator = __webpack_require__(124);
var createIterResultObject = __webpack_require__(127);
var IS_PURE = __webpack_require__(14);
var DESCRIPTORS = __webpack_require__(8);

var ARRAY_ITERATOR = 'Array Iterator';
var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(ARRAY_ITERATOR);

// `Array.prototype.entries` method
// https://tc39.es/ecma262/#sec-array.prototype.entries
// `Array.prototype.keys` method
// https://tc39.es/ecma262/#sec-array.prototype.keys
// `Array.prototype.values` method
// https://tc39.es/ecma262/#sec-array.prototype.values
// `Array.prototype[@@iterator]` method
// https://tc39.es/ecma262/#sec-array.prototype-@@iterator
// `CreateArrayIterator` internal method
// https://tc39.es/ecma262/#sec-createarrayiterator
module.exports = defineIterator(Array, 'Array', function (iterated, kind) {
  setInternalState(this, {
    type: ARRAY_ITERATOR,
    target: toIndexedObject(iterated), // target
    index: 0,                          // next index
    kind: kind                         // kind
  });
// `%ArrayIteratorPrototype%.next` method
// https://tc39.es/ecma262/#sec-%arrayiteratorprototype%.next
}, function () {
  var state = getInternalState(this);
  var target = state.target;
  var kind = state.kind;
  var index = state.index++;
  if (!target || index >= target.length) {
    state.target = undefined;
    return createIterResultObject(undefined, true);
  }
  if (kind == 'keys') return createIterResultObject(index, false);
  if (kind == 'values') return createIterResultObject(target[index], false);
  return createIterResultObject([index, target[index]], false);
}, 'values');

// argumentsList[@@iterator] is %ArrayProto_values%
// https://tc39.es/ecma262/#sec-createunmappedargumentsobject
// https://tc39.es/ecma262/#sec-createmappedargumentsobject
var values = Iterators.Arguments = Iterators.Array;

// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables
addToUnscopables('keys');
addToUnscopables('values');
addToUnscopables('entries');

// V8 ~ Chrome 45- bug
if (!IS_PURE && DESCRIPTORS && values.name !== 'values') try {
  defineProperty(values, 'name', { value: 'values' });
} catch (error) { /* empty */ }


/***/ }),
/* 30 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var fails = __webpack_require__(1);
var isArray = __webpack_require__(50);
var isObject = __webpack_require__(13);
var toObject = __webpack_require__(15);
var lengthOfArrayLike = __webpack_require__(23);
var doesNotExceedSafeInteger = __webpack_require__(135);
var createProperty = __webpack_require__(76);
var arraySpeciesCreate = __webpack_require__(85);
var arrayMethodHasSpeciesSupport = __webpack_require__(58);
var wellKnownSymbol = __webpack_require__(5);
var V8_VERSION = __webpack_require__(45);

var IS_CONCAT_SPREADABLE = wellKnownSymbol('isConcatSpreadable');

// We can't use this feature detection in V8 since it causes
// deoptimization and serious performance degradation
// https://github.com/zloirock/core-js/issues/679
var IS_CONCAT_SPREADABLE_SUPPORT = V8_VERSION >= 51 || !fails(function () {
  var array = [];
  array[IS_CONCAT_SPREADABLE] = false;
  return array.concat()[0] !== array;
});

var isConcatSpreadable = function (O) {
  if (!isObject(O)) return false;
  var spreadable = O[IS_CONCAT_SPREADABLE];
  return spreadable !== undefined ? !!spreadable : isArray(O);
};

var FORCED = !IS_CONCAT_SPREADABLE_SUPPORT || !arrayMethodHasSpeciesSupport('concat');

// `Array.prototype.concat` method
// https://tc39.es/ecma262/#sec-array.prototype.concat
// with adding support of @@isConcatSpreadable and @@species
$({ target: 'Array', proto: true, arity: 1, forced: FORCED }, {
  // eslint-disable-next-line no-unused-vars -- required for `.length`
  concat: function concat(arg) {
    var O = toObject(this);
    var A = arraySpeciesCreate(O, 0);
    var n = 0;
    var i, k, length, len, E;
    for (i = -1, length = arguments.length; i < length; i++) {
      E = i === -1 ? O : arguments[i];
      if (isConcatSpreadable(E)) {
        len = lengthOfArrayLike(E);
        doesNotExceedSafeInteger(n + len);
        for (k = 0; k < len; k++, n++) if (k in E) createProperty(A, n, E[k]);
      } else {
        doesNotExceedSafeInteger(n + 1);
        createProperty(A, n++, E);
      }
    }
    A.length = n;
    return A;
  }
});


/***/ }),
/* 31 */
/***/ (function(module, exports, __webpack_require__) {

var IS_PURE = __webpack_require__(14);
var store = __webpack_require__(80);

(module.exports = function (key, value) {
  return store[key] || (store[key] = value !== undefined ? value : {});
})('versions', []).push({
  version: '3.30.2',
  mode: IS_PURE ? 'pure' : 'global',
  copyright: '© 2014-2023 Denis Pushkarev (zloirock.ru)',
  license: 'https://github.com/zloirock/core-js/blob/v3.30.2/LICENSE',
  source: 'https://github.com/zloirock/core-js'
});


/***/ }),
/* 32 */
/***/ (function(module, exports) {

// we can't use just `it == null` since of `document.all` special case
// https://tc39.es/ecma262/#sec-IsHTMLDDA-internal-slot-aec
module.exports = function (it) {
  return it === null || it === undefined;
};


/***/ }),
/* 33 */
/***/ (function(module, exports, __webpack_require__) {

/* eslint-disable es/no-symbol -- required for testing */
var V8_VERSION = __webpack_require__(45);
var fails = __webpack_require__(1);
var global = __webpack_require__(4);

var $String = global.String;

// eslint-disable-next-line es/no-object-getownpropertysymbols -- required for testing
module.exports = !!Object.getOwnPropertySymbols && !fails(function () {
  var symbol = Symbol();
  // Chrome 38 Symbol has incorrect toString conversion
  // `get-own-property-symbols` polyfill symbols converted to object are not Symbol instances
  // nb: Do not call `String` directly to avoid this being optimized out to `symbol+''` which will,
  // of course, fail.
  return !$String(symbol) || !(Object(symbol) instanceof Symbol) ||
    // Chrome 38-40 symbols are not inherited from DOM collections prototypes to instances
    !Symbol.sham && V8_VERSION && V8_VERSION < 41;
});


/***/ }),
/* 34 */
/***/ (function(module, exports) {

var $String = String;

module.exports = function (argument) {
  try {
    return $String(argument);
  } catch (error) {
    return 'Object';
  }
};


/***/ }),
/* 35 */
/***/ (function(module, exports, __webpack_require__) {

var NATIVE_WEAK_MAP = __webpack_require__(150);
var global = __webpack_require__(4);
var isObject = __webpack_require__(13);
var createNonEnumerableProperty = __webpack_require__(36);
var hasOwn = __webpack_require__(7);
var shared = __webpack_require__(80);
var sharedKey = __webpack_require__(63);
var hiddenKeys = __webpack_require__(64);

var OBJECT_ALREADY_INITIALIZED = 'Object already initialized';
var TypeError = global.TypeError;
var WeakMap = global.WeakMap;
var set, get, has;

var enforce = function (it) {
  return has(it) ? get(it) : set(it, {});
};

var getterFor = function (TYPE) {
  return function (it) {
    var state;
    if (!isObject(it) || (state = get(it)).type !== TYPE) {
      throw TypeError('Incompatible receiver, ' + TYPE + ' required');
    } return state;
  };
};

if (NATIVE_WEAK_MAP || shared.state) {
  var store = shared.state || (shared.state = new WeakMap());
  /* eslint-disable no-self-assign -- prototype methods protection */
  store.get = store.get;
  store.has = store.has;
  store.set = store.set;
  /* eslint-enable no-self-assign -- prototype methods protection */
  set = function (it, metadata) {
    if (store.has(it)) throw TypeError(OBJECT_ALREADY_INITIALIZED);
    metadata.facade = it;
    store.set(it, metadata);
    return metadata;
  };
  get = function (it) {
    return store.get(it) || {};
  };
  has = function (it) {
    return store.has(it);
  };
} else {
  var STATE = sharedKey('state');
  hiddenKeys[STATE] = true;
  set = function (it, metadata) {
    if (hasOwn(it, STATE)) throw TypeError(OBJECT_ALREADY_INITIALIZED);
    metadata.facade = it;
    createNonEnumerableProperty(it, STATE, metadata);
    return metadata;
  };
  get = function (it) {
    return hasOwn(it, STATE) ? it[STATE] : {};
  };
  has = function (it) {
    return hasOwn(it, STATE);
  };
}

module.exports = {
  set: set,
  get: get,
  has: has,
  enforce: enforce,
  getterFor: getterFor
};


/***/ }),
/* 36 */
/***/ (function(module, exports, __webpack_require__) {

var DESCRIPTORS = __webpack_require__(8);
var definePropertyModule = __webpack_require__(12);
var createPropertyDescriptor = __webpack_require__(48);

module.exports = DESCRIPTORS ? function (object, key, value) {
  return definePropertyModule.f(object, key, createPropertyDescriptor(1, value));
} : function (object, key, value) {
  object[key] = value;
  return object;
};


/***/ }),
/* 37 */
/***/ (function(module, exports, __webpack_require__) {

// TODO: Remove this module from `core-js@4` since it's split to modules listed below
__webpack_require__(159);
__webpack_require__(173);
__webpack_require__(178);
__webpack_require__(179);
__webpack_require__(180);
__webpack_require__(181);


/***/ }),
/* 38 */
/***/ (function(module, exports, __webpack_require__) {

// TODO: Remove this module from `core-js@4` since it's split to modules listed below
__webpack_require__(183);
__webpack_require__(186);
__webpack_require__(187);
__webpack_require__(188);
__webpack_require__(190);


/***/ }),
/* 39 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
// `Symbol.prototype.description` getter
// https://tc39.es/ecma262/#sec-symbol.prototype.description

var $ = __webpack_require__(2);
var DESCRIPTORS = __webpack_require__(8);
var global = __webpack_require__(4);
var uncurryThis = __webpack_require__(0);
var hasOwn = __webpack_require__(7);
var isCallable = __webpack_require__(3);
var isPrototypeOf = __webpack_require__(26);
var toString = __webpack_require__(11);
var defineBuiltInAccessor = __webpack_require__(75);
var copyConstructorProperties = __webpack_require__(107);

var NativeSymbol = global.Symbol;
var SymbolPrototype = NativeSymbol && NativeSymbol.prototype;

if (DESCRIPTORS && isCallable(NativeSymbol) && (!('description' in SymbolPrototype) ||
  // Safari 12 bug
  NativeSymbol().description !== undefined
)) {
  var EmptyStringDescriptionStore = {};
  // wrap Symbol constructor for correct work with undefined description
  var SymbolWrapper = function Symbol() {
    var description = arguments.length < 1 || arguments[0] === undefined ? undefined : toString(arguments[0]);
    var result = isPrototypeOf(SymbolPrototype, this)
      ? new NativeSymbol(description)
      // in Edge 13, String(Symbol(undefined)) === 'Symbol(undefined)'
      : description === undefined ? NativeSymbol() : NativeSymbol(description);
    if (description === '') EmptyStringDescriptionStore[result] = true;
    return result;
  };

  copyConstructorProperties(SymbolWrapper, NativeSymbol);
  SymbolWrapper.prototype = SymbolPrototype;
  SymbolPrototype.constructor = SymbolWrapper;

  var NATIVE_SYMBOL = String(NativeSymbol('test')) == 'Symbol(test)';
  var thisSymbolValue = uncurryThis(SymbolPrototype.valueOf);
  var symbolDescriptiveString = uncurryThis(SymbolPrototype.toString);
  var regexp = /^Symbol\((.*)\)[^)]+$/;
  var replace = uncurryThis(''.replace);
  var stringSlice = uncurryThis(''.slice);

  defineBuiltInAccessor(SymbolPrototype, 'description', {
    configurable: true,
    get: function description() {
      var symbol = thisSymbolValue(this);
      if (hasOwn(EmptyStringDescriptionStore, symbol)) return '';
      var string = symbolDescriptiveString(symbol);
      var desc = NATIVE_SYMBOL ? stringSlice(string, 7, -1) : replace(string, regexp, '$1');
      return desc === '' ? undefined : desc;
    }
  });

  $({ global: true, constructor: true, forced: true }, {
    Symbol: SymbolWrapper
  });
}


/***/ }),
/* 40 */
/***/ (function(module, exports, __webpack_require__) {

var defineWellKnownSymbol = __webpack_require__(120);

// `Symbol.iterator` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.iterator
defineWellKnownSymbol('iterator');


/***/ }),
/* 41 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var charAt = __webpack_require__(128).charAt;
var toString = __webpack_require__(11);
var InternalStateModule = __webpack_require__(35);
var defineIterator = __webpack_require__(124);
var createIterResultObject = __webpack_require__(127);

var STRING_ITERATOR = 'String Iterator';
var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(STRING_ITERATOR);

// `String.prototype[@@iterator]` method
// https://tc39.es/ecma262/#sec-string.prototype-@@iterator
defineIterator(String, 'String', function (iterated) {
  setInternalState(this, {
    type: STRING_ITERATOR,
    string: toString(iterated),
    index: 0
  });
// `%StringIteratorPrototype%.next` method
// https://tc39.es/ecma262/#sec-%stringiteratorprototype%.next
}, function next() {
  var state = getInternalState(this);
  var string = state.string;
  var index = state.index;
  var point;
  if (index >= string.length) return createIterResultObject(undefined, true);
  point = charAt(string, index);
  state.index += point.length;
  return createIterResultObject(point, false);
});


/***/ }),
/* 42 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var DOMIterables = __webpack_require__(104);
var DOMTokenListPrototype = __webpack_require__(105);
var ArrayIteratorMethods = __webpack_require__(29);
var createNonEnumerableProperty = __webpack_require__(36);
var wellKnownSymbol = __webpack_require__(5);

var ITERATOR = wellKnownSymbol('iterator');
var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var ArrayValues = ArrayIteratorMethods.values;

var handlePrototype = function (CollectionPrototype, COLLECTION_NAME) {
  if (CollectionPrototype) {
    // some Chrome versions have non-configurable methods on DOMTokenList
    if (CollectionPrototype[ITERATOR] !== ArrayValues) try {
      createNonEnumerableProperty(CollectionPrototype, ITERATOR, ArrayValues);
    } catch (error) {
      CollectionPrototype[ITERATOR] = ArrayValues;
    }
    if (!CollectionPrototype[TO_STRING_TAG]) {
      createNonEnumerableProperty(CollectionPrototype, TO_STRING_TAG, COLLECTION_NAME);
    }
    if (DOMIterables[COLLECTION_NAME]) for (var METHOD_NAME in ArrayIteratorMethods) {
      // some Chrome versions have non-configurable methods on DOMTokenList
      if (CollectionPrototype[METHOD_NAME] !== ArrayIteratorMethods[METHOD_NAME]) try {
        createNonEnumerableProperty(CollectionPrototype, METHOD_NAME, ArrayIteratorMethods[METHOD_NAME]);
      } catch (error) {
        CollectionPrototype[METHOD_NAME] = ArrayIteratorMethods[METHOD_NAME];
      }
    }
  }
};

for (var COLLECTION_NAME in DOMIterables) {
  handlePrototype(global[COLLECTION_NAME] && global[COLLECTION_NAME].prototype, COLLECTION_NAME);
}

handlePrototype(DOMTokenListPrototype, 'DOMTokenList');


/***/ }),
/* 43 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var exec = __webpack_require__(94);

// `RegExp.prototype.exec` method
// https://tc39.es/ecma262/#sec-regexp.prototype.exec
$({ target: 'RegExp', proto: true, forced: /./.exec !== exec }, {
  exec: exec
});


/***/ }),
/* 44 */
/***/ (function(module, exports, __webpack_require__) {

var DESCRIPTORS = __webpack_require__(8);
var FUNCTION_NAME_EXISTS = __webpack_require__(62).EXISTS;
var uncurryThis = __webpack_require__(0);
var defineBuiltInAccessor = __webpack_require__(75);

var FunctionPrototype = Function.prototype;
var functionToString = uncurryThis(FunctionPrototype.toString);
var nameRE = /function\b(?:\s|\/\*[\S\s]*?\*\/|\/\/[^\n\r]*[\n\r]+)*([^\s(/]*)/;
var regExpExec = uncurryThis(nameRE.exec);
var NAME = 'name';

// Function instances `.name` property
// https://tc39.es/ecma262/#sec-function-instances-name
if (DESCRIPTORS && !FUNCTION_NAME_EXISTS) {
  defineBuiltInAccessor(FunctionPrototype, NAME, {
    configurable: true,
    get: function () {
      try {
        return regExpExec(nameRE, functionToString(this))[1];
      } catch (error) {
        return '';
      }
    }
  });
}


/***/ }),
/* 45 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var userAgent = __webpack_require__(25);

var process = global.process;
var Deno = global.Deno;
var versions = process && process.versions || Deno && Deno.version;
var v8 = versions && versions.v8;
var match, version;

if (v8) {
  match = v8.split('.');
  // in old Chrome, versions of V8 isn't V8 = Chrome / 10
  // but their correct versions are not interesting for us
  version = match[0] > 0 && match[0] < 4 ? 1 : +(match[0] + match[1]);
}

// BrowserFS NodeJS `process` polyfill incorrectly set `.v8` to `0.0`
// so check `userAgent` even if `.v8` exists, but 0
if (!version && userAgent) {
  match = userAgent.match(/Edge\/(\d+)/);
  if (!match || match[1] >= 74) {
    match = userAgent.match(/Chrome\/(\d+)/);
    if (match) version = +match[1];
  }
}

module.exports = version;


/***/ }),
/* 46 */
/***/ (function(module, exports, __webpack_require__) {

var getBuiltIn = __webpack_require__(17);
var isCallable = __webpack_require__(3);
var isPrototypeOf = __webpack_require__(26);
var USE_SYMBOL_AS_UID = __webpack_require__(98);

var $Object = Object;

module.exports = USE_SYMBOL_AS_UID ? function (it) {
  return typeof it == 'symbol';
} : function (it) {
  var $Symbol = getBuiltIn('Symbol');
  return isCallable($Symbol) && isPrototypeOf($Symbol.prototype, $Object(it));
};


/***/ }),
/* 47 */
/***/ (function(module, exports, __webpack_require__) {

var aCallable = __webpack_require__(18);
var isNullOrUndefined = __webpack_require__(32);

// `GetMethod` abstract operation
// https://tc39.es/ecma262/#sec-getmethod
module.exports = function (V, P) {
  var func = V[P];
  return isNullOrUndefined(func) ? undefined : aCallable(func);
};


/***/ }),
/* 48 */
/***/ (function(module, exports) {

module.exports = function (bitmap, value) {
  return {
    enumerable: !(bitmap & 1),
    configurable: !(bitmap & 2),
    writable: !(bitmap & 4),
    value: value
  };
};


/***/ }),
/* 49 */
/***/ (function(module, exports, __webpack_require__) {

var bind = __webpack_require__(66);
var uncurryThis = __webpack_require__(0);
var IndexedObject = __webpack_require__(67);
var toObject = __webpack_require__(15);
var lengthOfArrayLike = __webpack_require__(23);
var arraySpeciesCreate = __webpack_require__(85);

var push = uncurryThis([].push);

// `Array.prototype.{ forEach, map, filter, some, every, find, findIndex, filterReject }` methods implementation
var createMethod = function (TYPE) {
  var IS_MAP = TYPE == 1;
  var IS_FILTER = TYPE == 2;
  var IS_SOME = TYPE == 3;
  var IS_EVERY = TYPE == 4;
  var IS_FIND_INDEX = TYPE == 6;
  var IS_FILTER_REJECT = TYPE == 7;
  var NO_HOLES = TYPE == 5 || IS_FIND_INDEX;
  return function ($this, callbackfn, that, specificCreate) {
    var O = toObject($this);
    var self = IndexedObject(O);
    var boundFunction = bind(callbackfn, that);
    var length = lengthOfArrayLike(self);
    var index = 0;
    var create = specificCreate || arraySpeciesCreate;
    var target = IS_MAP ? create($this, length) : IS_FILTER || IS_FILTER_REJECT ? create($this, 0) : undefined;
    var value, result;
    for (;length > index; index++) if (NO_HOLES || index in self) {
      value = self[index];
      result = boundFunction(value, index, O);
      if (TYPE) {
        if (IS_MAP) target[index] = result; // map
        else if (result) switch (TYPE) {
          case 3: return true;              // some
          case 5: return value;             // find
          case 6: return index;             // findIndex
          case 2: push(target, value);      // filter
        } else switch (TYPE) {
          case 4: return false;             // every
          case 7: push(target, value);      // filterReject
        }
      }
    }
    return IS_FIND_INDEX ? -1 : IS_SOME || IS_EVERY ? IS_EVERY : target;
  };
};

module.exports = {
  // `Array.prototype.forEach` method
  // https://tc39.es/ecma262/#sec-array.prototype.foreach
  forEach: createMethod(0),
  // `Array.prototype.map` method
  // https://tc39.es/ecma262/#sec-array.prototype.map
  map: createMethod(1),
  // `Array.prototype.filter` method
  // https://tc39.es/ecma262/#sec-array.prototype.filter
  filter: createMethod(2),
  // `Array.prototype.some` method
  // https://tc39.es/ecma262/#sec-array.prototype.some
  some: createMethod(3),
  // `Array.prototype.every` method
  // https://tc39.es/ecma262/#sec-array.prototype.every
  every: createMethod(4),
  // `Array.prototype.find` method
  // https://tc39.es/ecma262/#sec-array.prototype.find
  find: createMethod(5),
  // `Array.prototype.findIndex` method
  // https://tc39.es/ecma262/#sec-array.prototype.findIndex
  findIndex: createMethod(6),
  // `Array.prototype.filterReject` method
  // https://github.com/tc39/proposal-array-filtering
  filterReject: createMethod(7)
};


/***/ }),
/* 50 */
/***/ (function(module, exports, __webpack_require__) {

var classof = __webpack_require__(22);

// `IsArray` abstract operation
// https://tc39.es/ecma262/#sec-isarray
// eslint-disable-next-line es/no-array-isarray -- safe
module.exports = Array.isArray || function isArray(argument) {
  return classof(argument) == 'Array';
};


/***/ }),
/* 51 */
/***/ (function(module, exports, __webpack_require__) {

var DESCRIPTORS = __webpack_require__(8);
var call = __webpack_require__(6);
var propertyIsEnumerableModule = __webpack_require__(88);
var createPropertyDescriptor = __webpack_require__(48);
var toIndexedObject = __webpack_require__(20);
var toPropertyKey = __webpack_require__(61);
var hasOwn = __webpack_require__(7);
var IE8_DOM_DEFINE = __webpack_require__(100);

// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

// `Object.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptor
exports.f = DESCRIPTORS ? $getOwnPropertyDescriptor : function getOwnPropertyDescriptor(O, P) {
  O = toIndexedObject(O);
  P = toPropertyKey(P);
  if (IE8_DOM_DEFINE) try {
    return $getOwnPropertyDescriptor(O, P);
  } catch (error) { /* empty */ }
  if (hasOwn(O, P)) return createPropertyDescriptor(!call(propertyIsEnumerableModule.f, O, P), O[P]);
};


/***/ }),
/* 52 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);

module.exports = global.Promise;


/***/ }),
/* 53 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var NativePromiseConstructor = __webpack_require__(52);
var isCallable = __webpack_require__(3);
var isForced = __webpack_require__(90);
var inspectSource = __webpack_require__(83);
var wellKnownSymbol = __webpack_require__(5);
var IS_BROWSER = __webpack_require__(172);
var IS_DENO = __webpack_require__(113);
var IS_PURE = __webpack_require__(14);
var V8_VERSION = __webpack_require__(45);

var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;
var SPECIES = wellKnownSymbol('species');
var SUBCLASSING = false;
var NATIVE_PROMISE_REJECTION_EVENT = isCallable(global.PromiseRejectionEvent);

var FORCED_PROMISE_CONSTRUCTOR = isForced('Promise', function () {
  var PROMISE_CONSTRUCTOR_SOURCE = inspectSource(NativePromiseConstructor);
  var GLOBAL_CORE_JS_PROMISE = PROMISE_CONSTRUCTOR_SOURCE !== String(NativePromiseConstructor);
  // V8 6.6 (Node 10 and Chrome 66) have a bug with resolving custom thenables
  // https://bugs.chromium.org/p/chromium/issues/detail?id=830565
  // We can't detect it synchronously, so just check versions
  if (!GLOBAL_CORE_JS_PROMISE && V8_VERSION === 66) return true;
  // We need Promise#{ catch, finally } in the pure version for preventing prototype pollution
  if (IS_PURE && !(NativePromisePrototype['catch'] && NativePromisePrototype['finally'])) return true;
  // We can't use @@species feature detection in V8 since it causes
  // deoptimization and performance degradation
  // https://github.com/zloirock/core-js/issues/679
  if (!V8_VERSION || V8_VERSION < 51 || !/native code/.test(PROMISE_CONSTRUCTOR_SOURCE)) {
    // Detect correctness of subclassing with @@species support
    var promise = new NativePromiseConstructor(function (resolve) { resolve(1); });
    var FakePromise = function (exec) {
      exec(function () { /* empty */ }, function () { /* empty */ });
    };
    var constructor = promise.constructor = {};
    constructor[SPECIES] = FakePromise;
    SUBCLASSING = promise.then(function () { /* empty */ }) instanceof FakePromise;
    if (!SUBCLASSING) return true;
  // Unhandled rejections tracking support, NodeJS Promise without it fails @@species test
  } return !GLOBAL_CORE_JS_PROMISE && (IS_BROWSER || IS_DENO) && !NATIVE_PROMISE_REJECTION_EVENT;
});

module.exports = {
  CONSTRUCTOR: FORCED_PROMISE_CONSTRUCTOR,
  REJECTION_EVENT: NATIVE_PROMISE_REJECTION_EVENT,
  SUBCLASSING: SUBCLASSING
};


/***/ }),
/* 54 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var aCallable = __webpack_require__(18);

var $TypeError = TypeError;

var PromiseCapability = function (C) {
  var resolve, reject;
  this.promise = new C(function ($$resolve, $$reject) {
    if (resolve !== undefined || reject !== undefined) throw $TypeError('Bad Promise constructor');
    resolve = $$resolve;
    reject = $$reject;
  });
  this.resolve = aCallable(resolve);
  this.reject = aCallable(reject);
};

// `NewPromiseCapability` abstract operation
// https://tc39.es/ecma262/#sec-newpromisecapability
module.exports.f = function (C) {
  return new PromiseCapability(C);
};


/***/ }),
/* 55 */
/***/ (function(module, exports) {

module.exports = {};


/***/ }),
/* 56 */
/***/ (function(module, exports, __webpack_require__) {

/* global ActiveXObject -- old IE, WSH */
var anObject = __webpack_require__(9);
var definePropertiesModule = __webpack_require__(117);
var enumBugKeys = __webpack_require__(89);
var hiddenKeys = __webpack_require__(64);
var html = __webpack_require__(110);
var documentCreateElement = __webpack_require__(60);
var sharedKey = __webpack_require__(63);

var GT = '>';
var LT = '<';
var PROTOTYPE = 'prototype';
var SCRIPT = 'script';
var IE_PROTO = sharedKey('IE_PROTO');

var EmptyConstructor = function () { /* empty */ };

var scriptTag = function (content) {
  return LT + SCRIPT + GT + content + LT + '/' + SCRIPT + GT;
};

// Create object with fake `null` prototype: use ActiveX Object with cleared prototype
var NullProtoObjectViaActiveX = function (activeXDocument) {
  activeXDocument.write(scriptTag(''));
  activeXDocument.close();
  var temp = activeXDocument.parentWindow.Object;
  activeXDocument = null; // avoid memory leak
  return temp;
};

// Create object with fake `null` prototype: use iframe Object with cleared prototype
var NullProtoObjectViaIFrame = function () {
  // Thrash, waste and sodomy: IE GC bug
  var iframe = documentCreateElement('iframe');
  var JS = 'java' + SCRIPT + ':';
  var iframeDocument;
  iframe.style.display = 'none';
  html.appendChild(iframe);
  // https://github.com/zloirock/core-js/issues/475
  iframe.src = String(JS);
  iframeDocument = iframe.contentWindow.document;
  iframeDocument.open();
  iframeDocument.write(scriptTag('document.F=Object'));
  iframeDocument.close();
  return iframeDocument.F;
};

// Check for document.domain and active x support
// No need to use active x approach when document.domain is not set
// see https://github.com/es-shims/es5-shim/issues/150
// variation of https://github.com/kitcambridge/es5-shim/commit/4f738ac066346
// avoid IE GC bug
var activeXDocument;
var NullProtoObject = function () {
  try {
    activeXDocument = new ActiveXObject('htmlfile');
  } catch (error) { /* ignore */ }
  NullProtoObject = typeof document != 'undefined'
    ? document.domain && activeXDocument
      ? NullProtoObjectViaActiveX(activeXDocument) // old IE
      : NullProtoObjectViaIFrame()
    : NullProtoObjectViaActiveX(activeXDocument); // WSH
  var length = enumBugKeys.length;
  while (length--) delete NullProtoObject[PROTOTYPE][enumBugKeys[length]];
  return NullProtoObject();
};

hiddenKeys[IE_PROTO] = true;

// `Object.create` method
// https://tc39.es/ecma262/#sec-object.create
// eslint-disable-next-line es/no-object-create -- safe
module.exports = Object.create || function create(O, Properties) {
  var result;
  if (O !== null) {
    EmptyConstructor[PROTOTYPE] = anObject(O);
    result = new EmptyConstructor();
    EmptyConstructor[PROTOTYPE] = null;
    // add "__proto__" for Object.getPrototypeOf polyfill
    result[IE_PROTO] = O;
  } else result = NullProtoObject();
  return Properties === undefined ? result : definePropertiesModule.f(result, Properties);
};


/***/ }),
/* 57 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
Object.defineProperty(exports, "Amount", {
  enumerable: true,
  get: function get() {
    return _amount.Amount;
  }
});
Object.defineProperty(exports, "ELocaleStringCurrencyDisplayOption", {
  enumerable: true,
  get: function get() {
    return _numeral.ELocaleStringCurrencyDisplayOption;
  }
});
Object.defineProperty(exports, "ELocaleStringMatcherOption", {
  enumerable: true,
  get: function get() {
    return _numeral.ELocaleStringMatcherOption;
  }
});
Object.defineProperty(exports, "ELocaleStringStyleOption", {
  enumerable: true,
  get: function get() {
    return _numeral.ELocaleStringStyleOption;
  }
});
Object.defineProperty(exports, "MD5", {
  enumerable: true,
  get: function get() {
    return _md.MD5;
  }
});
Object.defineProperty(exports, "Numeral", {
  enumerable: true,
  get: function get() {
    return _numeral.Numeral;
  }
});
Object.defineProperty(exports, "Scalar", {
  enumerable: true,
  get: function get() {
    return _scalar.Scalar;
  }
});
Object.defineProperty(exports, "Tools", {
  enumerable: true,
  get: function get() {
    return _tools.Tools;
  }
});
Object.defineProperty(exports, "Vector2", {
  enumerable: true,
  get: function get() {
    return _vector.Vector2;
  }
});
Object.defineProperty(exports, "deterministicStringify", {
  enumerable: true,
  get: function get() {
    return _deterministicStringify.deterministicStringify;
  }
});
var _numeral = __webpack_require__(130);
var _amount = __webpack_require__(201);
var _tools = __webpack_require__(136);
var _vector = __webpack_require__(205);
var _scalar = __webpack_require__(206);
var _md = __webpack_require__(211);
var _deterministicStringify = __webpack_require__(212);

/***/ }),
/* 58 */
/***/ (function(module, exports, __webpack_require__) {

var fails = __webpack_require__(1);
var wellKnownSymbol = __webpack_require__(5);
var V8_VERSION = __webpack_require__(45);

var SPECIES = wellKnownSymbol('species');

module.exports = function (METHOD_NAME) {
  // We can't use this feature detection in V8 since it causes
  // deoptimization and serious performance degradation
  // https://github.com/zloirock/core-js/issues/677
  return V8_VERSION >= 51 || !fails(function () {
    var array = [];
    var constructor = array.constructor = {};
    constructor[SPECIES] = function () {
      return { foo: 1 };
    };
    return array[METHOD_NAME](Boolean).foo !== 1;
  });
};


/***/ }),
/* 59 */
/***/ (function(module, exports, __webpack_require__) {

var fails = __webpack_require__(1);

module.exports = !fails(function () {
  // eslint-disable-next-line es/no-function-prototype-bind -- safe
  var test = (function () { /* empty */ }).bind();
  // eslint-disable-next-line no-prototype-builtins -- safe
  return typeof test != 'function' || test.hasOwnProperty('prototype');
});


/***/ }),
/* 60 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var isObject = __webpack_require__(13);

var document = global.document;
// typeof document.createElement is 'object' in old IE
var EXISTS = isObject(document) && isObject(document.createElement);

module.exports = function (it) {
  return EXISTS ? document.createElement(it) : {};
};


/***/ }),
/* 61 */
/***/ (function(module, exports, __webpack_require__) {

var toPrimitive = __webpack_require__(102);
var isSymbol = __webpack_require__(46);

// `ToPropertyKey` abstract operation
// https://tc39.es/ecma262/#sec-topropertykey
module.exports = function (argument) {
  var key = toPrimitive(argument, 'string');
  return isSymbol(key) ? key : key + '';
};


/***/ }),
/* 62 */
/***/ (function(module, exports, __webpack_require__) {

var DESCRIPTORS = __webpack_require__(8);
var hasOwn = __webpack_require__(7);

var FunctionPrototype = Function.prototype;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var getDescriptor = DESCRIPTORS && Object.getOwnPropertyDescriptor;

var EXISTS = hasOwn(FunctionPrototype, 'name');
// additional protection from minified / mangled / dropped function names
var PROPER = EXISTS && (function something() { /* empty */ }).name === 'something';
var CONFIGURABLE = EXISTS && (!DESCRIPTORS || (DESCRIPTORS && getDescriptor(FunctionPrototype, 'name').configurable));

module.exports = {
  EXISTS: EXISTS,
  PROPER: PROPER,
  CONFIGURABLE: CONFIGURABLE
};


/***/ }),
/* 63 */
/***/ (function(module, exports, __webpack_require__) {

var shared = __webpack_require__(31);
var uid = __webpack_require__(82);

var keys = shared('keys');

module.exports = function (key) {
  return keys[key] || (keys[key] = uid(key));
};


/***/ }),
/* 64 */
/***/ (function(module, exports) {

module.exports = {};


/***/ }),
/* 65 */
/***/ (function(module, exports, __webpack_require__) {

var TO_STRING_TAG_SUPPORT = __webpack_require__(79);
var isCallable = __webpack_require__(3);
var classofRaw = __webpack_require__(22);
var wellKnownSymbol = __webpack_require__(5);

var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var $Object = Object;

// ES3 wrong here
var CORRECT_ARGUMENTS = classofRaw(function () { return arguments; }()) == 'Arguments';

// fallback for IE11 Script Access Denied error
var tryGet = function (it, key) {
  try {
    return it[key];
  } catch (error) { /* empty */ }
};

// getting tag from ES6+ `Object.prototype.toString`
module.exports = TO_STRING_TAG_SUPPORT ? classofRaw : function (it) {
  var O, tag, result;
  return it === undefined ? 'Undefined' : it === null ? 'Null'
    // @@toStringTag case
    : typeof (tag = tryGet(O = $Object(it), TO_STRING_TAG)) == 'string' ? tag
    // builtinTag case
    : CORRECT_ARGUMENTS ? classofRaw(O)
    // ES3 arguments fallback
    : (result = classofRaw(O)) == 'Object' && isCallable(O.callee) ? 'Arguments' : result;
};


/***/ }),
/* 66 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(106);
var aCallable = __webpack_require__(18);
var NATIVE_BIND = __webpack_require__(59);

var bind = uncurryThis(uncurryThis.bind);

// optional / simple context binding
module.exports = function (fn, that) {
  aCallable(fn);
  return that === undefined ? fn : NATIVE_BIND ? bind(fn, that) : function (/* ...args */) {
    return fn.apply(that, arguments);
  };
};


/***/ }),
/* 67 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var fails = __webpack_require__(1);
var classof = __webpack_require__(22);

var $Object = Object;
var split = uncurryThis(''.split);

// fallback for non-array-like ES3 and non-enumerable old V8 strings
module.exports = fails(function () {
  // throws an error in rhino, see https://github.com/mozilla/rhino/issues/346
  // eslint-disable-next-line no-prototype-builtins -- safe
  return !$Object('z').propertyIsEnumerable(0);
}) ? function (it) {
  return classof(it) == 'String' ? split(it, '') : $Object(it);
} : $Object;


/***/ }),
/* 68 */
/***/ (function(module, exports, __webpack_require__) {

var internalObjectKeys = __webpack_require__(108);
var enumBugKeys = __webpack_require__(89);

var hiddenKeys = enumBugKeys.concat('length', 'prototype');

// `Object.getOwnPropertyNames` method
// https://tc39.es/ecma262/#sec-object.getownpropertynames
// eslint-disable-next-line es/no-object-getownpropertynames -- safe
exports.f = Object.getOwnPropertyNames || function getOwnPropertyNames(O) {
  return internalObjectKeys(O, hiddenKeys);
};


/***/ }),
/* 69 */
/***/ (function(module, exports, __webpack_require__) {

var toIntegerOrInfinity = __webpack_require__(28);

var max = Math.max;
var min = Math.min;

// Helper for a popular repeating case of the spec:
// Let integer be ? ToInteger(index).
// If integer < 0, let result be max((length + integer), 0); else let result be min(integer, length).
module.exports = function (index, length) {
  var integer = toIntegerOrInfinity(index);
  return integer < 0 ? max(integer + length, 0) : min(integer, length);
};


/***/ }),
/* 70 */
/***/ (function(module, exports) {

// eslint-disable-next-line es/no-object-getownpropertysymbols -- safe
exports.f = Object.getOwnPropertySymbols;


/***/ }),
/* 71 */
/***/ (function(module, exports, __webpack_require__) {

var internalObjectKeys = __webpack_require__(108);
var enumBugKeys = __webpack_require__(89);

// `Object.keys` method
// https://tc39.es/ecma262/#sec-object.keys
// eslint-disable-next-line es/no-object-keys -- safe
module.exports = Object.keys || function keys(O) {
  return internalObjectKeys(O, enumBugKeys);
};


/***/ }),
/* 72 */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(process) {var classof = __webpack_require__(22);

module.exports = typeof process != 'undefined' && classof(process) == 'process';

/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(160)))

/***/ }),
/* 73 */
/***/ (function(module, exports, __webpack_require__) {

/* eslint-disable no-proto -- safe */
var uncurryThisAccessor = __webpack_require__(161);
var anObject = __webpack_require__(9);
var aPossiblePrototype = __webpack_require__(162);

// `Object.setPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.setprototypeof
// Works with __proto__ only. Old v8 can't work with null proto objects.
// eslint-disable-next-line es/no-object-setprototypeof -- safe
module.exports = Object.setPrototypeOf || ('__proto__' in {} ? function () {
  var CORRECT_SETTER = false;
  var test = {};
  var setter;
  try {
    setter = uncurryThisAccessor(Object.prototype, '__proto__', 'set');
    setter(test, []);
    CORRECT_SETTER = test instanceof Array;
  } catch (error) { /* empty */ }
  return function setPrototypeOf(O, proto) {
    anObject(O);
    aPossiblePrototype(proto);
    if (CORRECT_SETTER) setter(O, proto);
    else O.__proto__ = proto;
    return O;
  };
}() : undefined);


/***/ }),
/* 74 */
/***/ (function(module, exports, __webpack_require__) {

var defineProperty = __webpack_require__(12).f;
var hasOwn = __webpack_require__(7);
var wellKnownSymbol = __webpack_require__(5);

var TO_STRING_TAG = wellKnownSymbol('toStringTag');

module.exports = function (target, TAG, STATIC) {
  if (target && !STATIC) target = target.prototype;
  if (target && !hasOwn(target, TO_STRING_TAG)) {
    defineProperty(target, TO_STRING_TAG, { configurable: true, value: TAG });
  }
};


/***/ }),
/* 75 */
/***/ (function(module, exports, __webpack_require__) {

var makeBuiltIn = __webpack_require__(103);
var defineProperty = __webpack_require__(12);

module.exports = function (target, name, descriptor) {
  if (descriptor.get) makeBuiltIn(descriptor.get, name, { getter: true });
  if (descriptor.set) makeBuiltIn(descriptor.set, name, { setter: true });
  return defineProperty.f(target, name, descriptor);
};


/***/ }),
/* 76 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var toPropertyKey = __webpack_require__(61);
var definePropertyModule = __webpack_require__(12);
var createPropertyDescriptor = __webpack_require__(48);

module.exports = function (object, key, value) {
  var propertyKey = toPropertyKey(key);
  if (propertyKey in object) definePropertyModule.f(object, propertyKey, createPropertyDescriptor(0, value));
  else object[propertyKey] = value;
};


/***/ }),
/* 77 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var apply = __webpack_require__(91);
var call = __webpack_require__(6);
var uncurryThis = __webpack_require__(0);
var fixRegExpWellKnownSymbolLogic = __webpack_require__(131);
var fails = __webpack_require__(1);
var anObject = __webpack_require__(9);
var isCallable = __webpack_require__(3);
var isNullOrUndefined = __webpack_require__(32);
var toIntegerOrInfinity = __webpack_require__(28);
var toLength = __webpack_require__(84);
var toString = __webpack_require__(11);
var requireObjectCoercible = __webpack_require__(21);
var advanceStringIndex = __webpack_require__(132);
var getMethod = __webpack_require__(47);
var getSubstitution = __webpack_require__(197);
var regExpExec = __webpack_require__(133);
var wellKnownSymbol = __webpack_require__(5);

var REPLACE = wellKnownSymbol('replace');
var max = Math.max;
var min = Math.min;
var concat = uncurryThis([].concat);
var push = uncurryThis([].push);
var stringIndexOf = uncurryThis(''.indexOf);
var stringSlice = uncurryThis(''.slice);

var maybeToString = function (it) {
  return it === undefined ? it : String(it);
};

// IE <= 11 replaces $0 with the whole match, as if it was $&
// https://stackoverflow.com/questions/6024666/getting-ie-to-replace-a-regex-with-the-literal-string-0
var REPLACE_KEEPS_$0 = (function () {
  // eslint-disable-next-line regexp/prefer-escape-replacement-dollar-char -- required for testing
  return 'a'.replace(/./, '$0') === '$0';
})();

// Safari <= 13.0.3(?) substitutes nth capture where n>m with an empty string
var REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE = (function () {
  if (/./[REPLACE]) {
    return /./[REPLACE]('a', '$0') === '';
  }
  return false;
})();

var REPLACE_SUPPORTS_NAMED_GROUPS = !fails(function () {
  var re = /./;
  re.exec = function () {
    var result = [];
    result.groups = { a: '7' };
    return result;
  };
  // eslint-disable-next-line regexp/no-useless-dollar-replacements -- false positive
  return ''.replace(re, '$<a>') !== '7';
});

// @@replace logic
fixRegExpWellKnownSymbolLogic('replace', function (_, nativeReplace, maybeCallNative) {
  var UNSAFE_SUBSTITUTE = REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE ? '$' : '$0';

  return [
    // `String.prototype.replace` method
    // https://tc39.es/ecma262/#sec-string.prototype.replace
    function replace(searchValue, replaceValue) {
      var O = requireObjectCoercible(this);
      var replacer = isNullOrUndefined(searchValue) ? undefined : getMethod(searchValue, REPLACE);
      return replacer
        ? call(replacer, searchValue, O, replaceValue)
        : call(nativeReplace, toString(O), searchValue, replaceValue);
    },
    // `RegExp.prototype[@@replace]` method
    // https://tc39.es/ecma262/#sec-regexp.prototype-@@replace
    function (string, replaceValue) {
      var rx = anObject(this);
      var S = toString(string);

      if (
        typeof replaceValue == 'string' &&
        stringIndexOf(replaceValue, UNSAFE_SUBSTITUTE) === -1 &&
        stringIndexOf(replaceValue, '$<') === -1
      ) {
        var res = maybeCallNative(nativeReplace, rx, S, replaceValue);
        if (res.done) return res.value;
      }

      var functionalReplace = isCallable(replaceValue);
      if (!functionalReplace) replaceValue = toString(replaceValue);

      var global = rx.global;
      if (global) {
        var fullUnicode = rx.unicode;
        rx.lastIndex = 0;
      }
      var results = [];
      while (true) {
        var result = regExpExec(rx, S);
        if (result === null) break;

        push(results, result);
        if (!global) break;

        var matchStr = toString(result[0]);
        if (matchStr === '') rx.lastIndex = advanceStringIndex(S, toLength(rx.lastIndex), fullUnicode);
      }

      var accumulatedResult = '';
      var nextSourcePosition = 0;
      for (var i = 0; i < results.length; i++) {
        result = results[i];

        var matched = toString(result[0]);
        var position = max(min(toIntegerOrInfinity(result.index), S.length), 0);
        var captures = [];
        // NOTE: This is equivalent to
        //   captures = result.slice(1).map(maybeToString)
        // but for some reason `nativeSlice.call(result, 1, result.length)` (called in
        // the slice polyfill when slicing native arrays) "doesn't work" in safari 9 and
        // causes a crash (https://pastebin.com/N21QzeQA) when trying to debug it.
        for (var j = 1; j < result.length; j++) push(captures, maybeToString(result[j]));
        var namedCaptures = result.groups;
        if (functionalReplace) {
          var replacerArgs = concat([matched], captures, position, S);
          if (namedCaptures !== undefined) push(replacerArgs, namedCaptures);
          var replacement = toString(apply(replaceValue, undefined, replacerArgs));
        } else {
          replacement = getSubstitution(matched, S, position, captures, namedCaptures, replaceValue);
        }
        if (position >= nextSourcePosition) {
          accumulatedResult += stringSlice(S, nextSourcePosition, position) + replacement;
          nextSourcePosition = position + matched.length;
        }
      }
      return accumulatedResult + stringSlice(S, nextSourcePosition);
    }
  ];
}, !REPLACE_SUPPORTS_NAMED_GROUPS || !REPLACE_KEEPS_$0 || REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE);


/***/ }),
/* 78 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var PROPER_FUNCTION_NAME = __webpack_require__(62).PROPER;
var defineBuiltIn = __webpack_require__(16);
var anObject = __webpack_require__(9);
var $toString = __webpack_require__(11);
var fails = __webpack_require__(1);
var getRegExpFlags = __webpack_require__(204);

var TO_STRING = 'toString';
var RegExpPrototype = RegExp.prototype;
var nativeToString = RegExpPrototype[TO_STRING];

var NOT_GENERIC = fails(function () { return nativeToString.call({ source: 'a', flags: 'b' }) != '/a/b'; });
// FF44- RegExp#toString has a wrong name
var INCORRECT_NAME = PROPER_FUNCTION_NAME && nativeToString.name != TO_STRING;

// `RegExp.prototype.toString` method
// https://tc39.es/ecma262/#sec-regexp.prototype.tostring
if (NOT_GENERIC || INCORRECT_NAME) {
  defineBuiltIn(RegExp.prototype, TO_STRING, function toString() {
    var R = anObject(this);
    var pattern = $toString(R.source);
    var flags = $toString(getRegExpFlags(R));
    return '/' + pattern + '/' + flags;
  }, { unsafe: true });
}


/***/ }),
/* 79 */
/***/ (function(module, exports, __webpack_require__) {

var wellKnownSymbol = __webpack_require__(5);

var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var test = {};

test[TO_STRING_TAG] = 'z';

module.exports = String(test) === '[object z]';


/***/ }),
/* 80 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var defineGlobalProperty = __webpack_require__(81);

var SHARED = '__core-js_shared__';
var store = global[SHARED] || defineGlobalProperty(SHARED, {});

module.exports = store;


/***/ }),
/* 81 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);

// eslint-disable-next-line es/no-object-defineproperty -- safe
var defineProperty = Object.defineProperty;

module.exports = function (key, value) {
  try {
    defineProperty(global, key, { value: value, configurable: true, writable: true });
  } catch (error) {
    global[key] = value;
  } return value;
};


/***/ }),
/* 82 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);

var id = 0;
var postfix = Math.random();
var toString = uncurryThis(1.0.toString);

module.exports = function (key) {
  return 'Symbol(' + (key === undefined ? '' : key) + ')_' + toString(++id + postfix, 36);
};


/***/ }),
/* 83 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var isCallable = __webpack_require__(3);
var store = __webpack_require__(80);

var functionToString = uncurryThis(Function.toString);

// this helper broken in `core-js@3.4.1-3.4.4`, so we can't use `shared` helper
if (!isCallable(store.inspectSource)) {
  store.inspectSource = function (it) {
    return functionToString(it);
  };
}

module.exports = store.inspectSource;


/***/ }),
/* 84 */
/***/ (function(module, exports, __webpack_require__) {

var toIntegerOrInfinity = __webpack_require__(28);

var min = Math.min;

// `ToLength` abstract operation
// https://tc39.es/ecma262/#sec-tolength
module.exports = function (argument) {
  return argument > 0 ? min(toIntegerOrInfinity(argument), 0x1FFFFFFFFFFFFF) : 0; // 2 ** 53 - 1 == 9007199254740991
};


/***/ }),
/* 85 */
/***/ (function(module, exports, __webpack_require__) {

var arraySpeciesConstructor = __webpack_require__(154);

// `ArraySpeciesCreate` abstract operation
// https://tc39.es/ecma262/#sec-arrayspeciescreate
module.exports = function (originalArray, length) {
  return new (arraySpeciesConstructor(originalArray))(length === 0 ? 0 : length);
};


/***/ }),
/* 86 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var fails = __webpack_require__(1);
var isCallable = __webpack_require__(3);
var classof = __webpack_require__(65);
var getBuiltIn = __webpack_require__(17);
var inspectSource = __webpack_require__(83);

var noop = function () { /* empty */ };
var empty = [];
var construct = getBuiltIn('Reflect', 'construct');
var constructorRegExp = /^\s*(?:class|function)\b/;
var exec = uncurryThis(constructorRegExp.exec);
var INCORRECT_TO_STRING = !constructorRegExp.exec(noop);

var isConstructorModern = function isConstructor(argument) {
  if (!isCallable(argument)) return false;
  try {
    construct(noop, empty, argument);
    return true;
  } catch (error) {
    return false;
  }
};

var isConstructorLegacy = function isConstructor(argument) {
  if (!isCallable(argument)) return false;
  switch (classof(argument)) {
    case 'AsyncFunction':
    case 'GeneratorFunction':
    case 'AsyncGeneratorFunction': return false;
  }
  try {
    // we can't check .prototype since constructors produced by .bind haven't it
    // `Function#toString` throws on some built-it function in some legacy engines
    // (for example, `DOMQuad` and similar in FF41-)
    return INCORRECT_TO_STRING || !!exec(constructorRegExp, inspectSource(argument));
  } catch (error) {
    return true;
  }
};

isConstructorLegacy.sham = true;

// `IsConstructor` abstract operation
// https://tc39.es/ecma262/#sec-isconstructor
module.exports = !construct || fails(function () {
  var called;
  return isConstructorModern(isConstructorModern.call)
    || !isConstructorModern(Object)
    || !isConstructorModern(function () { called = true; })
    || called;
}) ? isConstructorLegacy : isConstructorModern;


/***/ }),
/* 87 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var fails = __webpack_require__(1);

module.exports = function (METHOD_NAME, argument) {
  var method = [][METHOD_NAME];
  return !!method && fails(function () {
    // eslint-disable-next-line no-useless-call -- required for testing
    method.call(null, argument || function () { return 1; }, 1);
  });
};


/***/ }),
/* 88 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $propertyIsEnumerable = {}.propertyIsEnumerable;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

// Nashorn ~ JDK8 bug
var NASHORN_BUG = getOwnPropertyDescriptor && !$propertyIsEnumerable.call({ 1: 2 }, 1);

// `Object.prototype.propertyIsEnumerable` method implementation
// https://tc39.es/ecma262/#sec-object.prototype.propertyisenumerable
exports.f = NASHORN_BUG ? function propertyIsEnumerable(V) {
  var descriptor = getOwnPropertyDescriptor(this, V);
  return !!descriptor && descriptor.enumerable;
} : $propertyIsEnumerable;


/***/ }),
/* 89 */
/***/ (function(module, exports) {

// IE8- don't enum bug keys
module.exports = [
  'constructor',
  'hasOwnProperty',
  'isPrototypeOf',
  'propertyIsEnumerable',
  'toLocaleString',
  'toString',
  'valueOf'
];


/***/ }),
/* 90 */
/***/ (function(module, exports, __webpack_require__) {

var fails = __webpack_require__(1);
var isCallable = __webpack_require__(3);

var replacement = /#|\.prototype\./;

var isForced = function (feature, detection) {
  var value = data[normalize(feature)];
  return value == POLYFILL ? true
    : value == NATIVE ? false
    : isCallable(detection) ? fails(detection)
    : !!detection;
};

var normalize = isForced.normalize = function (string) {
  return String(string).replace(replacement, '.').toLowerCase();
};

var data = isForced.data = {};
var NATIVE = isForced.NATIVE = 'N';
var POLYFILL = isForced.POLYFILL = 'P';

module.exports = isForced;


/***/ }),
/* 91 */
/***/ (function(module, exports, __webpack_require__) {

var NATIVE_BIND = __webpack_require__(59);

var FunctionPrototype = Function.prototype;
var apply = FunctionPrototype.apply;
var call = FunctionPrototype.call;

// eslint-disable-next-line es/no-reflect -- safe
module.exports = typeof Reflect == 'object' && Reflect.apply || (NATIVE_BIND ? call.bind(apply) : function () {
  return call.apply(apply, arguments);
});


/***/ }),
/* 92 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);

module.exports = uncurryThis([].slice);


/***/ }),
/* 93 */
/***/ (function(module, exports) {

module.exports = function (exec) {
  try {
    return { error: false, value: exec() };
  } catch (error) {
    return { error: true, value: error };
  }
};


/***/ }),
/* 94 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

/* eslint-disable regexp/no-empty-capturing-group, regexp/no-empty-group, regexp/no-lazy-ends -- testing */
/* eslint-disable regexp/no-useless-quantifier -- testing */
var call = __webpack_require__(6);
var uncurryThis = __webpack_require__(0);
var toString = __webpack_require__(11);
var regexpFlags = __webpack_require__(129);
var stickyHelpers = __webpack_require__(193);
var shared = __webpack_require__(31);
var create = __webpack_require__(56);
var getInternalState = __webpack_require__(35).get;
var UNSUPPORTED_DOT_ALL = __webpack_require__(194);
var UNSUPPORTED_NCG = __webpack_require__(195);

var nativeReplace = shared('native-string-replace', String.prototype.replace);
var nativeExec = RegExp.prototype.exec;
var patchedExec = nativeExec;
var charAt = uncurryThis(''.charAt);
var indexOf = uncurryThis(''.indexOf);
var replace = uncurryThis(''.replace);
var stringSlice = uncurryThis(''.slice);

var UPDATES_LAST_INDEX_WRONG = (function () {
  var re1 = /a/;
  var re2 = /b*/g;
  call(nativeExec, re1, 'a');
  call(nativeExec, re2, 'a');
  return re1.lastIndex !== 0 || re2.lastIndex !== 0;
})();

var UNSUPPORTED_Y = stickyHelpers.BROKEN_CARET;

// nonparticipating capturing group, copied from es5-shim's String#split patch.
var NPCG_INCLUDED = /()??/.exec('')[1] !== undefined;

var PATCH = UPDATES_LAST_INDEX_WRONG || NPCG_INCLUDED || UNSUPPORTED_Y || UNSUPPORTED_DOT_ALL || UNSUPPORTED_NCG;

if (PATCH) {
  patchedExec = function exec(string) {
    var re = this;
    var state = getInternalState(re);
    var str = toString(string);
    var raw = state.raw;
    var result, reCopy, lastIndex, match, i, object, group;

    if (raw) {
      raw.lastIndex = re.lastIndex;
      result = call(patchedExec, raw, str);
      re.lastIndex = raw.lastIndex;
      return result;
    }

    var groups = state.groups;
    var sticky = UNSUPPORTED_Y && re.sticky;
    var flags = call(regexpFlags, re);
    var source = re.source;
    var charsAdded = 0;
    var strCopy = str;

    if (sticky) {
      flags = replace(flags, 'y', '');
      if (indexOf(flags, 'g') === -1) {
        flags += 'g';
      }

      strCopy = stringSlice(str, re.lastIndex);
      // Support anchored sticky behavior.
      if (re.lastIndex > 0 && (!re.multiline || re.multiline && charAt(str, re.lastIndex - 1) !== '\n')) {
        source = '(?: ' + source + ')';
        strCopy = ' ' + strCopy;
        charsAdded++;
      }
      // ^(? + rx + ) is needed, in combination with some str slicing, to
      // simulate the 'y' flag.
      reCopy = new RegExp('^(?:' + source + ')', flags);
    }

    if (NPCG_INCLUDED) {
      reCopy = new RegExp('^' + source + '$(?!\\s)', flags);
    }
    if (UPDATES_LAST_INDEX_WRONG) lastIndex = re.lastIndex;

    match = call(nativeExec, sticky ? reCopy : re, strCopy);

    if (sticky) {
      if (match) {
        match.input = stringSlice(match.input, charsAdded);
        match[0] = stringSlice(match[0], charsAdded);
        match.index = re.lastIndex;
        re.lastIndex += match[0].length;
      } else re.lastIndex = 0;
    } else if (UPDATES_LAST_INDEX_WRONG && match) {
      re.lastIndex = re.global ? match.index + match[0].length : lastIndex;
    }
    if (NPCG_INCLUDED && match && match.length > 1) {
      // Fix browsers whose `exec` methods don't consistently return `undefined`
      // for NPCG, like IE8. NOTE: This doesn't work for /(.?)?/
      call(nativeReplace, match[0], reCopy, function () {
        for (i = 1; i < arguments.length - 2; i++) {
          if (arguments[i] === undefined) match[i] = undefined;
        }
      });
    }

    if (match && groups) {
      match.groups = object = create(null);
      for (i = 0; i < groups.length; i++) {
        group = groups[i];
        object[group[0]] = match[group[1]];
      }
    }

    return match;
  };
}

module.exports = patchedExec;


/***/ }),
/* 95 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var uncurryThis = __webpack_require__(0);
var IndexedObject = __webpack_require__(67);
var toIndexedObject = __webpack_require__(20);
var arrayMethodIsStrict = __webpack_require__(87);

var nativeJoin = uncurryThis([].join);

var ES3_STRINGS = IndexedObject != Object;
var FORCED = ES3_STRINGS || !arrayMethodIsStrict('join', ',');

// `Array.prototype.join` method
// https://tc39.es/ecma262/#sec-array.prototype.join
$({ target: 'Array', proto: true, forced: FORCED }, {
  join: function join(separator) {
    return nativeJoin(toIndexedObject(this), separator === undefined ? ',' : separator);
  }
});


/***/ }),
/* 96 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var isArray = __webpack_require__(50);
var isConstructor = __webpack_require__(86);
var isObject = __webpack_require__(13);
var toAbsoluteIndex = __webpack_require__(69);
var lengthOfArrayLike = __webpack_require__(23);
var toIndexedObject = __webpack_require__(20);
var createProperty = __webpack_require__(76);
var wellKnownSymbol = __webpack_require__(5);
var arrayMethodHasSpeciesSupport = __webpack_require__(58);
var nativeSlice = __webpack_require__(92);

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('slice');

var SPECIES = wellKnownSymbol('species');
var $Array = Array;
var max = Math.max;

// `Array.prototype.slice` method
// https://tc39.es/ecma262/#sec-array.prototype.slice
// fallback for not array-like ES3 strings and DOM objects
$({ target: 'Array', proto: true, forced: !HAS_SPECIES_SUPPORT }, {
  slice: function slice(start, end) {
    var O = toIndexedObject(this);
    var length = lengthOfArrayLike(O);
    var k = toAbsoluteIndex(start, length);
    var fin = toAbsoluteIndex(end === undefined ? length : end, length);
    // inline `ArraySpeciesCreate` for usage native `Array#slice` where it's possible
    var Constructor, result, n;
    if (isArray(O)) {
      Constructor = O.constructor;
      // cross-realm fallback
      if (isConstructor(Constructor) && (Constructor === $Array || isArray(Constructor.prototype))) {
        Constructor = undefined;
      } else if (isObject(Constructor)) {
        Constructor = Constructor[SPECIES];
        if (Constructor === null) Constructor = undefined;
      }
      if (Constructor === $Array || Constructor === undefined) {
        return nativeSlice(O, k, fin);
      }
    }
    result = new (Constructor === undefined ? $Array : Constructor)(max(fin - k, 0));
    for (n = 0; k < fin; k++, n++) if (k in O) createProperty(result, n, O[k]);
    result.length = n;
    return result;
  }
});


/***/ }),
/* 97 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EOutput = exports.ELevel = void 0;
var ELevel;
exports.ELevel = ELevel;
(function (ELevel) {
  ELevel[ELevel["Debug"] = 0] = "Debug";
  ELevel[ELevel["Info"] = 1] = "Info";
  ELevel[ELevel["Warn"] = 2] = "Warn";
  ELevel[ELevel["Error"] = 3] = "Error";
})(ELevel || (exports.ELevel = ELevel = {}));
var EOutput;
exports.EOutput = EOutput;
(function (EOutput) {
  EOutput[EOutput["Console"] = 0] = "Console";
})(EOutput || (exports.EOutput = EOutput = {}));

/***/ }),
/* 98 */
/***/ (function(module, exports, __webpack_require__) {

/* eslint-disable es/no-symbol -- required for testing */
var NATIVE_SYMBOL = __webpack_require__(33);

module.exports = NATIVE_SYMBOL
  && !Symbol.sham
  && typeof Symbol.iterator == 'symbol';


/***/ }),
/* 99 */
/***/ (function(module, exports) {

var documentAll = typeof document == 'object' && document.all;

// https://tc39.es/ecma262/#sec-IsHTMLDDA-internal-slot
// eslint-disable-next-line unicorn/no-typeof-undefined -- required for testing
var IS_HTMLDDA = typeof documentAll == 'undefined' && documentAll !== undefined;

module.exports = {
  all: documentAll,
  IS_HTMLDDA: IS_HTMLDDA
};


/***/ }),
/* 100 */
/***/ (function(module, exports, __webpack_require__) {

var DESCRIPTORS = __webpack_require__(8);
var fails = __webpack_require__(1);
var createElement = __webpack_require__(60);

// Thanks to IE8 for its funny defineProperty
module.exports = !DESCRIPTORS && !fails(function () {
  // eslint-disable-next-line es/no-object-defineproperty -- required for testing
  return Object.defineProperty(createElement('div'), 'a', {
    get: function () { return 7; }
  }).a != 7;
});


/***/ }),
/* 101 */
/***/ (function(module, exports, __webpack_require__) {

var DESCRIPTORS = __webpack_require__(8);
var fails = __webpack_require__(1);

// V8 ~ Chrome 36-
// https://bugs.chromium.org/p/v8/issues/detail?id=3334
module.exports = DESCRIPTORS && fails(function () {
  // eslint-disable-next-line es/no-object-defineproperty -- required for testing
  return Object.defineProperty(function () { /* empty */ }, 'prototype', {
    value: 42,
    writable: false
  }).prototype != 42;
});


/***/ }),
/* 102 */
/***/ (function(module, exports, __webpack_require__) {

var call = __webpack_require__(6);
var isObject = __webpack_require__(13);
var isSymbol = __webpack_require__(46);
var getMethod = __webpack_require__(47);
var ordinaryToPrimitive = __webpack_require__(149);
var wellKnownSymbol = __webpack_require__(5);

var $TypeError = TypeError;
var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');

// `ToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-toprimitive
module.exports = function (input, pref) {
  if (!isObject(input) || isSymbol(input)) return input;
  var exoticToPrim = getMethod(input, TO_PRIMITIVE);
  var result;
  if (exoticToPrim) {
    if (pref === undefined) pref = 'default';
    result = call(exoticToPrim, input, pref);
    if (!isObject(result) || isSymbol(result)) return result;
    throw $TypeError("Can't convert object to primitive value");
  }
  if (pref === undefined) pref = 'number';
  return ordinaryToPrimitive(input, pref);
};


/***/ }),
/* 103 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var fails = __webpack_require__(1);
var isCallable = __webpack_require__(3);
var hasOwn = __webpack_require__(7);
var DESCRIPTORS = __webpack_require__(8);
var CONFIGURABLE_FUNCTION_NAME = __webpack_require__(62).CONFIGURABLE;
var inspectSource = __webpack_require__(83);
var InternalStateModule = __webpack_require__(35);

var enforceInternalState = InternalStateModule.enforce;
var getInternalState = InternalStateModule.get;
var $String = String;
// eslint-disable-next-line es/no-object-defineproperty -- safe
var defineProperty = Object.defineProperty;
var stringSlice = uncurryThis(''.slice);
var replace = uncurryThis(''.replace);
var join = uncurryThis([].join);

var CONFIGURABLE_LENGTH = DESCRIPTORS && !fails(function () {
  return defineProperty(function () { /* empty */ }, 'length', { value: 8 }).length !== 8;
});

var TEMPLATE = String(String).split('String');

var makeBuiltIn = module.exports = function (value, name, options) {
  if (stringSlice($String(name), 0, 7) === 'Symbol(') {
    name = '[' + replace($String(name), /^Symbol\(([^)]*)\)/, '$1') + ']';
  }
  if (options && options.getter) name = 'get ' + name;
  if (options && options.setter) name = 'set ' + name;
  if (!hasOwn(value, 'name') || (CONFIGURABLE_FUNCTION_NAME && value.name !== name)) {
    if (DESCRIPTORS) defineProperty(value, 'name', { value: name, configurable: true });
    else value.name = name;
  }
  if (CONFIGURABLE_LENGTH && options && hasOwn(options, 'arity') && value.length !== options.arity) {
    defineProperty(value, 'length', { value: options.arity });
  }
  try {
    if (options && hasOwn(options, 'constructor') && options.constructor) {
      if (DESCRIPTORS) defineProperty(value, 'prototype', { writable: false });
    // in V8 ~ Chrome 53, prototypes of some methods, like `Array.prototype.values`, are non-writable
    } else if (value.prototype) value.prototype = undefined;
  } catch (error) { /* empty */ }
  var state = enforceInternalState(value);
  if (!hasOwn(state, 'source')) {
    state.source = join(TEMPLATE, typeof name == 'string' ? name : '');
  } return value;
};

// add fake Function#toString for correct work wrapped methods / constructors with methods like LoDash isNative
// eslint-disable-next-line no-extend-native -- required
Function.prototype.toString = makeBuiltIn(function toString() {
  return isCallable(this) && getInternalState(this).source || inspectSource(this);
}, 'toString');


/***/ }),
/* 104 */
/***/ (function(module, exports) {

// iterable DOM collections
// flag - `iterable` interface - 'entries', 'keys', 'values', 'forEach' methods
module.exports = {
  CSSRuleList: 0,
  CSSStyleDeclaration: 0,
  CSSValueList: 0,
  ClientRectList: 0,
  DOMRectList: 0,
  DOMStringList: 0,
  DOMTokenList: 1,
  DataTransferItemList: 0,
  FileList: 0,
  HTMLAllCollection: 0,
  HTMLCollection: 0,
  HTMLFormElement: 0,
  HTMLSelectElement: 0,
  MediaList: 0,
  MimeTypeArray: 0,
  NamedNodeMap: 0,
  NodeList: 1,
  PaintRequestList: 0,
  Plugin: 0,
  PluginArray: 0,
  SVGLengthList: 0,
  SVGNumberList: 0,
  SVGPathSegList: 0,
  SVGPointList: 0,
  SVGStringList: 0,
  SVGTransformList: 0,
  SourceBufferList: 0,
  StyleSheetList: 0,
  TextTrackCueList: 0,
  TextTrackList: 0,
  TouchList: 0
};


/***/ }),
/* 105 */
/***/ (function(module, exports, __webpack_require__) {

// in old WebKit versions, `element.classList` is not an instance of global `DOMTokenList`
var documentCreateElement = __webpack_require__(60);

var classList = documentCreateElement('span').classList;
var DOMTokenListPrototype = classList && classList.constructor && classList.constructor.prototype;

module.exports = DOMTokenListPrototype === Object.prototype ? undefined : DOMTokenListPrototype;


/***/ }),
/* 106 */
/***/ (function(module, exports, __webpack_require__) {

var classofRaw = __webpack_require__(22);
var uncurryThis = __webpack_require__(0);

module.exports = function (fn) {
  // Nashorn bug:
  //   https://github.com/zloirock/core-js/issues/1128
  //   https://github.com/zloirock/core-js/issues/1130
  if (classofRaw(fn) === 'Function') return uncurryThis(fn);
};


/***/ }),
/* 107 */
/***/ (function(module, exports, __webpack_require__) {

var hasOwn = __webpack_require__(7);
var ownKeys = __webpack_require__(155);
var getOwnPropertyDescriptorModule = __webpack_require__(51);
var definePropertyModule = __webpack_require__(12);

module.exports = function (target, source, exceptions) {
  var keys = ownKeys(source);
  var defineProperty = definePropertyModule.f;
  var getOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
  for (var i = 0; i < keys.length; i++) {
    var key = keys[i];
    if (!hasOwn(target, key) && !(exceptions && hasOwn(exceptions, key))) {
      defineProperty(target, key, getOwnPropertyDescriptor(source, key));
    }
  }
};


/***/ }),
/* 108 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var hasOwn = __webpack_require__(7);
var toIndexedObject = __webpack_require__(20);
var indexOf = __webpack_require__(156).indexOf;
var hiddenKeys = __webpack_require__(64);

var push = uncurryThis([].push);

module.exports = function (object, names) {
  var O = toIndexedObject(object);
  var i = 0;
  var result = [];
  var key;
  for (key in O) !hasOwn(hiddenKeys, key) && hasOwn(O, key) && push(result, key);
  // Don't enum bug & hidden keys
  while (names.length > i) if (hasOwn(O, key = names[i++])) {
    ~indexOf(result, key) || push(result, key);
  }
  return result;
};


/***/ }),
/* 109 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var apply = __webpack_require__(91);
var bind = __webpack_require__(66);
var isCallable = __webpack_require__(3);
var hasOwn = __webpack_require__(7);
var fails = __webpack_require__(1);
var html = __webpack_require__(110);
var arraySlice = __webpack_require__(92);
var createElement = __webpack_require__(60);
var validateArgumentsLength = __webpack_require__(167);
var IS_IOS = __webpack_require__(111);
var IS_NODE = __webpack_require__(72);

var set = global.setImmediate;
var clear = global.clearImmediate;
var process = global.process;
var Dispatch = global.Dispatch;
var Function = global.Function;
var MessageChannel = global.MessageChannel;
var String = global.String;
var counter = 0;
var queue = {};
var ONREADYSTATECHANGE = 'onreadystatechange';
var $location, defer, channel, port;

fails(function () {
  // Deno throws a ReferenceError on `location` access without `--location` flag
  $location = global.location;
});

var run = function (id) {
  if (hasOwn(queue, id)) {
    var fn = queue[id];
    delete queue[id];
    fn();
  }
};

var runner = function (id) {
  return function () {
    run(id);
  };
};

var eventListener = function (event) {
  run(event.data);
};

var globalPostMessageDefer = function (id) {
  // old engines have not location.origin
  global.postMessage(String(id), $location.protocol + '//' + $location.host);
};

// Node.js 0.9+ & IE10+ has setImmediate, otherwise:
if (!set || !clear) {
  set = function setImmediate(handler) {
    validateArgumentsLength(arguments.length, 1);
    var fn = isCallable(handler) ? handler : Function(handler);
    var args = arraySlice(arguments, 1);
    queue[++counter] = function () {
      apply(fn, undefined, args);
    };
    defer(counter);
    return counter;
  };
  clear = function clearImmediate(id) {
    delete queue[id];
  };
  // Node.js 0.8-
  if (IS_NODE) {
    defer = function (id) {
      process.nextTick(runner(id));
    };
  // Sphere (JS game engine) Dispatch API
  } else if (Dispatch && Dispatch.now) {
    defer = function (id) {
      Dispatch.now(runner(id));
    };
  // Browsers with MessageChannel, includes WebWorkers
  // except iOS - https://github.com/zloirock/core-js/issues/624
  } else if (MessageChannel && !IS_IOS) {
    channel = new MessageChannel();
    port = channel.port2;
    channel.port1.onmessage = eventListener;
    defer = bind(port.postMessage, port);
  // Browsers with postMessage, skip WebWorkers
  // IE8 has postMessage, but it's sync & typeof its postMessage is 'object'
  } else if (
    global.addEventListener &&
    isCallable(global.postMessage) &&
    !global.importScripts &&
    $location && $location.protocol !== 'file:' &&
    !fails(globalPostMessageDefer)
  ) {
    defer = globalPostMessageDefer;
    global.addEventListener('message', eventListener, false);
  // IE8-
  } else if (ONREADYSTATECHANGE in createElement('script')) {
    defer = function (id) {
      html.appendChild(createElement('script'))[ONREADYSTATECHANGE] = function () {
        html.removeChild(this);
        run(id);
      };
    };
  // Rest old browsers
  } else {
    defer = function (id) {
      setTimeout(runner(id), 0);
    };
  }
}

module.exports = {
  set: set,
  clear: clear
};


/***/ }),
/* 110 */
/***/ (function(module, exports, __webpack_require__) {

var getBuiltIn = __webpack_require__(17);

module.exports = getBuiltIn('document', 'documentElement');


/***/ }),
/* 111 */
/***/ (function(module, exports, __webpack_require__) {

var userAgent = __webpack_require__(25);

// eslint-disable-next-line redos/no-vulnerable -- safe
module.exports = /(?:ipad|iphone|ipod).*applewebkit/i.test(userAgent);


/***/ }),
/* 112 */
/***/ (function(module, exports) {

var Queue = function () {
  this.head = null;
  this.tail = null;
};

Queue.prototype = {
  add: function (item) {
    var entry = { item: item, next: null };
    var tail = this.tail;
    if (tail) tail.next = entry;
    else this.head = entry;
    this.tail = entry;
  },
  get: function () {
    var entry = this.head;
    if (entry) {
      var next = this.head = entry.next;
      if (next === null) this.tail = null;
      return entry.item;
    }
  }
};

module.exports = Queue;


/***/ }),
/* 113 */
/***/ (function(module, exports) {

/* global Deno -- Deno case */
module.exports = typeof Deno == 'object' && Deno && typeof Deno.version == 'object';


/***/ }),
/* 114 */
/***/ (function(module, exports, __webpack_require__) {

var bind = __webpack_require__(66);
var call = __webpack_require__(6);
var anObject = __webpack_require__(9);
var tryToString = __webpack_require__(34);
var isArrayIteratorMethod = __webpack_require__(174);
var lengthOfArrayLike = __webpack_require__(23);
var isPrototypeOf = __webpack_require__(26);
var getIterator = __webpack_require__(175);
var getIteratorMethod = __webpack_require__(115);
var iteratorClose = __webpack_require__(176);

var $TypeError = TypeError;

var Result = function (stopped, result) {
  this.stopped = stopped;
  this.result = result;
};

var ResultPrototype = Result.prototype;

module.exports = function (iterable, unboundFunction, options) {
  var that = options && options.that;
  var AS_ENTRIES = !!(options && options.AS_ENTRIES);
  var IS_RECORD = !!(options && options.IS_RECORD);
  var IS_ITERATOR = !!(options && options.IS_ITERATOR);
  var INTERRUPTED = !!(options && options.INTERRUPTED);
  var fn = bind(unboundFunction, that);
  var iterator, iterFn, index, length, result, next, step;

  var stop = function (condition) {
    if (iterator) iteratorClose(iterator, 'normal', condition);
    return new Result(true, condition);
  };

  var callFn = function (value) {
    if (AS_ENTRIES) {
      anObject(value);
      return INTERRUPTED ? fn(value[0], value[1], stop) : fn(value[0], value[1]);
    } return INTERRUPTED ? fn(value, stop) : fn(value);
  };

  if (IS_RECORD) {
    iterator = iterable.iterator;
  } else if (IS_ITERATOR) {
    iterator = iterable;
  } else {
    iterFn = getIteratorMethod(iterable);
    if (!iterFn) throw $TypeError(tryToString(iterable) + ' is not iterable');
    // optimisation for array iterators
    if (isArrayIteratorMethod(iterFn)) {
      for (index = 0, length = lengthOfArrayLike(iterable); length > index; index++) {
        result = callFn(iterable[index]);
        if (result && isPrototypeOf(ResultPrototype, result)) return result;
      } return new Result(false);
    }
    iterator = getIterator(iterable, iterFn);
  }

  next = IS_RECORD ? iterable.next : iterator.next;
  while (!(step = call(next, iterator)).done) {
    try {
      result = callFn(step.value);
    } catch (error) {
      iteratorClose(iterator, 'throw', error);
    }
    if (typeof result == 'object' && result && isPrototypeOf(ResultPrototype, result)) return result;
  } return new Result(false);
};


/***/ }),
/* 115 */
/***/ (function(module, exports, __webpack_require__) {

var classof = __webpack_require__(65);
var getMethod = __webpack_require__(47);
var isNullOrUndefined = __webpack_require__(32);
var Iterators = __webpack_require__(55);
var wellKnownSymbol = __webpack_require__(5);

var ITERATOR = wellKnownSymbol('iterator');

module.exports = function (it) {
  if (!isNullOrUndefined(it)) return getMethod(it, ITERATOR)
    || getMethod(it, '@@iterator')
    || Iterators[classof(it)];
};


/***/ }),
/* 116 */
/***/ (function(module, exports, __webpack_require__) {

var NativePromiseConstructor = __webpack_require__(52);
var checkCorrectnessOfIteration = __webpack_require__(177);
var FORCED_PROMISE_CONSTRUCTOR = __webpack_require__(53).CONSTRUCTOR;

module.exports = FORCED_PROMISE_CONSTRUCTOR || !checkCorrectnessOfIteration(function (iterable) {
  NativePromiseConstructor.all(iterable).then(undefined, function () { /* empty */ });
});


/***/ }),
/* 117 */
/***/ (function(module, exports, __webpack_require__) {

var DESCRIPTORS = __webpack_require__(8);
var V8_PROTOTYPE_DEFINE_BUG = __webpack_require__(101);
var definePropertyModule = __webpack_require__(12);
var anObject = __webpack_require__(9);
var toIndexedObject = __webpack_require__(20);
var objectKeys = __webpack_require__(71);

// `Object.defineProperties` method
// https://tc39.es/ecma262/#sec-object.defineproperties
// eslint-disable-next-line es/no-object-defineproperties -- safe
exports.f = DESCRIPTORS && !V8_PROTOTYPE_DEFINE_BUG ? Object.defineProperties : function defineProperties(O, Properties) {
  anObject(O);
  var props = toIndexedObject(Properties);
  var keys = objectKeys(Properties);
  var length = keys.length;
  var index = 0;
  var key;
  while (length > index) definePropertyModule.f(O, key = keys[index++], props[key]);
  return O;
};


/***/ }),
/* 118 */
/***/ (function(module, exports, __webpack_require__) {

var toAbsoluteIndex = __webpack_require__(69);
var lengthOfArrayLike = __webpack_require__(23);
var createProperty = __webpack_require__(76);

var $Array = Array;
var max = Math.max;

module.exports = function (O, start, end) {
  var length = lengthOfArrayLike(O);
  var k = toAbsoluteIndex(start, length);
  var fin = toAbsoluteIndex(end === undefined ? length : end, length);
  var result = $Array(max(fin - k, 0));
  for (var n = 0; k < fin; k++, n++) createProperty(result, n, O[k]);
  result.length = n;
  return result;
};


/***/ }),
/* 119 */
/***/ (function(module, exports, __webpack_require__) {

var wellKnownSymbol = __webpack_require__(5);

exports.f = wellKnownSymbol;


/***/ }),
/* 120 */
/***/ (function(module, exports, __webpack_require__) {

var path = __webpack_require__(121);
var hasOwn = __webpack_require__(7);
var wrappedWellKnownSymbolModule = __webpack_require__(119);
var defineProperty = __webpack_require__(12).f;

module.exports = function (NAME) {
  var Symbol = path.Symbol || (path.Symbol = {});
  if (!hasOwn(Symbol, NAME)) defineProperty(Symbol, NAME, {
    value: wrappedWellKnownSymbolModule.f(NAME)
  });
};


/***/ }),
/* 121 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);

module.exports = global;


/***/ }),
/* 122 */
/***/ (function(module, exports, __webpack_require__) {

var NATIVE_SYMBOL = __webpack_require__(33);

/* eslint-disable es/no-symbol -- safe */
module.exports = NATIVE_SYMBOL && !!Symbol['for'] && !!Symbol.keyFor;


/***/ }),
/* 123 */
/***/ (function(module, exports, __webpack_require__) {

var wellKnownSymbol = __webpack_require__(5);
var create = __webpack_require__(56);
var defineProperty = __webpack_require__(12).f;

var UNSCOPABLES = wellKnownSymbol('unscopables');
var ArrayPrototype = Array.prototype;

// Array.prototype[@@unscopables]
// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables
if (ArrayPrototype[UNSCOPABLES] == undefined) {
  defineProperty(ArrayPrototype, UNSCOPABLES, {
    configurable: true,
    value: create(null)
  });
}

// add a key to Array.prototype[@@unscopables]
module.exports = function (key) {
  ArrayPrototype[UNSCOPABLES][key] = true;
};


/***/ }),
/* 124 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var call = __webpack_require__(6);
var IS_PURE = __webpack_require__(14);
var FunctionName = __webpack_require__(62);
var isCallable = __webpack_require__(3);
var createIteratorConstructor = __webpack_require__(191);
var getPrototypeOf = __webpack_require__(126);
var setPrototypeOf = __webpack_require__(73);
var setToStringTag = __webpack_require__(74);
var createNonEnumerableProperty = __webpack_require__(36);
var defineBuiltIn = __webpack_require__(16);
var wellKnownSymbol = __webpack_require__(5);
var Iterators = __webpack_require__(55);
var IteratorsCore = __webpack_require__(125);

var PROPER_FUNCTION_NAME = FunctionName.PROPER;
var CONFIGURABLE_FUNCTION_NAME = FunctionName.CONFIGURABLE;
var IteratorPrototype = IteratorsCore.IteratorPrototype;
var BUGGY_SAFARI_ITERATORS = IteratorsCore.BUGGY_SAFARI_ITERATORS;
var ITERATOR = wellKnownSymbol('iterator');
var KEYS = 'keys';
var VALUES = 'values';
var ENTRIES = 'entries';

var returnThis = function () { return this; };

module.exports = function (Iterable, NAME, IteratorConstructor, next, DEFAULT, IS_SET, FORCED) {
  createIteratorConstructor(IteratorConstructor, NAME, next);

  var getIterationMethod = function (KIND) {
    if (KIND === DEFAULT && defaultIterator) return defaultIterator;
    if (!BUGGY_SAFARI_ITERATORS && KIND in IterablePrototype) return IterablePrototype[KIND];
    switch (KIND) {
      case KEYS: return function keys() { return new IteratorConstructor(this, KIND); };
      case VALUES: return function values() { return new IteratorConstructor(this, KIND); };
      case ENTRIES: return function entries() { return new IteratorConstructor(this, KIND); };
    } return function () { return new IteratorConstructor(this); };
  };

  var TO_STRING_TAG = NAME + ' Iterator';
  var INCORRECT_VALUES_NAME = false;
  var IterablePrototype = Iterable.prototype;
  var nativeIterator = IterablePrototype[ITERATOR]
    || IterablePrototype['@@iterator']
    || DEFAULT && IterablePrototype[DEFAULT];
  var defaultIterator = !BUGGY_SAFARI_ITERATORS && nativeIterator || getIterationMethod(DEFAULT);
  var anyNativeIterator = NAME == 'Array' ? IterablePrototype.entries || nativeIterator : nativeIterator;
  var CurrentIteratorPrototype, methods, KEY;

  // fix native
  if (anyNativeIterator) {
    CurrentIteratorPrototype = getPrototypeOf(anyNativeIterator.call(new Iterable()));
    if (CurrentIteratorPrototype !== Object.prototype && CurrentIteratorPrototype.next) {
      if (!IS_PURE && getPrototypeOf(CurrentIteratorPrototype) !== IteratorPrototype) {
        if (setPrototypeOf) {
          setPrototypeOf(CurrentIteratorPrototype, IteratorPrototype);
        } else if (!isCallable(CurrentIteratorPrototype[ITERATOR])) {
          defineBuiltIn(CurrentIteratorPrototype, ITERATOR, returnThis);
        }
      }
      // Set @@toStringTag to native iterators
      setToStringTag(CurrentIteratorPrototype, TO_STRING_TAG, true, true);
      if (IS_PURE) Iterators[TO_STRING_TAG] = returnThis;
    }
  }

  // fix Array.prototype.{ values, @@iterator }.name in V8 / FF
  if (PROPER_FUNCTION_NAME && DEFAULT == VALUES && nativeIterator && nativeIterator.name !== VALUES) {
    if (!IS_PURE && CONFIGURABLE_FUNCTION_NAME) {
      createNonEnumerableProperty(IterablePrototype, 'name', VALUES);
    } else {
      INCORRECT_VALUES_NAME = true;
      defaultIterator = function values() { return call(nativeIterator, this); };
    }
  }

  // export additional methods
  if (DEFAULT) {
    methods = {
      values: getIterationMethod(VALUES),
      keys: IS_SET ? defaultIterator : getIterationMethod(KEYS),
      entries: getIterationMethod(ENTRIES)
    };
    if (FORCED) for (KEY in methods) {
      if (BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME || !(KEY in IterablePrototype)) {
        defineBuiltIn(IterablePrototype, KEY, methods[KEY]);
      }
    } else $({ target: NAME, proto: true, forced: BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME }, methods);
  }

  // define iterator
  if ((!IS_PURE || FORCED) && IterablePrototype[ITERATOR] !== defaultIterator) {
    defineBuiltIn(IterablePrototype, ITERATOR, defaultIterator, { name: DEFAULT });
  }
  Iterators[NAME] = defaultIterator;

  return methods;
};


/***/ }),
/* 125 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var fails = __webpack_require__(1);
var isCallable = __webpack_require__(3);
var isObject = __webpack_require__(13);
var create = __webpack_require__(56);
var getPrototypeOf = __webpack_require__(126);
var defineBuiltIn = __webpack_require__(16);
var wellKnownSymbol = __webpack_require__(5);
var IS_PURE = __webpack_require__(14);

var ITERATOR = wellKnownSymbol('iterator');
var BUGGY_SAFARI_ITERATORS = false;

// `%IteratorPrototype%` object
// https://tc39.es/ecma262/#sec-%iteratorprototype%-object
var IteratorPrototype, PrototypeOfArrayIteratorPrototype, arrayIterator;

/* eslint-disable es/no-array-prototype-keys -- safe */
if ([].keys) {
  arrayIterator = [].keys();
  // Safari 8 has buggy iterators w/o `next`
  if (!('next' in arrayIterator)) BUGGY_SAFARI_ITERATORS = true;
  else {
    PrototypeOfArrayIteratorPrototype = getPrototypeOf(getPrototypeOf(arrayIterator));
    if (PrototypeOfArrayIteratorPrototype !== Object.prototype) IteratorPrototype = PrototypeOfArrayIteratorPrototype;
  }
}

var NEW_ITERATOR_PROTOTYPE = !isObject(IteratorPrototype) || fails(function () {
  var test = {};
  // FF44- legacy iterators case
  return IteratorPrototype[ITERATOR].call(test) !== test;
});

if (NEW_ITERATOR_PROTOTYPE) IteratorPrototype = {};
else if (IS_PURE) IteratorPrototype = create(IteratorPrototype);

// `%IteratorPrototype%[@@iterator]()` method
// https://tc39.es/ecma262/#sec-%iteratorprototype%-@@iterator
if (!isCallable(IteratorPrototype[ITERATOR])) {
  defineBuiltIn(IteratorPrototype, ITERATOR, function () {
    return this;
  });
}

module.exports = {
  IteratorPrototype: IteratorPrototype,
  BUGGY_SAFARI_ITERATORS: BUGGY_SAFARI_ITERATORS
};


/***/ }),
/* 126 */
/***/ (function(module, exports, __webpack_require__) {

var hasOwn = __webpack_require__(7);
var isCallable = __webpack_require__(3);
var toObject = __webpack_require__(15);
var sharedKey = __webpack_require__(63);
var CORRECT_PROTOTYPE_GETTER = __webpack_require__(192);

var IE_PROTO = sharedKey('IE_PROTO');
var $Object = Object;
var ObjectPrototype = $Object.prototype;

// `Object.getPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.getprototypeof
// eslint-disable-next-line es/no-object-getprototypeof -- safe
module.exports = CORRECT_PROTOTYPE_GETTER ? $Object.getPrototypeOf : function (O) {
  var object = toObject(O);
  if (hasOwn(object, IE_PROTO)) return object[IE_PROTO];
  var constructor = object.constructor;
  if (isCallable(constructor) && object instanceof constructor) {
    return constructor.prototype;
  } return object instanceof $Object ? ObjectPrototype : null;
};


/***/ }),
/* 127 */
/***/ (function(module, exports) {

// `CreateIterResultObject` abstract operation
// https://tc39.es/ecma262/#sec-createiterresultobject
module.exports = function (value, done) {
  return { value: value, done: done };
};


/***/ }),
/* 128 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var toIntegerOrInfinity = __webpack_require__(28);
var toString = __webpack_require__(11);
var requireObjectCoercible = __webpack_require__(21);

var charAt = uncurryThis(''.charAt);
var charCodeAt = uncurryThis(''.charCodeAt);
var stringSlice = uncurryThis(''.slice);

var createMethod = function (CONVERT_TO_STRING) {
  return function ($this, pos) {
    var S = toString(requireObjectCoercible($this));
    var position = toIntegerOrInfinity(pos);
    var size = S.length;
    var first, second;
    if (position < 0 || position >= size) return CONVERT_TO_STRING ? '' : undefined;
    first = charCodeAt(S, position);
    return first < 0xD800 || first > 0xDBFF || position + 1 === size
      || (second = charCodeAt(S, position + 1)) < 0xDC00 || second > 0xDFFF
        ? CONVERT_TO_STRING
          ? charAt(S, position)
          : first
        : CONVERT_TO_STRING
          ? stringSlice(S, position, position + 2)
          : (first - 0xD800 << 10) + (second - 0xDC00) + 0x10000;
  };
};

module.exports = {
  // `String.prototype.codePointAt` method
  // https://tc39.es/ecma262/#sec-string.prototype.codepointat
  codeAt: createMethod(false),
  // `String.prototype.at` method
  // https://github.com/mathiasbynens/String.prototype.at
  charAt: createMethod(true)
};


/***/ }),
/* 129 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var anObject = __webpack_require__(9);

// `RegExp.prototype.flags` getter implementation
// https://tc39.es/ecma262/#sec-get-regexp.prototype.flags
module.exports = function () {
  var that = anObject(this);
  var result = '';
  if (that.hasIndices) result += 'd';
  if (that.global) result += 'g';
  if (that.ignoreCase) result += 'i';
  if (that.multiline) result += 'm';
  if (that.dotAll) result += 's';
  if (that.unicode) result += 'u';
  if (that.unicodeSets) result += 'v';
  if (that.sticky) result += 'y';
  return result;
};


/***/ }),
/* 130 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Numeral = exports.ELocaleStringStyleOption = exports.ELocaleStringMatcherOption = exports.ELocaleStringCurrencyDisplayOption = void 0;
var _localeString = __webpack_require__(196);
var ELocaleStringMatcherOption;
exports.ELocaleStringMatcherOption = ELocaleStringMatcherOption;
(function (ELocaleStringMatcherOption) {
  ELocaleStringMatcherOption["LookUp"] = "lookup";
  ELocaleStringMatcherOption["BestFit"] = "bestfit";
})(ELocaleStringMatcherOption || (exports.ELocaleStringMatcherOption = ELocaleStringMatcherOption = {}));
var ELocaleStringStyleOption;
exports.ELocaleStringStyleOption = ELocaleStringStyleOption;
(function (ELocaleStringStyleOption) {
  ELocaleStringStyleOption["Decimal"] = "decimal";
  ELocaleStringStyleOption["Currency"] = "currency";
  ELocaleStringStyleOption["Percent"] = "percent";
})(ELocaleStringStyleOption || (exports.ELocaleStringStyleOption = ELocaleStringStyleOption = {}));
var ELocaleStringCurrencyDisplayOption;
exports.ELocaleStringCurrencyDisplayOption = ELocaleStringCurrencyDisplayOption;
(function (ELocaleStringCurrencyDisplayOption) {
  ELocaleStringCurrencyDisplayOption["Symbol"] = "symbol";
  ELocaleStringCurrencyDisplayOption["Code"] = "code";
  ELocaleStringCurrencyDisplayOption["Name"] = "name";
})(ELocaleStringCurrencyDisplayOption || (exports.ELocaleStringCurrencyDisplayOption = ELocaleStringCurrencyDisplayOption = {}));
/**
 * Number formatting class exposing static methods as format (handles every cases)
 * and currency (handle most common currency formatting)
 * Fallback on a polyfill if native method is not supported
 */
var Numeral = /** @class */function () {
  function Numeral() {}
  /**
   * Check if Intl is supported
   */
  Numeral.hasSupport = function () {
    /** @TODO:
     * for now we just use the toLocaleString polyfill
     * when we'll have time, use a real Intl polyfill when native support not there and the native object when available
     */
    // return !!(typeof Intl == 'object' && Intl && typeof Intl.NumberFormat == 'function');
    return false;
  };
  /**
   * Generic number formatting method
   * will handle every options
   */
  Numeral.format = function (value, options) {
    if (options === void 0) {
      options = {};
    }
    if (Numeral.hasSupport()) {
      return new Intl.NumberFormat(Numeral.locale, options).format(value);
    } else {
      if (options.style === ELocaleStringStyleOption.Percent) {
        // debug purpose, we should probably use logger for that @TODO
        console.warn("toLocaleString polyfill does not support \"percent\" style");
      }
      options.currency = options.currency || Numeral.currencyCode; // base currency value used if nothing given
      return (0, _localeString.toLocaleString)(value, Numeral.locale, options);
    }
  };
  /**
   * Format a number or an amount into a locale currency string
   * value to format must be given in the smallest fraction digit of the currency (eg: 100 for 1€)
   */
  Numeral.currency = function (amount, options) {
    var _a;
    if (options === void 0) {
      options = {};
    }
    // set base locale options first (based on locale code)
    var hasAltDecim = _localeString.atlernateDecimalsSymbols.hasOwnProperty(Numeral.locale);
    var opts = {
      currency: amount.currency,
      style: ELocaleStringStyleOption.Currency,
      currencyDisplay: "symbol",
      alternativeDecimalSymbol: hasAltDecim ? _localeString.atlernateDecimalsSymbols[Numeral.locale] : null,
      minimumFractionDigits: _localeString.digits[amount.currency.toLowerCase()]
    };
    // lottery specific preset was given
    if (options.displayPreset) {
      var types = (_a = _localeString.displayPresets[Numeral.locale]) === null || _a === void 0 ? void 0 : _a[options.displayPreset];
      if (types !== undefined) {
        // rewrite the given options
        for (var v in types) {
          if (types.hasOwnProperty(v)) {
            opts[v] = types[v];
          }
        }
      }
    }
    // different options to overwrite were given
    for (var optValue in options) {
      if (options.hasOwnProperty(optValue)) {
        opts[optValue] = options[optValue];
      }
    }
    if (Numeral.hasSupport()) {
      // this currency is supported natively by intl (never happen?)
      var fractionDigits = Intl.NumberFormat(Numeral.locale, {
        style: "currency",
        currency: opts.currency
      }).resolvedOptions().minimumFractionDigits;
      var value = amount.value / Math.pow(10, fractionDigits);
      return Intl.NumberFormat(Numeral.locale, opts).format(value);
    } else {
      return (0, _localeString.toLocaleString)(amount.value, Numeral.locale, opts);
    }
  };
  // Static public members
  Numeral.currencyCode = "EUR";
  Numeral.locale = "fr-FR";
  return Numeral;
}();
exports.Numeral = Numeral;

/***/ }),
/* 131 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

// TODO: Remove from `core-js@4` since it's moved to entry points
__webpack_require__(43);
var uncurryThis = __webpack_require__(106);
var defineBuiltIn = __webpack_require__(16);
var regexpExec = __webpack_require__(94);
var fails = __webpack_require__(1);
var wellKnownSymbol = __webpack_require__(5);
var createNonEnumerableProperty = __webpack_require__(36);

var SPECIES = wellKnownSymbol('species');
var RegExpPrototype = RegExp.prototype;

module.exports = function (KEY, exec, FORCED, SHAM) {
  var SYMBOL = wellKnownSymbol(KEY);

  var DELEGATES_TO_SYMBOL = !fails(function () {
    // String methods call symbol-named RegEp methods
    var O = {};
    O[SYMBOL] = function () { return 7; };
    return ''[KEY](O) != 7;
  });

  var DELEGATES_TO_EXEC = DELEGATES_TO_SYMBOL && !fails(function () {
    // Symbol-named RegExp methods call .exec
    var execCalled = false;
    var re = /a/;

    if (KEY === 'split') {
      // We can't use real regex here since it causes deoptimization
      // and serious performance degradation in V8
      // https://github.com/zloirock/core-js/issues/306
      re = {};
      // RegExp[@@split] doesn't call the regex's exec method, but first creates
      // a new one. We need to return the patched regex when creating the new one.
      re.constructor = {};
      re.constructor[SPECIES] = function () { return re; };
      re.flags = '';
      re[SYMBOL] = /./[SYMBOL];
    }

    re.exec = function () { execCalled = true; return null; };

    re[SYMBOL]('');
    return !execCalled;
  });

  if (
    !DELEGATES_TO_SYMBOL ||
    !DELEGATES_TO_EXEC ||
    FORCED
  ) {
    var uncurriedNativeRegExpMethod = uncurryThis(/./[SYMBOL]);
    var methods = exec(SYMBOL, ''[KEY], function (nativeMethod, regexp, str, arg2, forceStringMethod) {
      var uncurriedNativeMethod = uncurryThis(nativeMethod);
      var $exec = regexp.exec;
      if ($exec === regexpExec || $exec === RegExpPrototype.exec) {
        if (DELEGATES_TO_SYMBOL && !forceStringMethod) {
          // The native String method already delegates to @@method (this
          // polyfilled function), leasing to infinite recursion.
          // We avoid it by directly calling the native @@method method.
          return { done: true, value: uncurriedNativeRegExpMethod(regexp, str, arg2) };
        }
        return { done: true, value: uncurriedNativeMethod(str, regexp, arg2) };
      }
      return { done: false };
    });

    defineBuiltIn(String.prototype, KEY, methods[0]);
    defineBuiltIn(RegExpPrototype, SYMBOL, methods[1]);
  }

  if (SHAM) createNonEnumerableProperty(RegExpPrototype[SYMBOL], 'sham', true);
};


/***/ }),
/* 132 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var charAt = __webpack_require__(128).charAt;

// `AdvanceStringIndex` abstract operation
// https://tc39.es/ecma262/#sec-advancestringindex
module.exports = function (S, index, unicode) {
  return index + (unicode ? charAt(S, index).length : 1);
};


/***/ }),
/* 133 */
/***/ (function(module, exports, __webpack_require__) {

var call = __webpack_require__(6);
var anObject = __webpack_require__(9);
var isCallable = __webpack_require__(3);
var classof = __webpack_require__(22);
var regexpExec = __webpack_require__(94);

var $TypeError = TypeError;

// `RegExpExec` abstract operation
// https://tc39.es/ecma262/#sec-regexpexec
module.exports = function (R, S) {
  var exec = R.exec;
  if (isCallable(exec)) {
    var result = call(exec, R, S);
    if (result !== null) anObject(result);
    return result;
  }
  if (classof(R) === 'RegExp') return call(regexpExec, R, S);
  throw $TypeError('RegExp#exec called on incompatible receiver');
};


/***/ }),
/* 134 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);

// `thisNumberValue` abstract operation
// https://tc39.es/ecma262/#sec-thisnumbervalue
module.exports = uncurryThis(1.0.valueOf);


/***/ }),
/* 135 */
/***/ (function(module, exports) {

var $TypeError = TypeError;
var MAX_SAFE_INTEGER = 0x1FFFFFFFFFFFFF; // 2 ** 53 - 1 == 9007199254740991

module.exports = function (it) {
  if (it > MAX_SAFE_INTEGER) throw $TypeError('Maximum allowed index exceeded');
  return it;
};


/***/ }),
/* 136 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Tools = void 0;
__webpack_require__(10);
__webpack_require__(37);
__webpack_require__(38);
__webpack_require__(39);
__webpack_require__(40);
__webpack_require__(29);
__webpack_require__(41);
__webpack_require__(42);
__webpack_require__(19);
__webpack_require__(27);
__webpack_require__(30);
__webpack_require__(44);
__webpack_require__(95);
__webpack_require__(43);
__webpack_require__(77);
__webpack_require__(78);
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
var __awaiter = void 0 && (void 0).__awaiter || function (thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function (resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function (resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator = void 0 && (void 0).__generator || function (thisArg, body) {
  var _ = {
      label: 0,
      sent: function sent() {
        if (t[0] & 1) throw t[1];
        return t[1];
      },
      trys: [],
      ops: []
    },
    f,
    y,
    t,
    g;
  return g = {
    next: verb(0),
    "throw": verb(1),
    "return": verb(2)
  }, typeof Symbol === "function" && (g[Symbol.iterator] = function () {
    return this;
  }), g;
  function verb(n) {
    return function (v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return {
            value: op[1],
            done: false
          };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return {
      value: op[0] ? op[1] : void 0,
      done: true
    };
  }
};
var Tools = /** @class */function () {
  function Tools() {}
  Tools.assign = function (target) {
    var sources = [];
    for (var _i = 1; _i < arguments.length; _i++) {
      sources[_i - 1] = arguments[_i];
    }
    // Convert target to Object, even if it's null or undefined
    var to = Object(target);
    for (var _a = 0, sources_1 = sources; _a < sources_1.length; _a++) {
      var source = sources_1[_a];
      to = this.merge(source, to);
    }
    return to;
  };
  /**
   * Merges the target's members to the source members
   * @param source the source object
   * @param target the target object
   */
  Tools.merge = function (source, target) {
    if (!source) {
      return target;
    }
    var keys = Object.keys(source);
    var _loop_1 = function _loop_1(key) {
      if (Array.isArray(source[key])) {
        target[key] = [];
        source[key].forEach(function (v) {
          return target[key].push(v);
        });
      } else if (_typeof(source[key]) === "object") {
        if (source[key] !== null) {
          target[key] = target[key] || {};
          this_1.merge(source[key], target[key]);
        } else {
          target[key] = null;
        }
      } else {
        target[key] = source[key];
      }
    };
    var this_1 = this;
    for (var _i = 0, keys_1 = keys; _i < keys_1.length; _i++) {
      var key = keys_1[_i];
      _loop_1(key);
    }
    return target;
  };
  /**
   * Returns a perfect clone of the given object.
   * @param object the object to clone.
   * @warning take care of cyclic references.
   * @warning should never be a class, only simple objects.
   */
  Tools.cloneObject = function (object) {
    return JSON.parse(JSON.stringify(object));
  };
  /**
   * Returns the extension of the file
   * @param filename the filename
   */
  Tools.getExtension = function (filename) {
    var lastDot = filename.lastIndexOf(".");
    return filename.substring(lastDot + 1);
  };
  /**
   * Returns the filename without extension
   * @param filename: the filename (path)
   * @param withPath: if the return value should contain all path
   */
  Tools.getFilenameWithoutExtension = function (filename, withPath) {
    var lastDot = filename.lastIndexOf(".");
    var lastSlash = filename.lastIndexOf("/");
    if (lastSlash === -1) {
      lastSlash = filename.lastIndexOf("\\"); // Windows
    }

    return filename.substring(withPath ? 0 : lastSlash + 1, lastDot);
  };
  /**
   * Returns the filename
   * @param filename: the complete filename with path
   */
  Tools.getFilename = function (filename) {
    return "".concat(this.getFilenameWithoutExtension(filename, false), ".").concat(this.getExtension(filename));
  };
  /**
   * Returns the folder name containing the given filename
   * @param filename the filename
   */
  Tools.getFolder = function (filename) {
    var index = filename.lastIndexOf("/");
    if (index === -1) {
      return "./";
    }
    return filename.substr(0, index);
  };
  /**
   * Returns the constructor name of the given object
   * @param obj the object
   */
  Tools.getConstructorName = function (obj) {
    var ctrName = obj && obj.constructor ? obj.constructor.name : "";
    if (ctrName === "") {
      ctrName = _typeof(obj);
    }
    return ctrName;
  };
  /**
   * Creates a new native popup
   * @param url: the URL of the web page
   * @param title: the title of the popup
   * @param width: the with of the popup. 800px by default
   * @param height: the height of the popup. 600px by default
   */
  Tools.createPopup = function (url, title, width, height) {
    if (width === void 0) {
      width = 800;
    }
    if (height === void 0) {
      height = 600;
    }
    var features = ["width=".concat(width), "height=".concat(height), "top=".concat(window.screenY + Math.max(window.outerHeight - height, 0) / 2), "left=".concat(window.screenX + Math.max(window.outerWidth - width, 0) / 2), "status=no", "resizable=yes", "toolbar=no", "menubar=no", "scrollbars=yes"].join(",");
    var popup = window.open(url, title, features);
    popup.focus();
    return popup;
  };
  /**
   * Generates an uuid. As it relies on Math.random it may not be truly unique,
   * however it is strong enough for our needs.
   */
  Tools.uuidv4 = function () {
    return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, function (c) {
      var r = Math.random() * 16 | 0;
      var v = c === "x" ? r : r & 0x3 | 0x8;
      return v.toString(16);
    });
  };
  /**
   * Returns the nearest power of two value
   * @param value the value get the power of 2
   * @param max the max value of the power of 2
   */
  Tools.getPowerOfTwo = function (value, max) {
    if (max === void 0) {
      max = 8192;
    }
    var count = 1;
    // Remove comma in order to get nearest power of two.
    // i.e: 32.154266 should return 32 :)
    value = value >> 0;
    while (count < max) {
      if (count >= value) {
        return count;
      }
      count = count << 1;
    }
    return count;
  };
  /**
   * Loads a file given its URL
   * @param url: the url of the file to load
   * @param arrayBuffer: if the response should be an array buffer instead of a text
   * @param callback: optional callback if not using promises
   */
  Tools.loadFile = function (url, arrayBuffer, callback) {
    return __awaiter(this, void 0, void 0, function () {
      return __generator(this, function (_a) {
        return [2 /*return*/, new Promise(function (resolve, reject) {
          var request = new XMLHttpRequest();
          request.open("GET", url, true);
          if (arrayBuffer) {
            request.responseType = "arraybuffer";
          }
          request.onreadystatechange = function () {
            // In case of undefined state in some browsers.
            if (request.readyState === (XMLHttpRequest.DONE || 4)) {
              // Some browsers have issues where onreadystatechange can be called multiple times with the same value
              request.onreadystatechange = function () {
                return undefined;
              };
              if (request.status >= 200 && request.status < 300) {
                // Success
                var data = !arrayBuffer ? request.responseText : request.response;
                if (callback) {
                  callback(data);
                }
                resolve(data);
              } else {
                // Failed
                reject(new Error("Error status: ".concat(request.status, " - Unable to load ").concat(url)));
              }
            }
          };
          request.send(null);
        })];
      });
    });
  };
  /**
   * Injects the given script code (javascript) to the DOM
   * @param code the code of the script
   * @param name the name of the script
   * @param namespace the namespace of the script (to help debugging in devtools)
   */
  Tools.injectScript = function (code, name, namespace) {
    if (namespace === void 0) {
      namespace = "scripts";
    }
    // Get url
    var url = window.location.href.split("?")[0];
    url = "".concat(url.replace(Tools.getFilename(url), "")).concat(namespace, "/");
    // Create script
    var script = document.createElement("script");
    script.type = "text/javascript";
    script.text = "".concat(code, "\n//# sourceURL=").concat(url).concat(name.replace(/ /g, ""), ".js\n");
    document.head.appendChild(script);
    return script;
  };
  /**
   * Imports the given script identified by its name or URL
   * @param keyOrUrl the name of the script to import
   * @param baseURL the base Url used to request the dependencies of the script
   */
  Tools.importScript = function (keyOrUrl, baseURL) {
    return __awaiter(this, void 0, void 0, function () {
      var exports;
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            if (baseURL) {
              SystemJS.config({
                baseURL: baseURL
              });
            }
            return [4 /*yield*/, SystemJS.import(keyOrUrl)];
          case 1:
            exports = _a.sent();
            return [2 /*return*/, exports];
        }
      });
    });
  };
  /**
   * Returns the already script loaded using Tools.importScript or Tools.injectScript in case of a SystemJS module
   * @param key the key of the script that has been registered
   */
  Tools.getScript = function (key) {
    return SystemJS.get(key);
  };
  return Tools;
}();
exports.Tools = Tools;

/***/ }),
/* 137 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var tryToString = __webpack_require__(34);

var $TypeError = TypeError;

module.exports = function (O, P) {
  if (!delete O[P]) throw $TypeError('Cannot delete property ' + tryToString(P) + ' of ' + tryToString(O));
};


/***/ }),
/* 138 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Settings = void 0;
var _orientation = __webpack_require__(139);
var Settings = /** @class */function () {
  function Settings() {
    // Public members
    this.orientation = _orientation.EOrientation.Landscape;
    this.language = "fr-FR";
    this.versions = {};
  }
  return Settings;
}();
exports.Settings = Settings;

/***/ }),
/* 139 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EOrientation = void 0;
/**
 * Enum which represents the game orientation
 */
var EOrientation;
exports.EOrientation = EOrientation;
(function (EOrientation) {
  EOrientation[EOrientation["Both"] = 0] = "Both";
  EOrientation[EOrientation["Portrait"] = 1] = "Portrait";
  EOrientation[EOrientation["Landscape"] = 2] = "Landscape";
})(EOrientation || (exports.EOrientation = EOrientation = {}));

/***/ }),
/* 140 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.LoaderError = exports.Loader = void 0;
__webpack_require__(141);
__webpack_require__(10);
__webpack_require__(37);
__webpack_require__(38);
__webpack_require__(39);
__webpack_require__(40);
__webpack_require__(29);
__webpack_require__(41);
__webpack_require__(42);
__webpack_require__(44);
__webpack_require__(30);
__webpack_require__(27);
var _i18next = _interopRequireDefault(__webpack_require__(221));
var _default = __webpack_require__(142);
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
var __extends = void 0 && (void 0).__extends || function () {
  var _extendStatics = function extendStatics(d, b) {
    _extendStatics = Object.setPrototypeOf || {
      __proto__: []
    } instanceof Array && function (d, b) {
      d.__proto__ = b;
    } || function (d, b) {
      for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
    };
    return _extendStatics(d, b);
  };
  return function (d, b) {
    if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    _extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __awaiter = void 0 && (void 0).__awaiter || function (thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function (resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function (resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator = void 0 && (void 0).__generator || function (thisArg, body) {
  var _ = {
      label: 0,
      sent: function sent() {
        if (t[0] & 1) throw t[1];
        return t[1];
      },
      trys: [],
      ops: []
    },
    f,
    y,
    t,
    g;
  return g = {
    next: verb(0),
    "throw": verb(1),
    "return": verb(2)
  }, typeof Symbol === "function" && (g[Symbol.iterator] = function () {
    return this;
  }), g;
  function verb(n) {
    return function (v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return {
            value: op[1],
            done: false
          };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return {
      value: op[0] ? op[1] : void 0,
      done: true
    };
  }
};
var LoaderError = /** @class */function (_super) {
  __extends(LoaderError, _super);
  function LoaderError(message) {
    var _this = _super.call(this, message) || this;
    _this.name = "LoaderError";
    return _this;
  }
  return LoaderError;
}(Error);
exports.LoaderError = LoaderError;
var Loader = /** @class */function () {
  /**
   * Constructor
   */
  function Loader(game) {
    this.game = game;
    this.assetsPath = "";
    // Protected members
    this.filesCount = 0;
    this.baseUrl = "./";
  }
  /**
   * Registers a loader which implements the interface ILoader
   * @param loader the loader to register
   */
  Loader.register = function (loader) {
    Loader._loaders.unshift(loader);
  };
  /**
   * Returns the resource identified by the given key
   * @param key the key of the value to get
   */
  Loader.get = function (key, raw) {
    for (var _i = 0, _a = Loader._loaders; _i < _a.length; _i++) {
      var loader = _a[_i];
      var resource = loader.get(key, raw);
      if (resource) {
        return resource;
      }
    }
    return null;
  };
  Object.defineProperty(Loader, "loaders", {
    get: function get() {
      return [].concat(Loader._loaders);
    },
    enumerable: false,
    configurable: true
  });
  Object.defineProperty(Loader.prototype, "defaultLoader", {
    /**
     * Returns the default loader
     */
    get: function get() {
      return Loader._loaders[0];
    },
    enumerable: false,
    configurable: true
  });
  Object.defineProperty(Loader.prototype, "baseUrl", {
    /**
     * Gets the base Url of the loader
     */
    get: function get() {
      return this._baseUrl;
    },
    /**
     * Sets the base Url of the loader
     */
    set: function set(url) {
      this._baseUrl = url;
      Loader._loaders.forEach(function (l) {
        return l.baseUrl = url;
      });
    },
    enumerable: false,
    configurable: true
  });
  /**
   * Add the given file to the right loader
   * @param file: the file to add to the proper loader
   * @param key: optional, the key of the asset when calling get(key);
   */
  Loader.prototype.add = function (file, key, options) {
    if (typeof file !== "string") {
      return this._addFileAdvanced(file, key);
    }
    var extension = file.substring(file.lastIndexOf(".") + 1);
    var supported = false;
    var supportedLoader = null;
    for (var _i = 0, _a = Loader._loaders; _i < _a.length; _i++) {
      supportedLoader = _a[_i];
      supported = supportedLoader.extensions.indexOf(extension) !== -1 || supportedLoader.extensions.length === 0;
      if (supported) {
        break;
      }
    }
    if (supported) {
      supportedLoader.add(file, key || file, options);
    } else {
      throw new Error("No loader found for extension \"".concat(extension, "\""));
    }
    this.filesCount++;
  };
  /**
   * Clears all the loaders
   */
  Loader.prototype.clear = function () {
    this.filesCount = 0;
    Loader._loaders.forEach(function (l) {
      if (l.clear) {
        l.clear();
      }
    });
  };
  /**
   * Removes the given entry identified by the given key
   * @param key the key used to identify the entry to remove
   */
  Loader.prototype.remove = function (key) {
    Loader._loaders.forEach(function (l) {
      return l.remove && l.remove(key);
    });
  };
  /**
   * Calls all loaders
   * @param callback called when all loaders have loaded their files
   */
  Loader.prototype.load = function (callback) {
    return __awaiter(this, void 0, void 0, function () {
      var _this = this;
      return __generator(this, function (_a) {
        return [2 /*return*/, new Promise(function (resolve, reject) {
          var length = Loader._loaders.length;
          var loadersCount = 0;
          var filesCount = 0;
          for (var _i = 0, _a = Loader._loaders; _i < _a.length; _i++) {
            var loader = _a[_i];
            loader.load(function () {
              // All loaded
              loadersCount++;
              if (loadersCount === length) {
                if (callback) {
                  callback();
                }
                resolve();
              }
            }, function (loadedFile) {
              // Progress
              filesCount++;
              if (_this.onProgress) {
                var percentage = Math.min(filesCount * 100 / _this.filesCount, 100) >> 0; // remove comma
                _this.onProgress(percentage, loadedFile);
              }
            }, function (fileError) {
              // Error
              if (_this.onError) {
                _this.onError(fileError);
              }
              reject(fileError);
            });
          }
        })];
      });
    });
  };
  /**
   * Returns the resource identified by the given key
   * @param key the key of the value to get
   */
  Loader.prototype.get = function (key, raw) {
    return Loader.get(key, raw);
  };
  Object.defineProperty(Loader.prototype, "loaders", {
    get: function get() {
      return Loader.loaders;
    },
    enumerable: false,
    configurable: true
  });
  /**
   * Configures the loader from the current theme's base Url
   * @param language the language configuration of the game (mandatory to know which locales to load)
   * @param baseUrl the base Url which contains the files.json file and all other files to load
   * @param i18nBaseUrl the base Url where to find i18n files (not necessary the same folder or ../)
   * @example loader.baseUrl = urlOfThemeFolder;
   * @example loader.configureFromTheme("vi", "fr-FR");
   * @example await loader.load();
   * @example const locales = this.loader.get("vi-i18n-base"); // Get main locales (i18n)
   * @example const overridenLocales = this.loader.get("vi-i18n"); // Get overriden locales (i18n)
   */
  Loader.prototype.configureFromTheme = function (namespace, language, baseUrl, i18nBaseUrl) {
    if (baseUrl === void 0) {
      baseUrl = "";
    }
    if (i18nBaseUrl === void 0) {
      i18nBaseUrl = "";
    }
    return __awaiter(this, void 0, void 0, function () {
      var tempLoader, themeAssets, key, asset;
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            tempLoader = new _default.DefaultLoader();
            tempLoader.baseUrl = this.baseUrl;
            tempLoader.add("".concat(baseUrl, "files.json"), "files-base");
            return [4 /*yield*/, tempLoader.load()];
          case 1:
            _a.sent();
            themeAssets = tempLoader.get("files-base");
            for (key in themeAssets) {
              if (!themeAssets.hasOwnProperty(key)) {
                continue;
              }
              asset = themeAssets[key];
              if (key === "i18n") {
                this.add(baseUrl + asset[language], "".concat(namespace, "-i18n"));
              } else {
                // Just fonts, images, json, mp3, ogg, etc.
                if (typeof asset === "string") {
                  this.add(baseUrl + asset, key);
                } else {
                  this._addFileAdvanced(asset, key, baseUrl);
                }
              }
            }
            // Finally add the i18n file and load
            this.add("".concat(i18nBaseUrl).concat(language, ".json"), "".concat(namespace, "-i18n-base"));
            return [2 /*return*/];
        }
      });
    });
  };
  /**
   * Parses the game conf and adds files to load (assets, i18n, etc.)
   * @param conf the game configuration object to parse
   */
  Loader.prototype.loadFrom = function (conf, callback) {
    var _this = this;
    if (!conf) {
      return;
    }
    // Theme
    var theme = conf.gameDescription.assets.themes[conf.selectedTheme];
    // Locales
    this.game.settings.language = conf.selectedLocale;
    // Load skin's file which defines all the files to load (images, fonts, etc.)
    var tempLoader = new _default.DefaultLoader();
    tempLoader.baseUrl = this._baseUrl;
    tempLoader.add("".concat(this.assetsPath).concat(theme, "/files.json"), "files-base");
    // Load skin description, then load the entire skin in the callback :)
    tempLoader.load(function () {
      // Default i18n files
      var currenti18n = conf.gameDescription.assets.i18n[conf.selectedLocale];
      if (currenti18n) {
        _this.add(_this.assetsPath + currenti18n, "i18n-base");
      }
      // Assets
      var themeAssets = tempLoader.get("files-base");
      for (var key in themeAssets) {
        if (!themeAssets.hasOwnProperty(key)) {
          continue;
        }
        var asset = themeAssets[key];
        if (key === "version") {
          _this.game.settings.versions.theme = themeAssets[key];
        } else if (key === "i18n") {
          var file = asset[conf.selectedLocale];
          if (typeof file !== "string") {
            throw new LoaderError("Expected file path to be set for locale \"".concat(conf.selectedLocale, "\"") + " (found <".concat(_typeof(file), "> instead of <string>;") + " did you forget to include your locale file" + " for locale \"".concat(conf.selectedLocale, "\" in your project") + "".concat(theme !== "" ? ', for theme "' + theme + '"' : "") + " ?)");
          }
          _this.add(_this.assetsPath + (theme === "" ? "" : "".concat(theme, "/")) + file, key);
        } else {
          // Just fonts, images, json, mp3, ogg, etc.
          if (typeof asset === "string") {
            _this.add(_this.assetsPath + (theme === "" ? "" : "".concat(theme, "/")) + asset, key);
          } else {
            _this._addFileAdvanced(asset, key, theme);
          }
        }
      }
      // Load all assets! Then, config i18n tell that loading is done
      _this.load(function () {
        _this._i18nConfig(conf.selectedLocale);
        callback();
      });
    });
  };
  // Adds the file
  Loader.prototype._addFileAdvanced = function (asset, key, theme) {
    if (theme === void 0) {
      theme = "";
    }
    // Internationalized?
    var language = this.game.settings.language;
    var assetPath = asset[language] || asset[language.toLowerCase()] || asset.default;
    if (!assetPath) {
      return;
    }
    this.add(this.assetsPath + (theme === "" ? "" : "".concat(theme, "/")) + assetPath, key || asset.default, asset.options);
  };
  // Configures the game
  Loader.prototype._i18nConfig = function (language) {
    var locales = this.get("i18n-base");
    if (locales.version) {
      this.game.settings.versions.i18n = locales.version;
    }
    _i18next.default.addResourceBundle(language, "game", locales, true, true);
    var overridedLocales = this.get("i18n");
    if (overridedLocales) {
      _i18next.default.addResourceBundle(language, "game", overridedLocales, true, true);
    }
  };
  // Public members
  // Static members
  Loader._loaders = [];
  return Loader;
}();
exports.Loader = Loader;
Loader.register(new _default.DefaultLoader());

/***/ }),
/* 141 */
/***/ (function(module, exports, __webpack_require__) {

var $ = __webpack_require__(2);
var setPrototypeOf = __webpack_require__(73);

// `Object.setPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.setprototypeof
$({ target: 'Object', stat: true }, {
  setPrototypeOf: setPrototypeOf
});


/***/ }),
/* 142 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.DefaultLoader = void 0;
__webpack_require__(10);
__webpack_require__(37);
__webpack_require__(38);
__webpack_require__(39);
__webpack_require__(40);
__webpack_require__(29);
__webpack_require__(41);
__webpack_require__(42);
__webpack_require__(19);
__webpack_require__(30);
var _tools = __webpack_require__(57);
var __awaiter = void 0 && (void 0).__awaiter || function (thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function (resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function (resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator = void 0 && (void 0).__generator || function (thisArg, body) {
  var _ = {
      label: 0,
      sent: function sent() {
        if (t[0] & 1) throw t[1];
        return t[1];
      },
      trys: [],
      ops: []
    },
    f,
    y,
    t,
    g;
  return g = {
    next: verb(0),
    "throw": verb(1),
    "return": verb(2)
  }, typeof Symbol === "function" && (g[Symbol.iterator] = function () {
    return this;
  }), g;
  function verb(n) {
    return function (v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return {
            value: op[1],
            done: false
          };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return {
      value: op[0] ? op[1] : void 0,
      done: true
    };
  }
};
var DefaultLoader = /** @class */function () {
  function DefaultLoader() {
    this.extensions = [];
    this.baseUrl = "";
    this.filesToLoad = {};
  }
  /**
   * Add the given file to the files to load
   * @param filename the file to add
   */
  DefaultLoader.prototype.add = function (filename, key) {
    this.filesToLoad[key || filename] = {
      data: null,
      filename: filename
    };
  };
  /**
   * Clears the files to load dictionary
   */
  DefaultLoader.prototype.clear = function () {
    this.filesToLoad = {};
  };
  /**
   * Returns the resource identified by the given key
   * @param key the key of the value to get
   */
  DefaultLoader.prototype.get = function (key) {
    for (var file in this.filesToLoad) {
      if (file === key) {
        return this.filesToLoad[file].data;
      }
    }
    return null;
  };
  /**
   * Load all files previously added
   * @param callback called when all files loaded
   */
  DefaultLoader.prototype.load = function (callback, progress, error) {
    return __awaiter(this, void 0, void 0, function () {
      var _this = this;
      return __generator(this, function (_a) {
        return [2 /*return*/, new Promise(function (resolve, reject) {
          var length = Object.keys(_this.filesToLoad).length;
          if (length === 0) {
            return callback();
          }
          var count = 0;
          for (var key in _this.filesToLoad) {
            if (!_this.filesToLoad.hasOwnProperty(key)) {
              continue;
            }
            var file = _this.filesToLoad[key];
            _this.loadFile(file.filename, key, false, function (loadedFile) {
              count++;
              if (progress) {
                progress(loadedFile);
              }
              if (count === length) {
                if (callback) {
                  callback();
                }
                resolve();
              }
            }, function (fileWithError) {
              if (error) {
                error(fileWithError);
              }
              reject(fileWithError);
            });
          }
        })];
      });
    });
  };
  // Loads the given file and callback when done
  DefaultLoader.prototype.loadFile = function (filename, key, useArrayBuffer, callback, error) {
    var _this = this;
    // If file alredy exists
    if (this.filesToLoad[key].data) {
      return callback(filename);
    }
    // Send request
    var request = new XMLHttpRequest();
    request.open("GET", this.baseUrl + filename, true);
    if (useArrayBuffer) {
      request.responseType = "arraybuffer";
    }
    request.onreadystatechange = function () {
      if (request.readyState !== (XMLHttpRequest.DONE || 4)) {
        return;
      }
      request.onreadystatechange = null;
      if (request.status >= 200 && request.status < 300) {
        if (_tools.Tools.getExtension(filename) === "json") {
          _this.filesToLoad[key].data = JSON.parse(request.responseText);
        } else {
          _this.filesToLoad[key].data = request.responseText;
        }
        callback(filename);
      } else {
        error(filename);
        throw new Error("Error status: ".concat(request.status, " - Unable to load ").concat(request.responseURL));
      }
    };
    request.send();
  };
  return DefaultLoader;
}();
exports.DefaultLoader = DefaultLoader;

/***/ }),
/* 143 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
Object.defineProperty(exports, "ButtonClick", {
  enumerable: true,
  get: function get() {
    return _click.ButtonClick;
  }
});
Object.defineProperty(exports, "CLICK_EVENT", {
  enumerable: true,
  get: function get() {
    return _click.CLICK_EVENT;
  }
});
Object.defineProperty(exports, "ClickEvent", {
  enumerable: true,
  get: function get() {
    return _click.ClickEvent;
  }
});
Object.defineProperty(exports, "EEventType", {
  enumerable: true,
  get: function get() {
    return _event.EEventType;
  }
});
Object.defineProperty(exports, "ESettingsEvent", {
  enumerable: true,
  get: function get() {
    return _settings.ESettingsEvent;
  }
});
Object.defineProperty(exports, "ESlideEvent", {
  enumerable: true,
  get: function get() {
    return _slide.ESlideEvent;
  }
});
Object.defineProperty(exports, "EViewState", {
  enumerable: true,
  get: function get() {
    return _view.EViewState;
  }
});
Object.defineProperty(exports, "ErrorEvent", {
  enumerable: true,
  get: function get() {
    return _error.ErrorEvent;
  }
});
Object.defineProperty(exports, "GameLoaded", {
  enumerable: true,
  get: function get() {
    return _game.GameLoaded;
  }
});
Object.defineProperty(exports, "GameQuit", {
  enumerable: true,
  get: function get() {
    return _game.GameQuit;
  }
});
Object.defineProperty(exports, "LinkClick", {
  enumerable: true,
  get: function get() {
    return _click.LinkClick;
  }
});
Object.defineProperty(exports, "SettingsEvent", {
  enumerable: true,
  get: function get() {
    return _settings.SettingsEvent;
  }
});
Object.defineProperty(exports, "SlideEvent", {
  enumerable: true,
  get: function get() {
    return _slide.SlideEvent;
  }
});
Object.defineProperty(exports, "Tracker", {
  enumerable: true,
  get: function get() {
    return _tracker.Tracker;
  }
});
Object.defineProperty(exports, "ViewStateEvent", {
  enumerable: true,
  get: function get() {
    return _view.ViewStateEvent;
  }
});
exports.tracker = void 0;
var _tracker = __webpack_require__(224);
var _click = __webpack_require__(226);
var _game = __webpack_require__(144);
var _view = __webpack_require__(145);
var _error = __webpack_require__(230);
var _slide = __webpack_require__(231);
var _settings = __webpack_require__(232);
var _event = __webpack_require__(24);
var tracker = new _tracker.Tracker();
exports.tracker = tracker;

/***/ }),
/* 144 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.GameQuit = exports.GameLoaded = exports.EGameEvent = void 0;
var _event = __webpack_require__(24);
var EGameEvent;
exports.EGameEvent = EGameEvent;
(function (EGameEvent) {
  EGameEvent["Loaded"] = "loaded";
  EGameEvent["Quit"] = "quit";
})(EGameEvent || (exports.EGameEvent = EGameEvent = {}));
var GameLoaded = /** @class */function () {
  function GameLoaded(gameInfo) {
    this.gameInfo = gameInfo;
    this.eventType = _event.EEventType.Game;
    this.event = EGameEvent.Loaded;
  }
  return GameLoaded;
}();
exports.GameLoaded = GameLoaded;
var GameQuit = /** @class */function () {
  function GameQuit() {
    this.eventType = _event.EEventType.Game;
    this.event = EGameEvent.Quit;
  }
  return GameQuit;
}();
exports.GameQuit = GameQuit;

/***/ }),
/* 145 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ViewStateEvent = exports.EViewState = exports.EViewEvent = void 0;
__webpack_require__(44);
var _event = __webpack_require__(24);
var EViewState;
exports.EViewState = EViewState;
(function (EViewState) {
  EViewState["WillDisappear"] = "willDisappear";
  EViewState["DidDisappear"] = "didDisappear";
  EViewState["WillAppear"] = "willAppear";
  EViewState["DidAppear"] = "didAppear";
})(EViewState || (exports.EViewState = EViewState = {}));
var EViewEvent;
exports.EViewEvent = EViewEvent;
(function (EViewEvent) {
  EViewEvent["StateChange"] = "state";
})(EViewEvent || (exports.EViewEvent = EViewEvent = {}));
var ViewStateEvent = /** @class */function () {
  function ViewStateEvent(name, state) {
    this.name = name;
    this.eventType = _event.EEventType.View;
    this.event = EViewEvent.StateChange;
    this.viewInfo = {
      name: name,
      state: state
    };
  }
  return ViewStateEvent;
}();
exports.ViewStateEvent = ViewStateEvent;

/***/ }),
/* 146 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.HowlerLoader = void 0;
__webpack_require__(19);
__webpack_require__(43);
__webpack_require__(77);
__webpack_require__(10);
__webpack_require__(27);
var _howler = __webpack_require__(239);
var HowlerLoader = /** @class */function () {
  function HowlerLoader() {
    // Public members
    this.extensions = ["mp3", "ogg", "webm", "wav", "snd"];
    this.baseUrl = "./";
  }
  /**
   * Add the given file to the files to load
   * @param filename the file to add
   * @param key the key of the asset (works as a shortcut)
   */
  HowlerLoader.prototype.add = function (filename, key) {
    HowlerLoader.sounds[key || filename] = {
      filename: filename,
      data: null,
      layer: -1
    };
  };
  /**
   * Clears the files to load dictionary
   */
  HowlerLoader.prototype.clear = function () {
    for (var file in HowlerLoader.sounds) {
      if (HowlerLoader.sounds[file].data) {
        HowlerLoader.sounds[file].data.unload();
      }
    }
    HowlerLoader.sounds = {};
  };
  /**
   * Returns the resource identified by the given key
   * @param key the key of the value to get
   */
  HowlerLoader.prototype.get = function (key) {
    var value = HowlerLoader.sounds[key];
    if (!value) {
      return null;
    }
    return value.data;
  };
  /**
   * Load all files previously added
   * @param callback called when all files loaded
   * @param progress called when a file has been loaded
   * @param error called when an error happens
   */
  HowlerLoader.prototype.load = function (callback, progress, error) {
    var keys = Object.keys(HowlerLoader.sounds);
    if (keys.length === 0) {
      return callback();
    }
    var count = 0;
    for (var _i = 0, keys_1 = keys; _i < keys_1.length; _i++) {
      var key = keys_1[_i];
      var file = HowlerLoader.sounds[key];
      this._createInstance(file.filename, key, function (loadedFile) {
        count++;
        if (progress) {
          progress(loadedFile);
        }
        if (count === keys.length) {
          callback();
        }
      }, function (filename) {
        if (error) {
          error(filename);
        }
      });
    }
  };
  // Returns the possible sources of sound files to load according to
  // supported extensions
  HowlerLoader.prototype._getSrc = function (filename) {
    var _this = this;
    var src = [];
    var extension = filename.substring(filename.lastIndexOf(".") + 1);
    var base = filename.replace(extension, "");
    // Add all supported extensions to src and let Howler just the needed one
    this.extensions.forEach(function (e) {
      return src.push(_this.baseUrl + base + e);
    });
    return src;
  };
  // Creates a new instanceof of Howler sound
  HowlerLoader.prototype._createInstance = function (filename, key, callback, error) {
    if (HowlerLoader.sounds[key].data) {
      return callback(filename);
    }
    var src = this._getSrc(filename);
    // Create new instance
    HowlerLoader.sounds[key].data = new _howler.Howl({
      src: src,
      onload: function onload() {
        callback(filename);
      },
      onloaderror: function onloaderror(soundId, err) {
        // Howler already throws an error, don't need to throw here
        error(filename);
      }
    });
  };
  HowlerLoader.sounds = {};
  return HowlerLoader;
}();
exports.HowlerLoader = HowlerLoader;

/***/ }),
/* 147 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(10);
__webpack_require__(27);
__webpack_require__(19);
Object.defineProperty(exports, "__esModule", {
  value: true
});
var _exportNames = {
  VERSION: true
};
exports.VERSION = void 0;
var _core = __webpack_require__(157);
Object.keys(_core).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _core[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _core[key];
    }
  });
});
var _logger = __webpack_require__(234);
Object.keys(_logger).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _logger[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _logger[key];
    }
  });
});
var _sound = __webpack_require__(238);
Object.keys(_sound).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _sound[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _sound[key];
    }
  });
});
var _tools = __webpack_require__(57);
Object.keys(_tools).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _tools[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _tools[key];
    }
  });
});
var _tracking = __webpack_require__(143);
Object.keys(_tracking).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _tracking[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _tracking[key];
    }
  });
});
var _decorators = __webpack_require__(241);
Object.keys(_decorators).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _decorators[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _decorators[key];
    }
  });
});
var VERSION = "3.6.4";
exports.VERSION = VERSION;

/***/ }),
/* 148 */
/***/ (function(module, exports) {

var g;

// This works in non-strict mode
g = (function() {
	return this;
})();

try {
	// This works if eval is allowed (see CSP)
	g = g || new Function("return this")();
} catch (e) {
	// This works if the window reference is available
	if (typeof window === "object") g = window;
}

// g can still be undefined, but nothing to do about it...
// We return undefined, instead of nothing here, so it's
// easier to handle this case. if(!global) { ...}

module.exports = g;


/***/ }),
/* 149 */
/***/ (function(module, exports, __webpack_require__) {

var call = __webpack_require__(6);
var isCallable = __webpack_require__(3);
var isObject = __webpack_require__(13);

var $TypeError = TypeError;

// `OrdinaryToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-ordinarytoprimitive
module.exports = function (input, pref) {
  var fn, val;
  if (pref === 'string' && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
  if (isCallable(fn = input.valueOf) && !isObject(val = call(fn, input))) return val;
  if (pref !== 'string' && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
  throw $TypeError("Can't convert object to primitive value");
};


/***/ }),
/* 150 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var isCallable = __webpack_require__(3);

var WeakMap = global.WeakMap;

module.exports = isCallable(WeakMap) && /native code/.test(String(WeakMap));


/***/ }),
/* 151 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var TO_STRING_TAG_SUPPORT = __webpack_require__(79);
var classof = __webpack_require__(65);

// `Object.prototype.toString` method implementation
// https://tc39.es/ecma262/#sec-object.prototype.tostring
module.exports = TO_STRING_TAG_SUPPORT ? {}.toString : function toString() {
  return '[object ' + classof(this) + ']';
};


/***/ }),
/* 152 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $forEach = __webpack_require__(49).forEach;
var arrayMethodIsStrict = __webpack_require__(87);

var STRICT_METHOD = arrayMethodIsStrict('forEach');

// `Array.prototype.forEach` method implementation
// https://tc39.es/ecma262/#sec-array.prototype.foreach
module.exports = !STRICT_METHOD ? function forEach(callbackfn /* , thisArg */) {
  return $forEach(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
// eslint-disable-next-line es/no-array-prototype-foreach -- safe
} : [].forEach;


/***/ }),
/* 153 */
/***/ (function(module, exports) {

var ceil = Math.ceil;
var floor = Math.floor;

// `Math.trunc` method
// https://tc39.es/ecma262/#sec-math.trunc
// eslint-disable-next-line es/no-math-trunc -- safe
module.exports = Math.trunc || function trunc(x) {
  var n = +x;
  return (n > 0 ? floor : ceil)(n);
};


/***/ }),
/* 154 */
/***/ (function(module, exports, __webpack_require__) {

var isArray = __webpack_require__(50);
var isConstructor = __webpack_require__(86);
var isObject = __webpack_require__(13);
var wellKnownSymbol = __webpack_require__(5);

var SPECIES = wellKnownSymbol('species');
var $Array = Array;

// a part of `ArraySpeciesCreate` abstract operation
// https://tc39.es/ecma262/#sec-arrayspeciescreate
module.exports = function (originalArray) {
  var C;
  if (isArray(originalArray)) {
    C = originalArray.constructor;
    // cross-realm fallback
    if (isConstructor(C) && (C === $Array || isArray(C.prototype))) C = undefined;
    else if (isObject(C)) {
      C = C[SPECIES];
      if (C === null) C = undefined;
    }
  } return C === undefined ? $Array : C;
};


/***/ }),
/* 155 */
/***/ (function(module, exports, __webpack_require__) {

var getBuiltIn = __webpack_require__(17);
var uncurryThis = __webpack_require__(0);
var getOwnPropertyNamesModule = __webpack_require__(68);
var getOwnPropertySymbolsModule = __webpack_require__(70);
var anObject = __webpack_require__(9);

var concat = uncurryThis([].concat);

// all object keys, includes non-enumerable and symbols
module.exports = getBuiltIn('Reflect', 'ownKeys') || function ownKeys(it) {
  var keys = getOwnPropertyNamesModule.f(anObject(it));
  var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
  return getOwnPropertySymbols ? concat(keys, getOwnPropertySymbols(it)) : keys;
};


/***/ }),
/* 156 */
/***/ (function(module, exports, __webpack_require__) {

var toIndexedObject = __webpack_require__(20);
var toAbsoluteIndex = __webpack_require__(69);
var lengthOfArrayLike = __webpack_require__(23);

// `Array.prototype.{ indexOf, includes }` methods implementation
var createMethod = function (IS_INCLUDES) {
  return function ($this, el, fromIndex) {
    var O = toIndexedObject($this);
    var length = lengthOfArrayLike(O);
    var index = toAbsoluteIndex(fromIndex, length);
    var value;
    // Array#includes uses SameValueZero equality algorithm
    // eslint-disable-next-line no-self-compare -- NaN check
    if (IS_INCLUDES && el != el) while (length > index) {
      value = O[index++];
      // eslint-disable-next-line no-self-compare -- NaN check
      if (value != value) return true;
    // Array#indexOf ignores holes, Array#includes - not
    } else for (;length > index; index++) {
      if ((IS_INCLUDES || index in O) && O[index] === el) return IS_INCLUDES || index || 0;
    } return !IS_INCLUDES && -1;
  };
};

module.exports = {
  // `Array.prototype.includes` method
  // https://tc39.es/ecma262/#sec-array.prototype.includes
  includes: createMethod(true),
  // `Array.prototype.indexOf` method
  // https://tc39.es/ecma262/#sec-array.prototype.indexof
  indexOf: createMethod(false)
};


/***/ }),
/* 157 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(10);
__webpack_require__(27);
__webpack_require__(19);
Object.defineProperty(exports, "__esModule", {
  value: true
});
var _exportNames = {
  GameCore: true,
  State: true,
  EStateEvents: true,
  EOrientation: true,
  Settings: true,
  Loader: true,
  DefaultLoader: true
};
Object.defineProperty(exports, "DefaultLoader", {
  enumerable: true,
  get: function get() {
    return _default.DefaultLoader;
  }
});
Object.defineProperty(exports, "EOrientation", {
  enumerable: true,
  get: function get() {
    return _orientation.EOrientation;
  }
});
Object.defineProperty(exports, "EStateEvents", {
  enumerable: true,
  get: function get() {
    return _state.EStateEvents;
  }
});
Object.defineProperty(exports, "GameCore", {
  enumerable: true,
  get: function get() {
    return _gameCore.GameCore;
  }
});
Object.defineProperty(exports, "Loader", {
  enumerable: true,
  get: function get() {
    return _loader.Loader;
  }
});
Object.defineProperty(exports, "Settings", {
  enumerable: true,
  get: function get() {
    return _settings.Settings;
  }
});
Object.defineProperty(exports, "State", {
  enumerable: true,
  get: function get() {
    return _state.State;
  }
});
var _gameCore = __webpack_require__(158);
var _state = __webpack_require__(222);
var _orientation = __webpack_require__(139);
var _settings = __webpack_require__(138);
var _loader = __webpack_require__(140);
var _default = __webpack_require__(142);
var _gameConfigurationTypes = __webpack_require__(233);
Object.keys(_gameConfigurationTypes).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _gameConfigurationTypes[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _gameConfigurationTypes[key];
    }
  });
});

/***/ }),
/* 158 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.GameCore = void 0;
__webpack_require__(10);
__webpack_require__(37);
__webpack_require__(38);
__webpack_require__(39);
__webpack_require__(40);
__webpack_require__(29);
__webpack_require__(41);
__webpack_require__(42);
__webpack_require__(43);
var _tools = __webpack_require__(57);
var _settings = __webpack_require__(138);
var _loader = __webpack_require__(140);
var __awaiter = void 0 && (void 0).__awaiter || function (thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function (resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function (resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator = void 0 && (void 0).__generator || function (thisArg, body) {
  var _ = {
      label: 0,
      sent: function sent() {
        if (t[0] & 1) throw t[1];
        return t[1];
      },
      trys: [],
      ops: []
    },
    f,
    y,
    t,
    g;
  return g = {
    next: verb(0),
    "throw": verb(1),
    "return": verb(2)
  }, typeof Symbol === "function" && (g[Symbol.iterator] = function () {
    return this;
  }), g;
  function verb(n) {
    return function (v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return {
            value: op[1],
            done: false
          };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return {
      value: op[0] ? op[1] : void 0,
      done: true
    };
  }
};
var GameCore = /** @class */function () {
  /**
   * Constructor
   * @param window specifies the window to work on
   * @param canvas specifies the canvas to work on
   * @param createGameDiv if a game div should be created to put canvas in it
   */
  function GameCore(window, canvas, createGameDiv) {
    if (createGameDiv === void 0) {
      createGameDiv = true;
    }
    // Public members
    this.state = null;
    this.settings = new _settings.Settings();
    this.loader = new _loader.Loader(this);
    this.window = window;
    this.canvas = canvas;
    if (createGameDiv) {
      this.createGameDiv();
    }
  }
  /**
   * Starts the game.
   * @param view Specifies the first view to make appear.
   * @returns A promise resolved when the game is loaded and started or void for backward compatibility.
   */
  GameCore.prototype.start = function (view) {
    var _this = this;
    // Bind events
    if (this.window) {
      this.window.addEventListener("blur", function () {
        return _this._blur();
      });
      this.window.addEventListener("focus", function () {
        return _this._focus();
      });
    }
    // Load here
    if (!this.state) {
      throw new Error("You must provide a state");
    }
    return this.state.load(view);
  };
  /**
   * Requests fullscreen on browser
   * @return true if fullscreen is possible, false if not available (i.e iPhones)
   */
  GameCore.prototype.requestFullscreen = function () {
    if (!this.gameConfig.gameParameters.fullscreenEnabled) {
      return;
    }
    var requestFullscreen = this.gameDiv && (this.gameDiv.requestFullscreen || this.gameDiv.msRequestFullscreen || this.gameDiv.webkitRequestFullscreen || this.gameDiv.mozRequestFullScreen);
    if (!requestFullscreen) {
      return false;
    }
    requestFullscreen.call(this.gameDiv);
    return true;
  };
  /**
   * Returns the resource identified by the given key
   * @param key the key of the value to get
   */
  GameCore.prototype.resource = function (key) {
    return _loader.Loader.get(key);
  };
  /**
   * Displays information about a game: its version, gdk modules versions
   */
  GameCore.prototype.infos = function () {
    return __awaiter(this, void 0, void 0, function () {
      var _a, _b;
      return __generator(this, function (_c) {
        switch (_c.label) {
          case 0:
            // For logging purpose, @TODO we should use the logger for this.
            _b = (_a = console).log;
            return [4 /*yield*/, this.getInfos()];
          case 1:
            // For logging purpose, @TODO we should use the logger for this.
            _b.apply(_a, [_c.sent()]);
            return [2 /*return*/];
        }
      });
    });
  };
  /**
   * Return the game's infos. Should be overidden to add
   * more informations to log
   */
  GameCore.prototype.getInfos = function () {
    return __awaiter(this, void 0, void 0, function () {
      var paths, infos;
      var _a, _b;
      return __generator(this, function (_c) {
        switch (_c.label) {
          case 0:
            paths = this.gameConfig.modules.paths;
            if (this.gameConfig.baseUrl) {
              SystemJS.config({
                baseURL: this.gameConfig.baseUrl
              });
            }
            _a = {};
            _b = {};
            return [4 /*yield*/, this._getGameVersion("@gdk/the-awesome-game")];
          case 1:
            _b.applet = _c.sent();
            return [4 /*yield*/, this._getModulesVersions(paths)];
          case 2:
            infos = (_a.gameInfo = (_b.modules = _c.sent(), _b), _a.envInfos = {}, _a);
            infos.gameInfo.hash = {
              applet: _tools.MD5.init((0, _tools.deterministicStringify)(infos.gameInfo.applet)),
              modules: _tools.MD5.init((0, _tools.deterministicStringify)(infos.gameInfo.modules))
            };
            infos.gameInfo.hash.global = _tools.MD5.init((0, _tools.deterministicStringify)(infos.gameInfo.hash));
            return [2 /*return*/, infos];
        }
      });
    });
  };
  /**
   * Create container div for the game
   * Called by gameCore constructor
   * Can be overrided
   */
  GameCore.prototype.createGameDiv = function () {
    this.gameDiv = document.createElement("div");
    this.gameDiv.setAttribute("id", "gdkGameDiv");
    this.gameDiv.setAttribute("ondragstart", "return false;");
    this.gameDiv.setAttribute("ondrop", "return false;");
    this.gameDiv.style.position = "fixed";
    this.gameDiv.style.top = "0";
    this.gameDiv.style.left = "0";
    document.body.appendChild(this.gameDiv);
  };
  // When the window/tab changed
  GameCore.prototype._blur = function () {
    if (this.onBlur) {
      this.onBlur();
    }
  };
  // When the window/tabs has been focused
  GameCore.prototype._focus = function () {
    if (this.onFocus) {
      this.onFocus();
    }
  };
  // Get version for a given module
  // @param name: name of the module
  GameCore.prototype._getVersion = function (name) {
    return __awaiter(this, void 0, void 0, function () {
      var normalizedUrl, gdkModule;
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            return [4 /*yield*/, SystemJS.resolve(name)];
          case 1:
            normalizedUrl = _a.sent();
            if (!SystemJS.has(normalizedUrl)) return [3 /*break*/, 3];
            return [4 /*yield*/, SystemJS.import(name)];
          case 2:
            gdkModule = _a.sent();
            return [2 /*return*/, gdkModule.VERSION];
          case 3:
            return [2 /*return*/];
        }
      });
    });
  };
  // Get version for gdk modules used in the applet except submodules
  // @param paths: object with names and paths to gdk modules
  GameCore.prototype._getModulesVersions = function (paths) {
    return __awaiter(this, void 0, void 0, function () {
      var modulesVersions, _a, _b, _c, _i, key, version;
      return __generator(this, function (_d) {
        switch (_d.label) {
          case 0:
            modulesVersions = {};
            _a = paths;
            _b = [];
            for (_c in _a) _b.push(_c);
            _i = 0;
            _d.label = 1;
          case 1:
            if (!(_i < _b.length)) return [3 /*break*/, 4];
            _c = _b[_i];
            if (!(_c in _a)) return [3 /*break*/, 3];
            key = _c;
            if (!(key.indexOf("@gdk") === 0 && /\/.*\/.*$/.test(key) === false)) return [3 /*break*/, 3];
            return [4 /*yield*/, this._getVersion(key)];
          case 2:
            version = _d.sent();
            modulesVersions[key] = version;
            _d.label = 3;
          case 3:
            _i++;
            return [3 /*break*/, 1];
          case 4:
            return [2 /*return*/, modulesVersions];
        }
      });
    });
  };
  // Get the applet's version.
  // If the game is run locally, we can't know its version. Thus, return 'dev'.
  // @param name: game name
  GameCore.prototype._getGameVersion = function (name) {
    return __awaiter(this, void 0, void 0, function () {
      var gameVersion;
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            return [4 /*yield*/, this._getVersion(name)];
          case 1:
            gameVersion = _a.sent();
            return [2 /*return*/, gameVersion || "dev"];
        }
      });
    });
  };
  return GameCore;
}();
exports.GameCore = GameCore;

/***/ }),
/* 159 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var IS_PURE = __webpack_require__(14);
var IS_NODE = __webpack_require__(72);
var global = __webpack_require__(4);
var call = __webpack_require__(6);
var defineBuiltIn = __webpack_require__(16);
var setPrototypeOf = __webpack_require__(73);
var setToStringTag = __webpack_require__(74);
var setSpecies = __webpack_require__(163);
var aCallable = __webpack_require__(18);
var isCallable = __webpack_require__(3);
var isObject = __webpack_require__(13);
var anInstance = __webpack_require__(164);
var speciesConstructor = __webpack_require__(165);
var task = __webpack_require__(109).set;
var microtask = __webpack_require__(168);
var hostReportErrors = __webpack_require__(171);
var perform = __webpack_require__(93);
var Queue = __webpack_require__(112);
var InternalStateModule = __webpack_require__(35);
var NativePromiseConstructor = __webpack_require__(52);
var PromiseConstructorDetection = __webpack_require__(53);
var newPromiseCapabilityModule = __webpack_require__(54);

var PROMISE = 'Promise';
var FORCED_PROMISE_CONSTRUCTOR = PromiseConstructorDetection.CONSTRUCTOR;
var NATIVE_PROMISE_REJECTION_EVENT = PromiseConstructorDetection.REJECTION_EVENT;
var NATIVE_PROMISE_SUBCLASSING = PromiseConstructorDetection.SUBCLASSING;
var getInternalPromiseState = InternalStateModule.getterFor(PROMISE);
var setInternalState = InternalStateModule.set;
var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;
var PromiseConstructor = NativePromiseConstructor;
var PromisePrototype = NativePromisePrototype;
var TypeError = global.TypeError;
var document = global.document;
var process = global.process;
var newPromiseCapability = newPromiseCapabilityModule.f;
var newGenericPromiseCapability = newPromiseCapability;

var DISPATCH_EVENT = !!(document && document.createEvent && global.dispatchEvent);
var UNHANDLED_REJECTION = 'unhandledrejection';
var REJECTION_HANDLED = 'rejectionhandled';
var PENDING = 0;
var FULFILLED = 1;
var REJECTED = 2;
var HANDLED = 1;
var UNHANDLED = 2;

var Internal, OwnPromiseCapability, PromiseWrapper, nativeThen;

// helpers
var isThenable = function (it) {
  var then;
  return isObject(it) && isCallable(then = it.then) ? then : false;
};

var callReaction = function (reaction, state) {
  var value = state.value;
  var ok = state.state == FULFILLED;
  var handler = ok ? reaction.ok : reaction.fail;
  var resolve = reaction.resolve;
  var reject = reaction.reject;
  var domain = reaction.domain;
  var result, then, exited;
  try {
    if (handler) {
      if (!ok) {
        if (state.rejection === UNHANDLED) onHandleUnhandled(state);
        state.rejection = HANDLED;
      }
      if (handler === true) result = value;
      else {
        if (domain) domain.enter();
        result = handler(value); // can throw
        if (domain) {
          domain.exit();
          exited = true;
        }
      }
      if (result === reaction.promise) {
        reject(TypeError('Promise-chain cycle'));
      } else if (then = isThenable(result)) {
        call(then, result, resolve, reject);
      } else resolve(result);
    } else reject(value);
  } catch (error) {
    if (domain && !exited) domain.exit();
    reject(error);
  }
};

var notify = function (state, isReject) {
  if (state.notified) return;
  state.notified = true;
  microtask(function () {
    var reactions = state.reactions;
    var reaction;
    while (reaction = reactions.get()) {
      callReaction(reaction, state);
    }
    state.notified = false;
    if (isReject && !state.rejection) onUnhandled(state);
  });
};

var dispatchEvent = function (name, promise, reason) {
  var event, handler;
  if (DISPATCH_EVENT) {
    event = document.createEvent('Event');
    event.promise = promise;
    event.reason = reason;
    event.initEvent(name, false, true);
    global.dispatchEvent(event);
  } else event = { promise: promise, reason: reason };
  if (!NATIVE_PROMISE_REJECTION_EVENT && (handler = global['on' + name])) handler(event);
  else if (name === UNHANDLED_REJECTION) hostReportErrors('Unhandled promise rejection', reason);
};

var onUnhandled = function (state) {
  call(task, global, function () {
    var promise = state.facade;
    var value = state.value;
    var IS_UNHANDLED = isUnhandled(state);
    var result;
    if (IS_UNHANDLED) {
      result = perform(function () {
        if (IS_NODE) {
          process.emit('unhandledRejection', value, promise);
        } else dispatchEvent(UNHANDLED_REJECTION, promise, value);
      });
      // Browsers should not trigger `rejectionHandled` event if it was handled here, NodeJS - should
      state.rejection = IS_NODE || isUnhandled(state) ? UNHANDLED : HANDLED;
      if (result.error) throw result.value;
    }
  });
};

var isUnhandled = function (state) {
  return state.rejection !== HANDLED && !state.parent;
};

var onHandleUnhandled = function (state) {
  call(task, global, function () {
    var promise = state.facade;
    if (IS_NODE) {
      process.emit('rejectionHandled', promise);
    } else dispatchEvent(REJECTION_HANDLED, promise, state.value);
  });
};

var bind = function (fn, state, unwrap) {
  return function (value) {
    fn(state, value, unwrap);
  };
};

var internalReject = function (state, value, unwrap) {
  if (state.done) return;
  state.done = true;
  if (unwrap) state = unwrap;
  state.value = value;
  state.state = REJECTED;
  notify(state, true);
};

var internalResolve = function (state, value, unwrap) {
  if (state.done) return;
  state.done = true;
  if (unwrap) state = unwrap;
  try {
    if (state.facade === value) throw TypeError("Promise can't be resolved itself");
    var then = isThenable(value);
    if (then) {
      microtask(function () {
        var wrapper = { done: false };
        try {
          call(then, value,
            bind(internalResolve, wrapper, state),
            bind(internalReject, wrapper, state)
          );
        } catch (error) {
          internalReject(wrapper, error, state);
        }
      });
    } else {
      state.value = value;
      state.state = FULFILLED;
      notify(state, false);
    }
  } catch (error) {
    internalReject({ done: false }, error, state);
  }
};

// constructor polyfill
if (FORCED_PROMISE_CONSTRUCTOR) {
  // 25.4.3.1 Promise(executor)
  PromiseConstructor = function Promise(executor) {
    anInstance(this, PromisePrototype);
    aCallable(executor);
    call(Internal, this);
    var state = getInternalPromiseState(this);
    try {
      executor(bind(internalResolve, state), bind(internalReject, state));
    } catch (error) {
      internalReject(state, error);
    }
  };

  PromisePrototype = PromiseConstructor.prototype;

  // eslint-disable-next-line no-unused-vars -- required for `.length`
  Internal = function Promise(executor) {
    setInternalState(this, {
      type: PROMISE,
      done: false,
      notified: false,
      parent: false,
      reactions: new Queue(),
      rejection: false,
      state: PENDING,
      value: undefined
    });
  };

  // `Promise.prototype.then` method
  // https://tc39.es/ecma262/#sec-promise.prototype.then
  Internal.prototype = defineBuiltIn(PromisePrototype, 'then', function then(onFulfilled, onRejected) {
    var state = getInternalPromiseState(this);
    var reaction = newPromiseCapability(speciesConstructor(this, PromiseConstructor));
    state.parent = true;
    reaction.ok = isCallable(onFulfilled) ? onFulfilled : true;
    reaction.fail = isCallable(onRejected) && onRejected;
    reaction.domain = IS_NODE ? process.domain : undefined;
    if (state.state == PENDING) state.reactions.add(reaction);
    else microtask(function () {
      callReaction(reaction, state);
    });
    return reaction.promise;
  });

  OwnPromiseCapability = function () {
    var promise = new Internal();
    var state = getInternalPromiseState(promise);
    this.promise = promise;
    this.resolve = bind(internalResolve, state);
    this.reject = bind(internalReject, state);
  };

  newPromiseCapabilityModule.f = newPromiseCapability = function (C) {
    return C === PromiseConstructor || C === PromiseWrapper
      ? new OwnPromiseCapability(C)
      : newGenericPromiseCapability(C);
  };

  if (!IS_PURE && isCallable(NativePromiseConstructor) && NativePromisePrototype !== Object.prototype) {
    nativeThen = NativePromisePrototype.then;

    if (!NATIVE_PROMISE_SUBCLASSING) {
      // make `Promise#then` return a polyfilled `Promise` for native promise-based APIs
      defineBuiltIn(NativePromisePrototype, 'then', function then(onFulfilled, onRejected) {
        var that = this;
        return new PromiseConstructor(function (resolve, reject) {
          call(nativeThen, that, resolve, reject);
        }).then(onFulfilled, onRejected);
      // https://github.com/zloirock/core-js/issues/640
      }, { unsafe: true });
    }

    // make `.constructor === Promise` work for native promise-based APIs
    try {
      delete NativePromisePrototype.constructor;
    } catch (error) { /* empty */ }

    // make `instanceof Promise` work for native promise-based APIs
    if (setPrototypeOf) {
      setPrototypeOf(NativePromisePrototype, PromisePrototype);
    }
  }
}

$({ global: true, constructor: true, wrap: true, forced: FORCED_PROMISE_CONSTRUCTOR }, {
  Promise: PromiseConstructor
});

setToStringTag(PromiseConstructor, PROMISE, false, true);
setSpecies(PROMISE);


/***/ }),
/* 160 */
/***/ (function(module, exports) {

// shim for using process in browser
var process = module.exports = {};

// cached from whatever global is present so that test runners that stub it
// don't break things.  But we need to wrap it in a try catch in case it is
// wrapped in strict mode code which doesn't define any globals.  It's inside a
// function because try/catches deoptimize in certain engines.

var cachedSetTimeout;
var cachedClearTimeout;

function defaultSetTimout() {
    throw new Error('setTimeout has not been defined');
}
function defaultClearTimeout () {
    throw new Error('clearTimeout has not been defined');
}
(function () {
    try {
        if (typeof setTimeout === 'function') {
            cachedSetTimeout = setTimeout;
        } else {
            cachedSetTimeout = defaultSetTimout;
        }
    } catch (e) {
        cachedSetTimeout = defaultSetTimout;
    }
    try {
        if (typeof clearTimeout === 'function') {
            cachedClearTimeout = clearTimeout;
        } else {
            cachedClearTimeout = defaultClearTimeout;
        }
    } catch (e) {
        cachedClearTimeout = defaultClearTimeout;
    }
} ())
function runTimeout(fun) {
    if (cachedSetTimeout === setTimeout) {
        //normal enviroments in sane situations
        return setTimeout(fun, 0);
    }
    // if setTimeout wasn't available but was latter defined
    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {
        cachedSetTimeout = setTimeout;
        return setTimeout(fun, 0);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedSetTimeout(fun, 0);
    } catch(e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally
            return cachedSetTimeout.call(null, fun, 0);
        } catch(e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error
            return cachedSetTimeout.call(this, fun, 0);
        }
    }


}
function runClearTimeout(marker) {
    if (cachedClearTimeout === clearTimeout) {
        //normal enviroments in sane situations
        return clearTimeout(marker);
    }
    // if clearTimeout wasn't available but was latter defined
    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {
        cachedClearTimeout = clearTimeout;
        return clearTimeout(marker);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedClearTimeout(marker);
    } catch (e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally
            return cachedClearTimeout.call(null, marker);
        } catch (e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.
            // Some versions of I.E. have different rules for clearTimeout vs setTimeout
            return cachedClearTimeout.call(this, marker);
        }
    }



}
var queue = [];
var draining = false;
var currentQueue;
var queueIndex = -1;

function cleanUpNextTick() {
    if (!draining || !currentQueue) {
        return;
    }
    draining = false;
    if (currentQueue.length) {
        queue = currentQueue.concat(queue);
    } else {
        queueIndex = -1;
    }
    if (queue.length) {
        drainQueue();
    }
}

function drainQueue() {
    if (draining) {
        return;
    }
    var timeout = runTimeout(cleanUpNextTick);
    draining = true;

    var len = queue.length;
    while(len) {
        currentQueue = queue;
        queue = [];
        while (++queueIndex < len) {
            if (currentQueue) {
                currentQueue[queueIndex].run();
            }
        }
        queueIndex = -1;
        len = queue.length;
    }
    currentQueue = null;
    draining = false;
    runClearTimeout(timeout);
}

process.nextTick = function (fun) {
    var args = new Array(arguments.length - 1);
    if (arguments.length > 1) {
        for (var i = 1; i < arguments.length; i++) {
            args[i - 1] = arguments[i];
        }
    }
    queue.push(new Item(fun, args));
    if (queue.length === 1 && !draining) {
        runTimeout(drainQueue);
    }
};

// v8 likes predictible objects
function Item(fun, array) {
    this.fun = fun;
    this.array = array;
}
Item.prototype.run = function () {
    this.fun.apply(null, this.array);
};
process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];
process.version = ''; // empty string to avoid regexp issues
process.versions = {};

function noop() {}

process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;
process.prependListener = noop;
process.prependOnceListener = noop;

process.listeners = function (name) { return [] }

process.binding = function (name) {
    throw new Error('process.binding is not supported');
};

process.cwd = function () { return '/' };
process.chdir = function (dir) {
    throw new Error('process.chdir is not supported');
};
process.umask = function() { return 0; };


/***/ }),
/* 161 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var aCallable = __webpack_require__(18);

module.exports = function (object, key, method) {
  try {
    // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
    return uncurryThis(aCallable(Object.getOwnPropertyDescriptor(object, key)[method]));
  } catch (error) { /* empty */ }
};


/***/ }),
/* 162 */
/***/ (function(module, exports, __webpack_require__) {

var isCallable = __webpack_require__(3);

var $String = String;
var $TypeError = TypeError;

module.exports = function (argument) {
  if (typeof argument == 'object' || isCallable(argument)) return argument;
  throw $TypeError("Can't set " + $String(argument) + ' as a prototype');
};


/***/ }),
/* 163 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var getBuiltIn = __webpack_require__(17);
var defineBuiltInAccessor = __webpack_require__(75);
var wellKnownSymbol = __webpack_require__(5);
var DESCRIPTORS = __webpack_require__(8);

var SPECIES = wellKnownSymbol('species');

module.exports = function (CONSTRUCTOR_NAME) {
  var Constructor = getBuiltIn(CONSTRUCTOR_NAME);

  if (DESCRIPTORS && Constructor && !Constructor[SPECIES]) {
    defineBuiltInAccessor(Constructor, SPECIES, {
      configurable: true,
      get: function () { return this; }
    });
  }
};


/***/ }),
/* 164 */
/***/ (function(module, exports, __webpack_require__) {

var isPrototypeOf = __webpack_require__(26);

var $TypeError = TypeError;

module.exports = function (it, Prototype) {
  if (isPrototypeOf(Prototype, it)) return it;
  throw $TypeError('Incorrect invocation');
};


/***/ }),
/* 165 */
/***/ (function(module, exports, __webpack_require__) {

var anObject = __webpack_require__(9);
var aConstructor = __webpack_require__(166);
var isNullOrUndefined = __webpack_require__(32);
var wellKnownSymbol = __webpack_require__(5);

var SPECIES = wellKnownSymbol('species');

// `SpeciesConstructor` abstract operation
// https://tc39.es/ecma262/#sec-speciesconstructor
module.exports = function (O, defaultConstructor) {
  var C = anObject(O).constructor;
  var S;
  return C === undefined || isNullOrUndefined(S = anObject(C)[SPECIES]) ? defaultConstructor : aConstructor(S);
};


/***/ }),
/* 166 */
/***/ (function(module, exports, __webpack_require__) {

var isConstructor = __webpack_require__(86);
var tryToString = __webpack_require__(34);

var $TypeError = TypeError;

// `Assert: IsConstructor(argument) is true`
module.exports = function (argument) {
  if (isConstructor(argument)) return argument;
  throw $TypeError(tryToString(argument) + ' is not a constructor');
};


/***/ }),
/* 167 */
/***/ (function(module, exports) {

var $TypeError = TypeError;

module.exports = function (passed, required) {
  if (passed < required) throw $TypeError('Not enough arguments');
  return passed;
};


/***/ }),
/* 168 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(4);
var bind = __webpack_require__(66);
var getOwnPropertyDescriptor = __webpack_require__(51).f;
var macrotask = __webpack_require__(109).set;
var Queue = __webpack_require__(112);
var IS_IOS = __webpack_require__(111);
var IS_IOS_PEBBLE = __webpack_require__(169);
var IS_WEBOS_WEBKIT = __webpack_require__(170);
var IS_NODE = __webpack_require__(72);

var MutationObserver = global.MutationObserver || global.WebKitMutationObserver;
var document = global.document;
var process = global.process;
var Promise = global.Promise;
// Node.js 11 shows ExperimentalWarning on getting `queueMicrotask`
var queueMicrotaskDescriptor = getOwnPropertyDescriptor(global, 'queueMicrotask');
var microtask = queueMicrotaskDescriptor && queueMicrotaskDescriptor.value;
var notify, toggle, node, promise, then;

// modern engines have queueMicrotask method
if (!microtask) {
  var queue = new Queue();

  var flush = function () {
    var parent, fn;
    if (IS_NODE && (parent = process.domain)) parent.exit();
    while (fn = queue.get()) try {
      fn();
    } catch (error) {
      if (queue.head) notify();
      throw error;
    }
    if (parent) parent.enter();
  };

  // browsers with MutationObserver, except iOS - https://github.com/zloirock/core-js/issues/339
  // also except WebOS Webkit https://github.com/zloirock/core-js/issues/898
  if (!IS_IOS && !IS_NODE && !IS_WEBOS_WEBKIT && MutationObserver && document) {
    toggle = true;
    node = document.createTextNode('');
    new MutationObserver(flush).observe(node, { characterData: true });
    notify = function () {
      node.data = toggle = !toggle;
    };
  // environments with maybe non-completely correct, but existent Promise
  } else if (!IS_IOS_PEBBLE && Promise && Promise.resolve) {
    // Promise.resolve without an argument throws an error in LG WebOS 2
    promise = Promise.resolve(undefined);
    // workaround of WebKit ~ iOS Safari 10.1 bug
    promise.constructor = Promise;
    then = bind(promise.then, promise);
    notify = function () {
      then(flush);
    };
  // Node.js without promises
  } else if (IS_NODE) {
    notify = function () {
      process.nextTick(flush);
    };
  // for other environments - macrotask based on:
  // - setImmediate
  // - MessageChannel
  // - window.postMessage
  // - onreadystatechange
  // - setTimeout
  } else {
    // `webpack` dev server bug on IE global methods - use bind(fn, global)
    macrotask = bind(macrotask, global);
    notify = function () {
      macrotask(flush);
    };
  }

  microtask = function (fn) {
    if (!queue.head) notify();
    queue.add(fn);
  };
}

module.exports = microtask;


/***/ }),
/* 169 */
/***/ (function(module, exports, __webpack_require__) {

var userAgent = __webpack_require__(25);

module.exports = /ipad|iphone|ipod/i.test(userAgent) && typeof Pebble != 'undefined';


/***/ }),
/* 170 */
/***/ (function(module, exports, __webpack_require__) {

var userAgent = __webpack_require__(25);

module.exports = /web0s(?!.*chrome)/i.test(userAgent);


/***/ }),
/* 171 */
/***/ (function(module, exports) {

module.exports = function (a, b) {
  try {
    // eslint-disable-next-line no-console -- safe
    arguments.length == 1 ? console.error(a) : console.error(a, b);
  } catch (error) { /* empty */ }
};


/***/ }),
/* 172 */
/***/ (function(module, exports, __webpack_require__) {

var IS_DENO = __webpack_require__(113);
var IS_NODE = __webpack_require__(72);

module.exports = !IS_DENO && !IS_NODE
  && typeof window == 'object'
  && typeof document == 'object';


/***/ }),
/* 173 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var call = __webpack_require__(6);
var aCallable = __webpack_require__(18);
var newPromiseCapabilityModule = __webpack_require__(54);
var perform = __webpack_require__(93);
var iterate = __webpack_require__(114);
var PROMISE_STATICS_INCORRECT_ITERATION = __webpack_require__(116);

// `Promise.all` method
// https://tc39.es/ecma262/#sec-promise.all
$({ target: 'Promise', stat: true, forced: PROMISE_STATICS_INCORRECT_ITERATION }, {
  all: function all(iterable) {
    var C = this;
    var capability = newPromiseCapabilityModule.f(C);
    var resolve = capability.resolve;
    var reject = capability.reject;
    var result = perform(function () {
      var $promiseResolve = aCallable(C.resolve);
      var values = [];
      var counter = 0;
      var remaining = 1;
      iterate(iterable, function (promise) {
        var index = counter++;
        var alreadyCalled = false;
        remaining++;
        call($promiseResolve, C, promise).then(function (value) {
          if (alreadyCalled) return;
          alreadyCalled = true;
          values[index] = value;
          --remaining || resolve(values);
        }, reject);
      });
      --remaining || resolve(values);
    });
    if (result.error) reject(result.value);
    return capability.promise;
  }
});


/***/ }),
/* 174 */
/***/ (function(module, exports, __webpack_require__) {

var wellKnownSymbol = __webpack_require__(5);
var Iterators = __webpack_require__(55);

var ITERATOR = wellKnownSymbol('iterator');
var ArrayPrototype = Array.prototype;

// check on default Array iterator
module.exports = function (it) {
  return it !== undefined && (Iterators.Array === it || ArrayPrototype[ITERATOR] === it);
};


/***/ }),
/* 175 */
/***/ (function(module, exports, __webpack_require__) {

var call = __webpack_require__(6);
var aCallable = __webpack_require__(18);
var anObject = __webpack_require__(9);
var tryToString = __webpack_require__(34);
var getIteratorMethod = __webpack_require__(115);

var $TypeError = TypeError;

module.exports = function (argument, usingIterator) {
  var iteratorMethod = arguments.length < 2 ? getIteratorMethod(argument) : usingIterator;
  if (aCallable(iteratorMethod)) return anObject(call(iteratorMethod, argument));
  throw $TypeError(tryToString(argument) + ' is not iterable');
};


/***/ }),
/* 176 */
/***/ (function(module, exports, __webpack_require__) {

var call = __webpack_require__(6);
var anObject = __webpack_require__(9);
var getMethod = __webpack_require__(47);

module.exports = function (iterator, kind, value) {
  var innerResult, innerError;
  anObject(iterator);
  try {
    innerResult = getMethod(iterator, 'return');
    if (!innerResult) {
      if (kind === 'throw') throw value;
      return value;
    }
    innerResult = call(innerResult, iterator);
  } catch (error) {
    innerError = true;
    innerResult = error;
  }
  if (kind === 'throw') throw value;
  if (innerError) throw innerResult;
  anObject(innerResult);
  return value;
};


/***/ }),
/* 177 */
/***/ (function(module, exports, __webpack_require__) {

var wellKnownSymbol = __webpack_require__(5);

var ITERATOR = wellKnownSymbol('iterator');
var SAFE_CLOSING = false;

try {
  var called = 0;
  var iteratorWithReturn = {
    next: function () {
      return { done: !!called++ };
    },
    'return': function () {
      SAFE_CLOSING = true;
    }
  };
  iteratorWithReturn[ITERATOR] = function () {
    return this;
  };
  // eslint-disable-next-line es/no-array-from, no-throw-literal -- required for testing
  Array.from(iteratorWithReturn, function () { throw 2; });
} catch (error) { /* empty */ }

module.exports = function (exec, SKIP_CLOSING) {
  if (!SKIP_CLOSING && !SAFE_CLOSING) return false;
  var ITERATION_SUPPORT = false;
  try {
    var object = {};
    object[ITERATOR] = function () {
      return {
        next: function () {
          return { done: ITERATION_SUPPORT = true };
        }
      };
    };
    exec(object);
  } catch (error) { /* empty */ }
  return ITERATION_SUPPORT;
};


/***/ }),
/* 178 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var IS_PURE = __webpack_require__(14);
var FORCED_PROMISE_CONSTRUCTOR = __webpack_require__(53).CONSTRUCTOR;
var NativePromiseConstructor = __webpack_require__(52);
var getBuiltIn = __webpack_require__(17);
var isCallable = __webpack_require__(3);
var defineBuiltIn = __webpack_require__(16);

var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;

// `Promise.prototype.catch` method
// https://tc39.es/ecma262/#sec-promise.prototype.catch
$({ target: 'Promise', proto: true, forced: FORCED_PROMISE_CONSTRUCTOR, real: true }, {
  'catch': function (onRejected) {
    return this.then(undefined, onRejected);
  }
});

// makes sure that native promise-based APIs `Promise#catch` properly works with patched `Promise#then`
if (!IS_PURE && isCallable(NativePromiseConstructor)) {
  var method = getBuiltIn('Promise').prototype['catch'];
  if (NativePromisePrototype['catch'] !== method) {
    defineBuiltIn(NativePromisePrototype, 'catch', method, { unsafe: true });
  }
}


/***/ }),
/* 179 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var call = __webpack_require__(6);
var aCallable = __webpack_require__(18);
var newPromiseCapabilityModule = __webpack_require__(54);
var perform = __webpack_require__(93);
var iterate = __webpack_require__(114);
var PROMISE_STATICS_INCORRECT_ITERATION = __webpack_require__(116);

// `Promise.race` method
// https://tc39.es/ecma262/#sec-promise.race
$({ target: 'Promise', stat: true, forced: PROMISE_STATICS_INCORRECT_ITERATION }, {
  race: function race(iterable) {
    var C = this;
    var capability = newPromiseCapabilityModule.f(C);
    var reject = capability.reject;
    var result = perform(function () {
      var $promiseResolve = aCallable(C.resolve);
      iterate(iterable, function (promise) {
        call($promiseResolve, C, promise).then(capability.resolve, reject);
      });
    });
    if (result.error) reject(result.value);
    return capability.promise;
  }
});


/***/ }),
/* 180 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var call = __webpack_require__(6);
var newPromiseCapabilityModule = __webpack_require__(54);
var FORCED_PROMISE_CONSTRUCTOR = __webpack_require__(53).CONSTRUCTOR;

// `Promise.reject` method
// https://tc39.es/ecma262/#sec-promise.reject
$({ target: 'Promise', stat: true, forced: FORCED_PROMISE_CONSTRUCTOR }, {
  reject: function reject(r) {
    var capability = newPromiseCapabilityModule.f(this);
    call(capability.reject, undefined, r);
    return capability.promise;
  }
});


/***/ }),
/* 181 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var getBuiltIn = __webpack_require__(17);
var IS_PURE = __webpack_require__(14);
var NativePromiseConstructor = __webpack_require__(52);
var FORCED_PROMISE_CONSTRUCTOR = __webpack_require__(53).CONSTRUCTOR;
var promiseResolve = __webpack_require__(182);

var PromiseConstructorWrapper = getBuiltIn('Promise');
var CHECK_WRAPPER = IS_PURE && !FORCED_PROMISE_CONSTRUCTOR;

// `Promise.resolve` method
// https://tc39.es/ecma262/#sec-promise.resolve
$({ target: 'Promise', stat: true, forced: IS_PURE || FORCED_PROMISE_CONSTRUCTOR }, {
  resolve: function resolve(x) {
    return promiseResolve(CHECK_WRAPPER && this === PromiseConstructorWrapper ? NativePromiseConstructor : this, x);
  }
});


/***/ }),
/* 182 */
/***/ (function(module, exports, __webpack_require__) {

var anObject = __webpack_require__(9);
var isObject = __webpack_require__(13);
var newPromiseCapability = __webpack_require__(54);

module.exports = function (C, x) {
  anObject(C);
  if (isObject(x) && x.constructor === C) return x;
  var promiseCapability = newPromiseCapability.f(C);
  var resolve = promiseCapability.resolve;
  resolve(x);
  return promiseCapability.promise;
};


/***/ }),
/* 183 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var global = __webpack_require__(4);
var call = __webpack_require__(6);
var uncurryThis = __webpack_require__(0);
var IS_PURE = __webpack_require__(14);
var DESCRIPTORS = __webpack_require__(8);
var NATIVE_SYMBOL = __webpack_require__(33);
var fails = __webpack_require__(1);
var hasOwn = __webpack_require__(7);
var isPrototypeOf = __webpack_require__(26);
var anObject = __webpack_require__(9);
var toIndexedObject = __webpack_require__(20);
var toPropertyKey = __webpack_require__(61);
var $toString = __webpack_require__(11);
var createPropertyDescriptor = __webpack_require__(48);
var nativeObjectCreate = __webpack_require__(56);
var objectKeys = __webpack_require__(71);
var getOwnPropertyNamesModule = __webpack_require__(68);
var getOwnPropertyNamesExternal = __webpack_require__(184);
var getOwnPropertySymbolsModule = __webpack_require__(70);
var getOwnPropertyDescriptorModule = __webpack_require__(51);
var definePropertyModule = __webpack_require__(12);
var definePropertiesModule = __webpack_require__(117);
var propertyIsEnumerableModule = __webpack_require__(88);
var defineBuiltIn = __webpack_require__(16);
var defineBuiltInAccessor = __webpack_require__(75);
var shared = __webpack_require__(31);
var sharedKey = __webpack_require__(63);
var hiddenKeys = __webpack_require__(64);
var uid = __webpack_require__(82);
var wellKnownSymbol = __webpack_require__(5);
var wrappedWellKnownSymbolModule = __webpack_require__(119);
var defineWellKnownSymbol = __webpack_require__(120);
var defineSymbolToPrimitive = __webpack_require__(185);
var setToStringTag = __webpack_require__(74);
var InternalStateModule = __webpack_require__(35);
var $forEach = __webpack_require__(49).forEach;

var HIDDEN = sharedKey('hidden');
var SYMBOL = 'Symbol';
var PROTOTYPE = 'prototype';

var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(SYMBOL);

var ObjectPrototype = Object[PROTOTYPE];
var $Symbol = global.Symbol;
var SymbolPrototype = $Symbol && $Symbol[PROTOTYPE];
var TypeError = global.TypeError;
var QObject = global.QObject;
var nativeGetOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
var nativeDefineProperty = definePropertyModule.f;
var nativeGetOwnPropertyNames = getOwnPropertyNamesExternal.f;
var nativePropertyIsEnumerable = propertyIsEnumerableModule.f;
var push = uncurryThis([].push);

var AllSymbols = shared('symbols');
var ObjectPrototypeSymbols = shared('op-symbols');
var WellKnownSymbolsStore = shared('wks');

// Don't use setters in Qt Script, https://github.com/zloirock/core-js/issues/173
var USE_SETTER = !QObject || !QObject[PROTOTYPE] || !QObject[PROTOTYPE].findChild;

// fallback for old Android, https://code.google.com/p/v8/issues/detail?id=687
var setSymbolDescriptor = DESCRIPTORS && fails(function () {
  return nativeObjectCreate(nativeDefineProperty({}, 'a', {
    get: function () { return nativeDefineProperty(this, 'a', { value: 7 }).a; }
  })).a != 7;
}) ? function (O, P, Attributes) {
  var ObjectPrototypeDescriptor = nativeGetOwnPropertyDescriptor(ObjectPrototype, P);
  if (ObjectPrototypeDescriptor) delete ObjectPrototype[P];
  nativeDefineProperty(O, P, Attributes);
  if (ObjectPrototypeDescriptor && O !== ObjectPrototype) {
    nativeDefineProperty(ObjectPrototype, P, ObjectPrototypeDescriptor);
  }
} : nativeDefineProperty;

var wrap = function (tag, description) {
  var symbol = AllSymbols[tag] = nativeObjectCreate(SymbolPrototype);
  setInternalState(symbol, {
    type: SYMBOL,
    tag: tag,
    description: description
  });
  if (!DESCRIPTORS) symbol.description = description;
  return symbol;
};

var $defineProperty = function defineProperty(O, P, Attributes) {
  if (O === ObjectPrototype) $defineProperty(ObjectPrototypeSymbols, P, Attributes);
  anObject(O);
  var key = toPropertyKey(P);
  anObject(Attributes);
  if (hasOwn(AllSymbols, key)) {
    if (!Attributes.enumerable) {
      if (!hasOwn(O, HIDDEN)) nativeDefineProperty(O, HIDDEN, createPropertyDescriptor(1, {}));
      O[HIDDEN][key] = true;
    } else {
      if (hasOwn(O, HIDDEN) && O[HIDDEN][key]) O[HIDDEN][key] = false;
      Attributes = nativeObjectCreate(Attributes, { enumerable: createPropertyDescriptor(0, false) });
    } return setSymbolDescriptor(O, key, Attributes);
  } return nativeDefineProperty(O, key, Attributes);
};

var $defineProperties = function defineProperties(O, Properties) {
  anObject(O);
  var properties = toIndexedObject(Properties);
  var keys = objectKeys(properties).concat($getOwnPropertySymbols(properties));
  $forEach(keys, function (key) {
    if (!DESCRIPTORS || call($propertyIsEnumerable, properties, key)) $defineProperty(O, key, properties[key]);
  });
  return O;
};

var $create = function create(O, Properties) {
  return Properties === undefined ? nativeObjectCreate(O) : $defineProperties(nativeObjectCreate(O), Properties);
};

var $propertyIsEnumerable = function propertyIsEnumerable(V) {
  var P = toPropertyKey(V);
  var enumerable = call(nativePropertyIsEnumerable, this, P);
  if (this === ObjectPrototype && hasOwn(AllSymbols, P) && !hasOwn(ObjectPrototypeSymbols, P)) return false;
  return enumerable || !hasOwn(this, P) || !hasOwn(AllSymbols, P) || hasOwn(this, HIDDEN) && this[HIDDEN][P]
    ? enumerable : true;
};

var $getOwnPropertyDescriptor = function getOwnPropertyDescriptor(O, P) {
  var it = toIndexedObject(O);
  var key = toPropertyKey(P);
  if (it === ObjectPrototype && hasOwn(AllSymbols, key) && !hasOwn(ObjectPrototypeSymbols, key)) return;
  var descriptor = nativeGetOwnPropertyDescriptor(it, key);
  if (descriptor && hasOwn(AllSymbols, key) && !(hasOwn(it, HIDDEN) && it[HIDDEN][key])) {
    descriptor.enumerable = true;
  }
  return descriptor;
};

var $getOwnPropertyNames = function getOwnPropertyNames(O) {
  var names = nativeGetOwnPropertyNames(toIndexedObject(O));
  var result = [];
  $forEach(names, function (key) {
    if (!hasOwn(AllSymbols, key) && !hasOwn(hiddenKeys, key)) push(result, key);
  });
  return result;
};

var $getOwnPropertySymbols = function (O) {
  var IS_OBJECT_PROTOTYPE = O === ObjectPrototype;
  var names = nativeGetOwnPropertyNames(IS_OBJECT_PROTOTYPE ? ObjectPrototypeSymbols : toIndexedObject(O));
  var result = [];
  $forEach(names, function (key) {
    if (hasOwn(AllSymbols, key) && (!IS_OBJECT_PROTOTYPE || hasOwn(ObjectPrototype, key))) {
      push(result, AllSymbols[key]);
    }
  });
  return result;
};

// `Symbol` constructor
// https://tc39.es/ecma262/#sec-symbol-constructor
if (!NATIVE_SYMBOL) {
  $Symbol = function Symbol() {
    if (isPrototypeOf(SymbolPrototype, this)) throw TypeError('Symbol is not a constructor');
    var description = !arguments.length || arguments[0] === undefined ? undefined : $toString(arguments[0]);
    var tag = uid(description);
    var setter = function (value) {
      if (this === ObjectPrototype) call(setter, ObjectPrototypeSymbols, value);
      if (hasOwn(this, HIDDEN) && hasOwn(this[HIDDEN], tag)) this[HIDDEN][tag] = false;
      setSymbolDescriptor(this, tag, createPropertyDescriptor(1, value));
    };
    if (DESCRIPTORS && USE_SETTER) setSymbolDescriptor(ObjectPrototype, tag, { configurable: true, set: setter });
    return wrap(tag, description);
  };

  SymbolPrototype = $Symbol[PROTOTYPE];

  defineBuiltIn(SymbolPrototype, 'toString', function toString() {
    return getInternalState(this).tag;
  });

  defineBuiltIn($Symbol, 'withoutSetter', function (description) {
    return wrap(uid(description), description);
  });

  propertyIsEnumerableModule.f = $propertyIsEnumerable;
  definePropertyModule.f = $defineProperty;
  definePropertiesModule.f = $defineProperties;
  getOwnPropertyDescriptorModule.f = $getOwnPropertyDescriptor;
  getOwnPropertyNamesModule.f = getOwnPropertyNamesExternal.f = $getOwnPropertyNames;
  getOwnPropertySymbolsModule.f = $getOwnPropertySymbols;

  wrappedWellKnownSymbolModule.f = function (name) {
    return wrap(wellKnownSymbol(name), name);
  };

  if (DESCRIPTORS) {
    // https://github.com/tc39/proposal-Symbol-description
    defineBuiltInAccessor(SymbolPrototype, 'description', {
      configurable: true,
      get: function description() {
        return getInternalState(this).description;
      }
    });
    if (!IS_PURE) {
      defineBuiltIn(ObjectPrototype, 'propertyIsEnumerable', $propertyIsEnumerable, { unsafe: true });
    }
  }
}

$({ global: true, constructor: true, wrap: true, forced: !NATIVE_SYMBOL, sham: !NATIVE_SYMBOL }, {
  Symbol: $Symbol
});

$forEach(objectKeys(WellKnownSymbolsStore), function (name) {
  defineWellKnownSymbol(name);
});

$({ target: SYMBOL, stat: true, forced: !NATIVE_SYMBOL }, {
  useSetter: function () { USE_SETTER = true; },
  useSimple: function () { USE_SETTER = false; }
});

$({ target: 'Object', stat: true, forced: !NATIVE_SYMBOL, sham: !DESCRIPTORS }, {
  // `Object.create` method
  // https://tc39.es/ecma262/#sec-object.create
  create: $create,
  // `Object.defineProperty` method
  // https://tc39.es/ecma262/#sec-object.defineproperty
  defineProperty: $defineProperty,
  // `Object.defineProperties` method
  // https://tc39.es/ecma262/#sec-object.defineproperties
  defineProperties: $defineProperties,
  // `Object.getOwnPropertyDescriptor` method
  // https://tc39.es/ecma262/#sec-object.getownpropertydescriptors
  getOwnPropertyDescriptor: $getOwnPropertyDescriptor
});

$({ target: 'Object', stat: true, forced: !NATIVE_SYMBOL }, {
  // `Object.getOwnPropertyNames` method
  // https://tc39.es/ecma262/#sec-object.getownpropertynames
  getOwnPropertyNames: $getOwnPropertyNames
});

// `Symbol.prototype[@@toPrimitive]` method
// https://tc39.es/ecma262/#sec-symbol.prototype-@@toprimitive
defineSymbolToPrimitive();

// `Symbol.prototype[@@toStringTag]` property
// https://tc39.es/ecma262/#sec-symbol.prototype-@@tostringtag
setToStringTag($Symbol, SYMBOL);

hiddenKeys[HIDDEN] = true;


/***/ }),
/* 184 */
/***/ (function(module, exports, __webpack_require__) {

/* eslint-disable es/no-object-getownpropertynames -- safe */
var classof = __webpack_require__(22);
var toIndexedObject = __webpack_require__(20);
var $getOwnPropertyNames = __webpack_require__(68).f;
var arraySlice = __webpack_require__(118);

var windowNames = typeof window == 'object' && window && Object.getOwnPropertyNames
  ? Object.getOwnPropertyNames(window) : [];

var getWindowNames = function (it) {
  try {
    return $getOwnPropertyNames(it);
  } catch (error) {
    return arraySlice(windowNames);
  }
};

// fallback for IE11 buggy Object.getOwnPropertyNames with iframe and window
module.exports.f = function getOwnPropertyNames(it) {
  return windowNames && classof(it) == 'Window'
    ? getWindowNames(it)
    : $getOwnPropertyNames(toIndexedObject(it));
};


/***/ }),
/* 185 */
/***/ (function(module, exports, __webpack_require__) {

var call = __webpack_require__(6);
var getBuiltIn = __webpack_require__(17);
var wellKnownSymbol = __webpack_require__(5);
var defineBuiltIn = __webpack_require__(16);

module.exports = function () {
  var Symbol = getBuiltIn('Symbol');
  var SymbolPrototype = Symbol && Symbol.prototype;
  var valueOf = SymbolPrototype && SymbolPrototype.valueOf;
  var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');

  if (SymbolPrototype && !SymbolPrototype[TO_PRIMITIVE]) {
    // `Symbol.prototype[@@toPrimitive]` method
    // https://tc39.es/ecma262/#sec-symbol.prototype-@@toprimitive
    // eslint-disable-next-line no-unused-vars -- required for .length
    defineBuiltIn(SymbolPrototype, TO_PRIMITIVE, function (hint) {
      return call(valueOf, this);
    }, { arity: 1 });
  }
};


/***/ }),
/* 186 */
/***/ (function(module, exports, __webpack_require__) {

var $ = __webpack_require__(2);
var getBuiltIn = __webpack_require__(17);
var hasOwn = __webpack_require__(7);
var toString = __webpack_require__(11);
var shared = __webpack_require__(31);
var NATIVE_SYMBOL_REGISTRY = __webpack_require__(122);

var StringToSymbolRegistry = shared('string-to-symbol-registry');
var SymbolToStringRegistry = shared('symbol-to-string-registry');

// `Symbol.for` method
// https://tc39.es/ecma262/#sec-symbol.for
$({ target: 'Symbol', stat: true, forced: !NATIVE_SYMBOL_REGISTRY }, {
  'for': function (key) {
    var string = toString(key);
    if (hasOwn(StringToSymbolRegistry, string)) return StringToSymbolRegistry[string];
    var symbol = getBuiltIn('Symbol')(string);
    StringToSymbolRegistry[string] = symbol;
    SymbolToStringRegistry[symbol] = string;
    return symbol;
  }
});


/***/ }),
/* 187 */
/***/ (function(module, exports, __webpack_require__) {

var $ = __webpack_require__(2);
var hasOwn = __webpack_require__(7);
var isSymbol = __webpack_require__(46);
var tryToString = __webpack_require__(34);
var shared = __webpack_require__(31);
var NATIVE_SYMBOL_REGISTRY = __webpack_require__(122);

var SymbolToStringRegistry = shared('symbol-to-string-registry');

// `Symbol.keyFor` method
// https://tc39.es/ecma262/#sec-symbol.keyfor
$({ target: 'Symbol', stat: true, forced: !NATIVE_SYMBOL_REGISTRY }, {
  keyFor: function keyFor(sym) {
    if (!isSymbol(sym)) throw TypeError(tryToString(sym) + ' is not a symbol');
    if (hasOwn(SymbolToStringRegistry, sym)) return SymbolToStringRegistry[sym];
  }
});


/***/ }),
/* 188 */
/***/ (function(module, exports, __webpack_require__) {

var $ = __webpack_require__(2);
var getBuiltIn = __webpack_require__(17);
var apply = __webpack_require__(91);
var call = __webpack_require__(6);
var uncurryThis = __webpack_require__(0);
var fails = __webpack_require__(1);
var isCallable = __webpack_require__(3);
var isSymbol = __webpack_require__(46);
var arraySlice = __webpack_require__(92);
var getReplacerFunction = __webpack_require__(189);
var NATIVE_SYMBOL = __webpack_require__(33);

var $String = String;
var $stringify = getBuiltIn('JSON', 'stringify');
var exec = uncurryThis(/./.exec);
var charAt = uncurryThis(''.charAt);
var charCodeAt = uncurryThis(''.charCodeAt);
var replace = uncurryThis(''.replace);
var numberToString = uncurryThis(1.0.toString);

var tester = /[\uD800-\uDFFF]/g;
var low = /^[\uD800-\uDBFF]$/;
var hi = /^[\uDC00-\uDFFF]$/;

var WRONG_SYMBOLS_CONVERSION = !NATIVE_SYMBOL || fails(function () {
  var symbol = getBuiltIn('Symbol')();
  // MS Edge converts symbol values to JSON as {}
  return $stringify([symbol]) != '[null]'
    // WebKit converts symbol values to JSON as null
    || $stringify({ a: symbol }) != '{}'
    // V8 throws on boxed symbols
    || $stringify(Object(symbol)) != '{}';
});

// https://github.com/tc39/proposal-well-formed-stringify
var ILL_FORMED_UNICODE = fails(function () {
  return $stringify('\uDF06\uD834') !== '"\\udf06\\ud834"'
    || $stringify('\uDEAD') !== '"\\udead"';
});

var stringifyWithSymbolsFix = function (it, replacer) {
  var args = arraySlice(arguments);
  var $replacer = getReplacerFunction(replacer);
  if (!isCallable($replacer) && (it === undefined || isSymbol(it))) return; // IE8 returns string on undefined
  args[1] = function (key, value) {
    // some old implementations (like WebKit) could pass numbers as keys
    if (isCallable($replacer)) value = call($replacer, this, $String(key), value);
    if (!isSymbol(value)) return value;
  };
  return apply($stringify, null, args);
};

var fixIllFormed = function (match, offset, string) {
  var prev = charAt(string, offset - 1);
  var next = charAt(string, offset + 1);
  if ((exec(low, match) && !exec(hi, next)) || (exec(hi, match) && !exec(low, prev))) {
    return '\\u' + numberToString(charCodeAt(match, 0), 16);
  } return match;
};

if ($stringify) {
  // `JSON.stringify` method
  // https://tc39.es/ecma262/#sec-json.stringify
  $({ target: 'JSON', stat: true, arity: 3, forced: WRONG_SYMBOLS_CONVERSION || ILL_FORMED_UNICODE }, {
    // eslint-disable-next-line no-unused-vars -- required for `.length`
    stringify: function stringify(it, replacer, space) {
      var args = arraySlice(arguments);
      var result = apply(WRONG_SYMBOLS_CONVERSION ? stringifyWithSymbolsFix : $stringify, null, args);
      return ILL_FORMED_UNICODE && typeof result == 'string' ? replace(result, tester, fixIllFormed) : result;
    }
  });
}


/***/ }),
/* 189 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var isArray = __webpack_require__(50);
var isCallable = __webpack_require__(3);
var classof = __webpack_require__(22);
var toString = __webpack_require__(11);

var push = uncurryThis([].push);

module.exports = function (replacer) {
  if (isCallable(replacer)) return replacer;
  if (!isArray(replacer)) return;
  var rawLength = replacer.length;
  var keys = [];
  for (var i = 0; i < rawLength; i++) {
    var element = replacer[i];
    if (typeof element == 'string') push(keys, element);
    else if (typeof element == 'number' || classof(element) == 'Number' || classof(element) == 'String') push(keys, toString(element));
  }
  var keysLength = keys.length;
  var root = true;
  return function (key, value) {
    if (root) {
      root = false;
      return value;
    }
    if (isArray(this)) return value;
    for (var j = 0; j < keysLength; j++) if (keys[j] === key) return value;
  };
};


/***/ }),
/* 190 */
/***/ (function(module, exports, __webpack_require__) {

var $ = __webpack_require__(2);
var NATIVE_SYMBOL = __webpack_require__(33);
var fails = __webpack_require__(1);
var getOwnPropertySymbolsModule = __webpack_require__(70);
var toObject = __webpack_require__(15);

// V8 ~ Chrome 38 and 39 `Object.getOwnPropertySymbols` fails on primitives
// https://bugs.chromium.org/p/v8/issues/detail?id=3443
var FORCED = !NATIVE_SYMBOL || fails(function () { getOwnPropertySymbolsModule.f(1); });

// `Object.getOwnPropertySymbols` method
// https://tc39.es/ecma262/#sec-object.getownpropertysymbols
$({ target: 'Object', stat: true, forced: FORCED }, {
  getOwnPropertySymbols: function getOwnPropertySymbols(it) {
    var $getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
    return $getOwnPropertySymbols ? $getOwnPropertySymbols(toObject(it)) : [];
  }
});


/***/ }),
/* 191 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var IteratorPrototype = __webpack_require__(125).IteratorPrototype;
var create = __webpack_require__(56);
var createPropertyDescriptor = __webpack_require__(48);
var setToStringTag = __webpack_require__(74);
var Iterators = __webpack_require__(55);

var returnThis = function () { return this; };

module.exports = function (IteratorConstructor, NAME, next, ENUMERABLE_NEXT) {
  var TO_STRING_TAG = NAME + ' Iterator';
  IteratorConstructor.prototype = create(IteratorPrototype, { next: createPropertyDescriptor(+!ENUMERABLE_NEXT, next) });
  setToStringTag(IteratorConstructor, TO_STRING_TAG, false, true);
  Iterators[TO_STRING_TAG] = returnThis;
  return IteratorConstructor;
};


/***/ }),
/* 192 */
/***/ (function(module, exports, __webpack_require__) {

var fails = __webpack_require__(1);

module.exports = !fails(function () {
  function F() { /* empty */ }
  F.prototype.constructor = null;
  // eslint-disable-next-line es/no-object-getprototypeof -- required for testing
  return Object.getPrototypeOf(new F()) !== F.prototype;
});


/***/ }),
/* 193 */
/***/ (function(module, exports, __webpack_require__) {

var fails = __webpack_require__(1);
var global = __webpack_require__(4);

// babel-minify and Closure Compiler transpiles RegExp('a', 'y') -> /a/y and it causes SyntaxError
var $RegExp = global.RegExp;

var UNSUPPORTED_Y = fails(function () {
  var re = $RegExp('a', 'y');
  re.lastIndex = 2;
  return re.exec('abcd') != null;
});

// UC Browser bug
// https://github.com/zloirock/core-js/issues/1008
var MISSED_STICKY = UNSUPPORTED_Y || fails(function () {
  return !$RegExp('a', 'y').sticky;
});

var BROKEN_CARET = UNSUPPORTED_Y || fails(function () {
  // https://bugzilla.mozilla.org/show_bug.cgi?id=773687
  var re = $RegExp('^r', 'gy');
  re.lastIndex = 2;
  return re.exec('str') != null;
});

module.exports = {
  BROKEN_CARET: BROKEN_CARET,
  MISSED_STICKY: MISSED_STICKY,
  UNSUPPORTED_Y: UNSUPPORTED_Y
};


/***/ }),
/* 194 */
/***/ (function(module, exports, __webpack_require__) {

var fails = __webpack_require__(1);
var global = __webpack_require__(4);

// babel-minify and Closure Compiler transpiles RegExp('.', 's') -> /./s and it causes SyntaxError
var $RegExp = global.RegExp;

module.exports = fails(function () {
  var re = $RegExp('.', 's');
  return !(re.dotAll && re.exec('\n') && re.flags === 's');
});


/***/ }),
/* 195 */
/***/ (function(module, exports, __webpack_require__) {

var fails = __webpack_require__(1);
var global = __webpack_require__(4);

// babel-minify and Closure Compiler transpiles RegExp('(?<a>b)', 'g') -> /(?<a>b)/g and it causes SyntaxError
var $RegExp = global.RegExp;

module.exports = fails(function () {
  var re = $RegExp('(?<a>b)', 'g');
  return re.exec('b').groups.a !== 'b' ||
    'b'.replace(re, '$<a>c') !== 'bc';
});


/***/ }),
/* 196 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.displayPresets = exports.digits = exports.currencyFormats = exports.atlernateDecimalsSymbols = void 0;
exports.toLocaleString = toLocaleString;
__webpack_require__(44);
__webpack_require__(43);
__webpack_require__(77);
__webpack_require__(95);
__webpack_require__(198);
__webpack_require__(199);
__webpack_require__(96);
__webpack_require__(30);
// Got this from MDN:
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/toLocaleString#Example:_Checking_for_support_for_locales_and_options_arguments
function toLocaleStringSupportsLocales() {
  var number = 0;
  try {
    number.toLocaleString("i");
  } catch (e) {
    return e.name === "RangeError";
  }
  return false;
}
function replaceSeparators(sNum, separators) {
  var sNumParts = sNum.split(".");
  if (separators && separators.thousands) {
    sNumParts[0] = sNumParts[0].replace(/(\d)(?=(\d\d\d)+(?!\d))/g, "$1".concat(separators.thousands));
  }
  sNum = sNumParts.join(separators.decimal);
  return sNum;
}
function renderFormat(template, props) {
  for (var prop in props) {
    if (!props.hasOwnProperty(prop)) {
      continue;
    }
    template = template.replace("{{".concat(prop, "}}"), props[prop]);
  }
  return template;
}
function mapMatch(map, locale) {
  var match = locale;
  var language = locale && locale.toLowerCase().match(/^\w+/);
  if (!map.hasOwnProperty(locale)) {
    if (map.hasOwnProperty(language)) {
      match = language;
    } else {
      match = "en";
    }
  }
  return map[match];
}
function dotThousCommaDec(sNum) {
  var separators = {
    decimal: ",",
    thousands: "."
  };
  return replaceSeparators(sNum, separators);
}
function commaThousDotDec(sNum) {
  var separators = {
    decimal: ".",
    thousands: ","
  };
  return replaceSeparators(sNum, separators);
}
function spaceThousCommaDec(sNum) {
  var seperators = {
    decimal: ",",
    thousands: "\xA0"
  };
  return replaceSeparators(sNum, seperators);
}
function spaceThousButCommaDec(sNum) {
  var sNumParts = sNum.split(".");
  if (sNumParts[0].length > 4) {
    return spaceThousCommaDec(sNum);
  }
  var seperators = {
    decimal: ",",
    thousands: ""
  };
  return replaceSeparators(sNum, seperators);
}
function apostrophThousDotDec(sNum) {
  var seperators = {
    decimal: ".",
    thousands: "'"
  };
  return replaceSeparators(sNum, seperators);
}
var transformForLocale = {
  "cs-CZ": spaceThousCommaDec,
  "da-DK": dotThousCommaDec,
  "de": dotThousCommaDec,
  "de-AT": dotThousCommaDec,
  "de-BE": dotThousCommaDec,
  "de-CH": apostrophThousDotDec,
  "de-DE": dotThousCommaDec,
  "de-LI": apostrophThousDotDec,
  "en": commaThousDotDec,
  "en-US": commaThousDotDec,
  "et-EE": spaceThousButCommaDec,
  "fr": spaceThousCommaDec,
  "fr-CH": apostrophThousDotDec,
  "fr-FR": spaceThousCommaDec,
  "hu": spaceThousCommaDec,
  "hu-HU": spaceThousCommaDec,
  "it": dotThousCommaDec,
  "nb-NO": spaceThousCommaDec,
  "ro": dotThousCommaDec,
  "ro-RO": dotThousCommaDec,
  "ru-EE": spaceThousCommaDec,
  "sv-SE": spaceThousCommaDec,
  "fi-FI": spaceThousCommaDec,
  "sv-FI": spaceThousCommaDec
};
var currencyFormatMap = {
  "cs-CZ": "postspace",
  "da-DK": "postspace",
  "de": "post",
  "de-AT": "prespace",
  "de-BE": "post",
  "de-CH": "prespace",
  "de-DE": "post",
  "de-LI": "post",
  "en": "pre",
  "en-US": "pre",
  "et-EE": "postspace",
  "fr": "postspace",
  "fr-CH": "prespace",
  "fr-FR": "postspace",
  "hu": "post",
  "hu-HU": "post",
  "it": "post",
  "nb-NO": "onlynum",
  "ro": "post",
  "ro-RO": "post",
  "ru-EE": "postspace",
  "sv-SE": "postspace"
};
var currencySymbols = {
  afn: "؋",
  ars: "$",
  awg: "ƒ",
  aud: "$",
  azn: "₼",
  bsd: "$",
  bbd: "$",
  byr: "p.",
  bzd: "BZ$",
  bmd: "$",
  bob: "Bs.",
  bam: "KM",
  bwp: "P",
  bgn: "лв",
  brl: "R$",
  bnd: "$",
  khr: "៛",
  cad: "$",
  kyd: "$",
  clp: "$",
  cny: "¥",
  cop: "$",
  crc: "₡",
  hrk: "kn",
  cup: "₱",
  czk: "Kč",
  dkk: "kr.",
  dop: "RD$",
  xcd: "$",
  egp: "£",
  svc: "$",
  eek: "kr",
  eur: "€",
  fkp: "£",
  fjd: "$",
  ghc: "¢",
  gip: "£",
  gtq: "Q",
  ggp: "£",
  gyd: "$",
  hnl: "L",
  hkd: "$",
  huf: "Ft",
  isk: "kr",
  inr: "₹",
  idr: "Rp",
  irr: "﷼",
  imp: "£",
  ils: "₪",
  jmd: "J$",
  jpy: "¥",
  jep: "£",
  kes: "KSh",
  kzt: "лв",
  kpw: "₩",
  krw: "₩",
  kgs: "лв",
  lak: "₭",
  lvl: "Ls",
  lbp: "£",
  lrd: "$",
  ltl: "Lt",
  mkd: "ден",
  myr: "RM",
  mur: "₨",
  mxn: "$",
  mnt: "₮",
  mzn: "MT",
  nad: "$",
  npr: "₨",
  ang: "ƒ",
  nzd: "$",
  nio: "C$",
  ngn: "₦",
  nok: "kr",
  omr: "﷼",
  pkr: "₨",
  pab: "B/.",
  pyg: "Gs",
  pen: "S/.",
  php: "₱",
  pln: "zł",
  qar: "﷼",
  ron: "lei",
  rub: "₽",
  shp: "£",
  sar: "﷼",
  rsd: "Дин.",
  scr: "₨",
  sgd: "$",
  sbd: "$",
  sos: "S",
  zar: "R",
  lkr: "₨",
  sek: "kr",
  chf: "CHF",
  srd: "$",
  syp: "£",
  tzs: "TSh",
  twd: "NT$",
  thb: "฿",
  ttd: "TT$",
  try: "",
  trl: "₤",
  tvd: "$",
  ugx: "USh",
  uah: "₴",
  gbp: "£",
  usd: "$",
  uyu: "$U",
  uzs: "лв",
  vef: "Bs",
  vnd: "₫",
  yer: "﷼",
  zwd: "Z$",
  xtk: ""
};
/**
 * used instead of the currency symbol if there is no decimals (eg 100,00 become 100,-)
 */
var atlernateDecimalsSymbols = {
  "nb-NO": ",-",
  "fr-CH": ".-",
  "de-CH": ".-"
};
exports.atlernateDecimalsSymbols = atlernateDecimalsSymbols;
var displayPresets = {
  "fr-CH": {
    symbol: {
      symbolFormatName: "onlynum"
    }
  },
  "de-CH": {
    symbol: {
      symbolFormatName: "onlynum"
    }
  }
};
/**
 * Constants
 */
exports.displayPresets = displayPresets;
var digits = {
  aed: 2,
  afn: 2,
  all: 2,
  amd: 2,
  ang: 2,
  aoa: 2,
  ars: 2,
  aud: 2,
  awg: 2,
  azn: 2,
  bam: 2,
  bbd: 2,
  bdt: 2,
  bgn: 2,
  bhd: 3,
  bif: 0,
  bmd: 2,
  bnd: 2,
  bob: 2,
  bov: 2,
  brl: 2,
  bsd: 2,
  btn: 2,
  bwp: 2,
  byn: 2,
  byr: 0,
  bzd: 2,
  cad: 2,
  cdf: 2,
  che: 2,
  chf: 2,
  chw: 2,
  clf: 4,
  clp: 0,
  cny: 2,
  cop: 0,
  cou: 2,
  crc: 2,
  cuc: 2,
  cup: 2,
  cve: 2,
  czk: 2,
  djf: 0,
  dkk: 2,
  dop: 2,
  dzd: 2,
  eek: 2,
  egp: 2,
  ern: 2,
  etb: 2,
  eur: 2,
  fjd: 2,
  fkp: 2,
  gbp: 2,
  gel: 2,
  ggp: 2,
  ghc: 2,
  ghs: 2,
  gip: 2,
  gmd: 2,
  gnf: 0,
  gtq: 2,
  gyd: 2,
  hkd: 2,
  hnl: 2,
  hrk: 2,
  htg: 2,
  huf: 0,
  idr: 2,
  ils: 2,
  imp: 2,
  inr: 2,
  iqd: 3,
  irr: 2,
  isk: 2,
  jep: 2,
  jmd: 2,
  jod: 3,
  jpy: 0,
  kes: 2,
  kgs: 2,
  khr: 2,
  kmf: 0,
  kpw: 0,
  krw: 0,
  kwd: 3,
  kyd: 2,
  kzt: 2,
  lak: 2,
  lbp: 2,
  lkr: 2,
  lrd: 2,
  lsl: 2,
  ltl: 2,
  lvl: 2,
  lyd: 3,
  mad: 2,
  mdl: 2,
  mga: 2,
  mkd: 2,
  mmk: 2,
  mnt: 2,
  mop: 2,
  mro: 2,
  mur: 2,
  mvr: 2,
  mwk: 2,
  mxn: 2,
  mxv: 2,
  myr: 2,
  mzn: 2,
  nad: 2,
  ngn: 2,
  nio: 2,
  nok: 2,
  npr: 2,
  nzd: 2,
  omr: 3,
  pab: 2,
  pen: 2,
  pgk: 2,
  php: 2,
  pkr: 2,
  pln: 2,
  pyg: 0,
  qar: 2,
  ron: 2,
  rsd: 2,
  rub: 2,
  rur: 2,
  rwf: 0,
  sar: 2,
  sbd: 2,
  scr: 2,
  sdg: 2,
  sek: 2,
  sgd: 2,
  shp: 2,
  sll: 2,
  sos: 2,
  srd: 2,
  ssp: 2,
  std: 2,
  svc: 2,
  syp: 2,
  szl: 2,
  thb: 2,
  tjs: 2,
  tmt: 2,
  tnd: 3,
  top: 2,
  trl: 2,
  try: 2,
  ttd: 2,
  twd: 0,
  tzs: 0,
  uah: 2,
  ugx: 0,
  usd: 2,
  usn: 2,
  uyi: 0,
  uyu: 0,
  uzs: 2,
  vef: 2,
  vnd: 0,
  vuv: 0,
  wst: 2,
  xaf: 0,
  xcd: 2,
  xdr: 0,
  xof: 0,
  xpf: 0,
  xsu: 0,
  xua: 0,
  yer: 2,
  zar: 2,
  zmw: 2,
  zwd: 2,
  zwl: 2,
  xtk: 0
};
//  "\xa0" is a non breaking space
exports.digits = digits;
var currencyFormats = {
  pre: "{{code}}{{num}}",
  post: "{{num}}{{code}}",
  postspace: "{{num}}" + "\xa0" + "{{code}}",
  prespace: "{{code}}" + "\xa0" + "{{num}}",
  onlynum: "{{num}}"
};
exports.currencyFormats = currencyFormats;
function toLocaleString(value, locale, options) {
  var _a;
  if (locale && locale.length < 2) {
    throw new RangeError("Invalid language tag: ".concat(locale));
  }
  // exemple values are (150000, fr-CH ...) // (150001, fr-CH ...)
  // value example = 150000 // 150001
  value = value / Math.pow(10, digits[options.currency.toLowerCase()]);
  // value example = 1500 // 1500.01
  var scaleOrder = Math.pow(10, options.minimumFractionDigits);
  value = Math.round(value * scaleOrder) / scaleOrder;
  // value example = 1500 // 1500.01 (round depending on minimumFractionDigits, no change on those examples)
  var sNum = value.toFixed(options.minimumFractionDigits);
  // sNum example = "1500.00"  // "1500.01"
  sNum = mapMatch(transformForLocale, locale)(sNum, options);
  // sNum example = "1'500.00"  // "1'500.01"
  if (options && options.currency && options.style === "currency") {
    if (value % 1 === 0 && options.alternativeDecimalSymbol !== null) {
      // alternative decimal used
      if (options.minimumFractionDigits > 0) {
        // delete the decimals
        sNum = sNum.slice(0, -(options.minimumFractionDigits + 1));
        // sNum example = "1'500"  // "1'500.01"(unaffected)
      }

      sNum = sNum.concat(options.alternativeDecimalSymbol);
      // sNum example = "1'500.-"  // "1'500.01"(unaffected)
    }

    var format = currencyFormats[(_a = options.symbolFormatName) !== null && _a !== void 0 ? _a : mapMatch(currencyFormatMap, locale)];
    // example format = "prespace"
    if (options.currencyDisplay === "code") {
      sNum = renderFormat(format, {
        num: sNum,
        code: options.currency.toUpperCase()
      });
      // sNum example would be = "CHF 1'500.-" // "CHF 1'500.01"
    } else {
      var currencySymbol = currencySymbols[options.currency.toLowerCase()];
      sNum = renderFormat(format, {
        num: sNum,
        code: currencySymbol
      });
      // CHF currency symbol is "CHF", so sNum example is = "CHF 1'500.-" // "CHF 1'500.01"
    }
  }

  return sNum;
}

/***/ }),
/* 197 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var toObject = __webpack_require__(15);

var floor = Math.floor;
var charAt = uncurryThis(''.charAt);
var replace = uncurryThis(''.replace);
var stringSlice = uncurryThis(''.slice);
// eslint-disable-next-line redos/no-vulnerable -- safe
var SUBSTITUTION_SYMBOLS = /\$([$&'`]|\d{1,2}|<[^>]*>)/g;
var SUBSTITUTION_SYMBOLS_NO_NAMED = /\$([$&'`]|\d{1,2})/g;

// `GetSubstitution` abstract operation
// https://tc39.es/ecma262/#sec-getsubstitution
module.exports = function (matched, str, position, captures, namedCaptures, replacement) {
  var tailPos = position + matched.length;
  var m = captures.length;
  var symbols = SUBSTITUTION_SYMBOLS_NO_NAMED;
  if (namedCaptures !== undefined) {
    namedCaptures = toObject(namedCaptures);
    symbols = SUBSTITUTION_SYMBOLS;
  }
  return replace(replacement, symbols, function (match, ch) {
    var capture;
    switch (charAt(ch, 0)) {
      case '$': return '$';
      case '&': return matched;
      case '`': return stringSlice(str, 0, position);
      case "'": return stringSlice(str, tailPos);
      case '<':
        capture = namedCaptures[stringSlice(ch, 1, -1)];
        break;
      default: // \d\d?
        var n = +ch;
        if (n === 0) return match;
        if (n > m) {
          var f = floor(n / 10);
          if (f === 0) return match;
          if (f <= m) return captures[f - 1] === undefined ? charAt(ch, 1) : captures[f - 1] + charAt(ch, 1);
          return match;
        }
        capture = captures[n - 1];
    }
    return capture === undefined ? '' : capture;
  });
};


/***/ }),
/* 198 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var call = __webpack_require__(6);
var fixRegExpWellKnownSymbolLogic = __webpack_require__(131);
var anObject = __webpack_require__(9);
var isNullOrUndefined = __webpack_require__(32);
var toLength = __webpack_require__(84);
var toString = __webpack_require__(11);
var requireObjectCoercible = __webpack_require__(21);
var getMethod = __webpack_require__(47);
var advanceStringIndex = __webpack_require__(132);
var regExpExec = __webpack_require__(133);

// @@match logic
fixRegExpWellKnownSymbolLogic('match', function (MATCH, nativeMatch, maybeCallNative) {
  return [
    // `String.prototype.match` method
    // https://tc39.es/ecma262/#sec-string.prototype.match
    function match(regexp) {
      var O = requireObjectCoercible(this);
      var matcher = isNullOrUndefined(regexp) ? undefined : getMethod(regexp, MATCH);
      return matcher ? call(matcher, regexp, O) : new RegExp(regexp)[MATCH](toString(O));
    },
    // `RegExp.prototype[@@match]` method
    // https://tc39.es/ecma262/#sec-regexp.prototype-@@match
    function (string) {
      var rx = anObject(this);
      var S = toString(string);
      var res = maybeCallNative(nativeMatch, rx, S);

      if (res.done) return res.value;

      if (!rx.global) return regExpExec(rx, S);

      var fullUnicode = rx.unicode;
      rx.lastIndex = 0;
      var A = [];
      var n = 0;
      var result;
      while ((result = regExpExec(rx, S)) !== null) {
        var matchStr = toString(result[0]);
        A[n] = matchStr;
        if (matchStr === '') rx.lastIndex = advanceStringIndex(S, toLength(rx.lastIndex), fullUnicode);
        n++;
      }
      return n === 0 ? null : A;
    }
  ];
});


/***/ }),
/* 199 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var uncurryThis = __webpack_require__(0);
var toIntegerOrInfinity = __webpack_require__(28);
var thisNumberValue = __webpack_require__(134);
var $repeat = __webpack_require__(200);
var fails = __webpack_require__(1);

var $RangeError = RangeError;
var $String = String;
var floor = Math.floor;
var repeat = uncurryThis($repeat);
var stringSlice = uncurryThis(''.slice);
var nativeToFixed = uncurryThis(1.0.toFixed);

var pow = function (x, n, acc) {
  return n === 0 ? acc : n % 2 === 1 ? pow(x, n - 1, acc * x) : pow(x * x, n / 2, acc);
};

var log = function (x) {
  var n = 0;
  var x2 = x;
  while (x2 >= 4096) {
    n += 12;
    x2 /= 4096;
  }
  while (x2 >= 2) {
    n += 1;
    x2 /= 2;
  } return n;
};

var multiply = function (data, n, c) {
  var index = -1;
  var c2 = c;
  while (++index < 6) {
    c2 += n * data[index];
    data[index] = c2 % 1e7;
    c2 = floor(c2 / 1e7);
  }
};

var divide = function (data, n) {
  var index = 6;
  var c = 0;
  while (--index >= 0) {
    c += data[index];
    data[index] = floor(c / n);
    c = (c % n) * 1e7;
  }
};

var dataToString = function (data) {
  var index = 6;
  var s = '';
  while (--index >= 0) {
    if (s !== '' || index === 0 || data[index] !== 0) {
      var t = $String(data[index]);
      s = s === '' ? t : s + repeat('0', 7 - t.length) + t;
    }
  } return s;
};

var FORCED = fails(function () {
  return nativeToFixed(0.00008, 3) !== '0.000' ||
    nativeToFixed(0.9, 0) !== '1' ||
    nativeToFixed(1.255, 2) !== '1.25' ||
    nativeToFixed(1000000000000000128.0, 0) !== '1000000000000000128';
}) || !fails(function () {
  // V8 ~ Android 4.3-
  nativeToFixed({});
});

// `Number.prototype.toFixed` method
// https://tc39.es/ecma262/#sec-number.prototype.tofixed
$({ target: 'Number', proto: true, forced: FORCED }, {
  toFixed: function toFixed(fractionDigits) {
    var number = thisNumberValue(this);
    var fractDigits = toIntegerOrInfinity(fractionDigits);
    var data = [0, 0, 0, 0, 0, 0];
    var sign = '';
    var result = '0';
    var e, z, j, k;

    // TODO: ES2018 increased the maximum number of fraction digits to 100, need to improve the implementation
    if (fractDigits < 0 || fractDigits > 20) throw $RangeError('Incorrect fraction digits');
    // eslint-disable-next-line no-self-compare -- NaN check
    if (number != number) return 'NaN';
    if (number <= -1e21 || number >= 1e21) return $String(number);
    if (number < 0) {
      sign = '-';
      number = -number;
    }
    if (number > 1e-21) {
      e = log(number * pow(2, 69, 1)) - 69;
      z = e < 0 ? number * pow(2, -e, 1) : number / pow(2, e, 1);
      z *= 0x10000000000000;
      e = 52 - e;
      if (e > 0) {
        multiply(data, 0, z);
        j = fractDigits;
        while (j >= 7) {
          multiply(data, 1e7, 0);
          j -= 7;
        }
        multiply(data, pow(10, j, 1), 0);
        j = e - 1;
        while (j >= 23) {
          divide(data, 1 << 23);
          j -= 23;
        }
        divide(data, 1 << j);
        multiply(data, 1, 1);
        divide(data, 2);
        result = dataToString(data);
      } else {
        multiply(data, 0, z);
        multiply(data, 1 << -e, 0);
        result = dataToString(data) + repeat('0', fractDigits);
      }
    }
    if (fractDigits > 0) {
      k = result.length;
      result = sign + (k <= fractDigits
        ? '0.' + repeat('0', fractDigits - k) + result
        : stringSlice(result, 0, k - fractDigits) + '.' + stringSlice(result, k - fractDigits));
    } else {
      result = sign + result;
    } return result;
  }
});


/***/ }),
/* 200 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var toIntegerOrInfinity = __webpack_require__(28);
var toString = __webpack_require__(11);
var requireObjectCoercible = __webpack_require__(21);

var $RangeError = RangeError;

// `String.prototype.repeat` method implementation
// https://tc39.es/ecma262/#sec-string.prototype.repeat
module.exports = function repeat(count) {
  var str = toString(requireObjectCoercible(this));
  var result = '';
  var n = toIntegerOrInfinity(count);
  if (n < 0 || n == Infinity) throw $RangeError('Wrong number of repetitions');
  for (;n > 0; (n >>>= 1) && (str += str)) if (n & 1) result += str;
  return result;
};


/***/ }),
/* 201 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Amount = void 0;
__webpack_require__(202);
__webpack_require__(10);
__webpack_require__(78);
var _numeral = __webpack_require__(130);
var __assign = void 0 && (void 0).__assign || function () {
  __assign = Object.assign || function (t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
    }
    return t;
  };
  return __assign.apply(this, arguments);
};
/**
 * Amount manipulation
 * value is a number given in the fraction digits of the currency
 * currency is an iso 4217 code
 */
var Amount = /** @class */function () {
  /**
   * Creates an amount object from an IAmount or a value.
   * If only a number value is given, the currency code will be the Numeral one.
   */
  function Amount(a) {
    if (typeof a === "number") {
      this.value = a;
      this.currency = _numeral.Numeral.currencyCode;
    } else {
      this.value = a.value;
      this.currency = a.currency;
    }
  }
  /**
   * Multiplies an amount with a scalar
   * @return a new amount with the operation result
   */
  Amount.prototype.multiply = function (scalar) {
    return new Amount({
      value: scalar * this.value,
      currency: this.currency
    });
  };
  /**
   * Sum amounts
   * @return a new amount with the operation result
   */
  Amount.prototype.sum = function (amount) {
    if (amount.currency !== this.currency) {
      throw new Error("can't sum amount with inconsistent currencies.");
    }
    return new Amount({
      value: amount.value + this.value,
      currency: this.currency
    });
  };
  /**
   * Currency formatting
   * shortcut for Numeral.currency(amount, options)
   */
  Amount.prototype.toString = function (options) {
    return _numeral.Numeral.currency(this, options);
  };
  /**
   * Create an Amount object from a number
   * @param value the value is a number given in the fraction digits of the currency
   * the currencyCode will be the one of Numeral
   */
  Amount.fromNumber = function (value) {
    return new Amount({
      value: value,
      currency: _numeral.Numeral.currencyCode
    });
  };
  /**
   * Format an amount value to string and show 2 digits if it's a whole number
   * @param value: The value is a number given in the fraction digits of the currency
   * @param options: Options to pass to the Numeral.currency function
   * @return The formatted amount in string
   */
  Amount.formatFromWholeOrRealNumber = function (value, options) {
    if (options === void 0) {
      options = {};
    }
    var minimumFractionDigits = value / 100 % 1 > 0 ? 2 : 0;
    return _numeral.Numeral.currency(Amount.fromNumber(value), __assign({
      minimumFractionDigits: minimumFractionDigits
    }, options));
  };
  return Amount;
}();
exports.Amount = Amount;

/***/ }),
/* 202 */
/***/ (function(module, exports, __webpack_require__) {

var $ = __webpack_require__(2);
var assign = __webpack_require__(203);

// `Object.assign` method
// https://tc39.es/ecma262/#sec-object.assign
// eslint-disable-next-line es/no-object-assign -- required for testing
$({ target: 'Object', stat: true, arity: 2, forced: Object.assign !== assign }, {
  assign: assign
});


/***/ }),
/* 203 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var DESCRIPTORS = __webpack_require__(8);
var uncurryThis = __webpack_require__(0);
var call = __webpack_require__(6);
var fails = __webpack_require__(1);
var objectKeys = __webpack_require__(71);
var getOwnPropertySymbolsModule = __webpack_require__(70);
var propertyIsEnumerableModule = __webpack_require__(88);
var toObject = __webpack_require__(15);
var IndexedObject = __webpack_require__(67);

// eslint-disable-next-line es/no-object-assign -- safe
var $assign = Object.assign;
// eslint-disable-next-line es/no-object-defineproperty -- required for testing
var defineProperty = Object.defineProperty;
var concat = uncurryThis([].concat);

// `Object.assign` method
// https://tc39.es/ecma262/#sec-object.assign
module.exports = !$assign || fails(function () {
  // should have correct order of operations (Edge bug)
  if (DESCRIPTORS && $assign({ b: 1 }, $assign(defineProperty({}, 'a', {
    enumerable: true,
    get: function () {
      defineProperty(this, 'b', {
        value: 3,
        enumerable: false
      });
    }
  }), { b: 2 })).b !== 1) return true;
  // should work with symbols and should have deterministic property order (V8 bug)
  var A = {};
  var B = {};
  // eslint-disable-next-line es/no-symbol -- safe
  var symbol = Symbol();
  var alphabet = 'abcdefghijklmnopqrst';
  A[symbol] = 7;
  alphabet.split('').forEach(function (chr) { B[chr] = chr; });
  return $assign({}, A)[symbol] != 7 || objectKeys($assign({}, B)).join('') != alphabet;
}) ? function assign(target, source) { // eslint-disable-line no-unused-vars -- required for `.length`
  var T = toObject(target);
  var argumentsLength = arguments.length;
  var index = 1;
  var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
  var propertyIsEnumerable = propertyIsEnumerableModule.f;
  while (argumentsLength > index) {
    var S = IndexedObject(arguments[index++]);
    var keys = getOwnPropertySymbols ? concat(objectKeys(S), getOwnPropertySymbols(S)) : objectKeys(S);
    var length = keys.length;
    var j = 0;
    var key;
    while (length > j) {
      key = keys[j++];
      if (!DESCRIPTORS || call(propertyIsEnumerable, S, key)) T[key] = S[key];
    }
  } return T;
} : $assign;


/***/ }),
/* 204 */
/***/ (function(module, exports, __webpack_require__) {

var call = __webpack_require__(6);
var hasOwn = __webpack_require__(7);
var isPrototypeOf = __webpack_require__(26);
var regExpFlags = __webpack_require__(129);

var RegExpPrototype = RegExp.prototype;

module.exports = function (R) {
  var flags = R.flags;
  return flags === undefined && !('flags' in RegExpPrototype) && !hasOwn(R, 'flags') && isPrototypeOf(RegExpPrototype, R)
    ? call(regExpFlags, R) : flags;
};


/***/ }),
/* 205 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Vector2 = void 0;
__webpack_require__(10);
__webpack_require__(37);
var Vector2 = /** @class */function () {
  /**
   * Constructor.
   * @param x: the x value of the new vector.
   * @param y: the y value of the new vector.
   */
  function Vector2(x, y) {
    this.x = x;
    this.y = y;
  }
  /**
   * Returns a vector from another object type
   * @param value the value containing (x, y)
   */
  Vector2.from = function (value) {
    return new Vector2(value.x, value.y);
  };
  /**
   * Returns the distance between the vectors
   * @param vector1: the first vector
   * @param vector2: the second vector
   */
  Vector2.Distance = function (vector1, vector2) {
    return Math.sqrt(this.DistanceSquared(vector1, vector2));
  };
  /**
   * Returns the squared distance between the vectors
   * @param vector1: the first vector
   * @param vector2: the second vector
   */
  Vector2.DistanceSquared = function (vector1, vector2) {
    var x = vector1.x - vector2.x;
    var y = vector1.y - vector2.y;
    return x * x + y * y;
  };
  /**
   * Clamps the given vector in the interval [min,max].
   * @param vector the vector to clamp.
   * @param min the minimum value the vector can have.
   * @param max the maximum value the vector can have,.
   */
  Vector2.clamp = function (vector, min, max) {
    var x = vector.x;
    x = x > max.x ? max.x : x;
    x = x < min.x ? min.x : x;
    var y = vector.y;
    y = y > max.y ? max.y : y;
    y = y < min.y ? min.y : y;
    return new Vector2(x, y);
  };
  /**
   * Gets the dot product of the given vectors (left and right).
   * @param left the first vector.
   * @param right the second vector.
   */
  Vector2.dot = function (left, right) {
    return left.x * right.x + left.y * right.y;
  };
  /**
   * Returns the vector located at the center of the given two vectors (vector1 and vector2).
   * @param vector1 defines the first vector.
   * @param vector2 defines the second vector.
   */
  Vector2.center = function (vector1, vector2) {
    return vector1.add(vector2).scaleSelf(0.5);
  };
  /**
   * Gets a new Vector2(0, 0)
   */
  Vector2.zero = function () {
    return new Vector2(0, 0);
  };
  /**
   * Returns a new Vector2 from the given array (index 0 and index 1).
   * @param array the array containing x and y.
   */
  Vector2.fromArray = function (array) {
    return new Vector2(array[0], array[1]);
  };
  /**
   * Sets the vector's value
   * @param x: the x value
   * @param y: the y value
   */
  Vector2.prototype.set = function (x, y) {
    this.x = x;
    this.y = y;
    return this;
  };
  /**
   * Copies the given vector to the current one.
   * @param otherVector the other vector to copy.
   */
  Vector2.prototype.copy = function (otherVector) {
    this.x = otherVector.x;
    this.y = otherVector.y;
    return this;
  };
  /**
   * Adds another vector with the current one and returns the new vector reference.
   * @param otherVector defines the other vector
   */
  Vector2.prototype.add = function (otherVector) {
    return new Vector2(this.x + otherVector.x, this.y + otherVector.y);
  };
  /**
   * Adds another vector in and with the current one.
   * @param otherVector defines the other vector
   */
  Vector2.prototype.addSelf = function (otherVector) {
    this.x += otherVector.x;
    this.y += otherVector.y;
    return this;
  };
  /**
   * Subtracts this with the given vector and returns the new vector.
   * @param otherVector the other vector to subtract.
   */
  Vector2.prototype.subtract = function (otherVector) {
    return new Vector2(this.x - otherVector.x, this.y - otherVector.y);
  };
  /**
   * Subtracts this in and with the given vector.
   * @param otherVector the other vector to subtract.
   */
  Vector2.prototype.subtractSelf = function (otherVector) {
    this.x -= otherVector.x;
    this.y -= otherVector.y;
    return this;
  };
  /**
   * Returns a new Vector2 set with the multiplication of the current Vector2 and the given one and returns the new vector.
   * @param otherVector defines the other vector
   */
  Vector2.prototype.multiply = function (otherVector) {
    return new Vector2(this.x * otherVector.x, this.y * otherVector.y);
  };
  /**
   * Returns the current Vector2 set with the multiplication of the current Vector2 and the given one.
   * @param otherVector defines the other vector
   */
  Vector2.prototype.multiplySelf = function (otherVector) {
    this.x *= otherVector.x;
    this.y *= otherVector.y;
    return this;
  };
  /**
   * Returns a new Vector2 set with the Vector2 coordinates divided by the given one and returns the new vector.
   * @param otherVector defines the other vector
   */
  Vector2.prototype.divide = function (otherVector) {
    return new Vector2(this.x / otherVector.x, this.y / otherVector.y);
  };
  /**
   * Returns the current Vector2 set with the Vector2 coordinates divided by the given one.
   * @param otherVector defines the other vector
   */
  Vector2.prototype.divideSelf = function (otherVector) {
    this.x /= otherVector.x;
    this.y /= otherVector.y;
    return this;
  };
  /**
   * Gets the length of the vector.
   */
  Vector2.prototype.length = function () {
    return Math.sqrt(this.x * this.x + this.y * this.y);
  };
  /**
   * Gets a new Vector2 with current Vector2 negated coordinates
   * @returns a new Vector2
   */
  Vector2.prototype.negate = function () {
    return new Vector2(-this.x, -this.y);
  };
  /**
   * Negates the current vector.
   */
  Vector2.prototype.negateSelf = function () {
    this.x *= -1;
    this.y *= -1;
    return this;
  };
  /**
   * Returns a new Vector2 scaled by "scale" from the current Vector2.
   * @param scale defines the scaling factor
   */
  Vector2.prototype.scale = function (scale) {
    return new Vector2(this.x * scale, this.y * scale);
  };
  /**
   * Returns the current Vector2 scale by the given number.
   * @param scale defines the scaling factor.
   */
  Vector2.prototype.scaleSelf = function (scale) {
    this.x *= scale;
    this.y *= scale;
    return this;
  };
  /**
   * Returns true if the given vector "otherVector" is equal to this vector.
   * @param otherVector defines the other vector
   */
  Vector2.prototype.equals = function (otherVector) {
    return otherVector && this.x === otherVector.x && this.y === otherVector.y;
  };
  /**
   * Normalizes the vector.
   */
  Vector2.prototype.normalize = function () {
    var len = this.length();
    if (len === 0) {
      return this;
    }
    var num = 1.0 / len;
    this.x *= num;
    this.y *= num;
    return this;
  };
  /**
   * Floors the current vector values and returns the new vector.
   */
  Vector2.prototype.floor = function () {
    return new Vector2(Math.round(this.x), Math.round(this.y));
  };
  /**
   * Floors the current vector values.
   */
  Vector2.prototype.floorSelf = function () {
    this.x = Math.round(this.x);
    this.y = Math.round(this.y);
    return this;
  };
  /**
   * Transforms the current vector to an array interpretation.
   * @param array the optional array to write (x,y) in.
   */
  Vector2.prototype.toArray = function (array) {
    if (array === void 0) {
      array = [];
    }
    array[0] = this.x;
    array[1] = this.y;
    return array;
  };
  return Vector2;
}();
exports.Vector2 = Vector2;

/***/ }),
/* 206 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Scalar = void 0;
__webpack_require__(207);
var Scalar = /** @class */function () {
  function Scalar() {}
  /**
   * Returns the value itself if it's between min and max.
   * Returns min if the value is lower than min.
   * Returns max if the value is greater than max.
   * @param value the value to clmap
   * @param min the min value to clamp to (default: 0)
   * @param max the max value to clamp to (default: 1)
   */
  Scalar.clamp = function (value, min, max) {
    if (min === void 0) {
      min = 0;
    }
    if (max === void 0) {
      max = 1;
    }
    return Math.min(max, Math.max(min, value));
  };
  /**
   * the log2 of value.
   * @param value the value to compute log2 of the given value
   */
  Scalar.log2 = function (value) {
    return Math.log(value) * Math.LOG2E;
  };
  /**
   * Returns -1 if value is negative and +1 is value is positive.
   * @param value the value
   * @returns the value itself if it's equal to zero.
   */
  Scalar.sign = function (value) {
    value = Number(value); // convert to a number
    if (value === 0 || isNaN(value)) {
      return value;
    }
    return value > 0 ? 1 : -1;
  };
  /**
   * Returns true if the absolute difference between a and b is lower than epsilon (default = 1.401298E-45)
   * @param a number to compare with b
   * @param b number to compare with a
   * @param epsilon (default = 1.401298E-45)
   */
  Scalar.withinEpsilon = function (a, b, epsilon) {
    if (epsilon === void 0) {
      epsilon = 1.401298e-45;
    }
    var num = a - b;
    return -epsilon <= num && num <= epsilon;
  };
  return Scalar;
}();
exports.Scalar = Scalar;

/***/ }),
/* 207 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var IS_PURE = __webpack_require__(14);
var DESCRIPTORS = __webpack_require__(8);
var global = __webpack_require__(4);
var path = __webpack_require__(121);
var uncurryThis = __webpack_require__(0);
var isForced = __webpack_require__(90);
var hasOwn = __webpack_require__(7);
var inheritIfRequired = __webpack_require__(208);
var isPrototypeOf = __webpack_require__(26);
var isSymbol = __webpack_require__(46);
var toPrimitive = __webpack_require__(102);
var fails = __webpack_require__(1);
var getOwnPropertyNames = __webpack_require__(68).f;
var getOwnPropertyDescriptor = __webpack_require__(51).f;
var defineProperty = __webpack_require__(12).f;
var thisNumberValue = __webpack_require__(134);
var trim = __webpack_require__(209).trim;

var NUMBER = 'Number';
var NativeNumber = global[NUMBER];
var PureNumberNamespace = path[NUMBER];
var NumberPrototype = NativeNumber.prototype;
var TypeError = global.TypeError;
var stringSlice = uncurryThis(''.slice);
var charCodeAt = uncurryThis(''.charCodeAt);

// `ToNumeric` abstract operation
// https://tc39.es/ecma262/#sec-tonumeric
var toNumeric = function (value) {
  var primValue = toPrimitive(value, 'number');
  return typeof primValue == 'bigint' ? primValue : toNumber(primValue);
};

// `ToNumber` abstract operation
// https://tc39.es/ecma262/#sec-tonumber
var toNumber = function (argument) {
  var it = toPrimitive(argument, 'number');
  var first, third, radix, maxCode, digits, length, index, code;
  if (isSymbol(it)) throw TypeError('Cannot convert a Symbol value to a number');
  if (typeof it == 'string' && it.length > 2) {
    it = trim(it);
    first = charCodeAt(it, 0);
    if (first === 43 || first === 45) {
      third = charCodeAt(it, 2);
      if (third === 88 || third === 120) return NaN; // Number('+0x1') should be NaN, old V8 fix
    } else if (first === 48) {
      switch (charCodeAt(it, 1)) {
        case 66: case 98: radix = 2; maxCode = 49; break; // fast equal of /^0b[01]+$/i
        case 79: case 111: radix = 8; maxCode = 55; break; // fast equal of /^0o[0-7]+$/i
        default: return +it;
      }
      digits = stringSlice(it, 2);
      length = digits.length;
      for (index = 0; index < length; index++) {
        code = charCodeAt(digits, index);
        // parseInt parses a string to a first unavailable symbol
        // but ToNumber should return NaN if a string contains unavailable symbols
        if (code < 48 || code > maxCode) return NaN;
      } return parseInt(digits, radix);
    }
  } return +it;
};

var FORCED = isForced(NUMBER, !NativeNumber(' 0o1') || !NativeNumber('0b1') || NativeNumber('+0x1'));

var calledWithNew = function (dummy) {
  // includes check on 1..constructor(foo) case
  return isPrototypeOf(NumberPrototype, dummy) && fails(function () { thisNumberValue(dummy); });
};

// `Number` constructor
// https://tc39.es/ecma262/#sec-number-constructor
var NumberWrapper = function Number(value) {
  var n = arguments.length < 1 ? 0 : NativeNumber(toNumeric(value));
  return calledWithNew(this) ? inheritIfRequired(Object(n), this, NumberWrapper) : n;
};

NumberWrapper.prototype = NumberPrototype;
if (FORCED && !IS_PURE) NumberPrototype.constructor = NumberWrapper;

$({ global: true, constructor: true, wrap: true, forced: FORCED }, {
  Number: NumberWrapper
});

// Use `internal/copy-constructor-properties` helper in `core-js@4`
var copyConstructorProperties = function (target, source) {
  for (var keys = DESCRIPTORS ? getOwnPropertyNames(source) : (
    // ES3:
    'MAX_VALUE,MIN_VALUE,NaN,NEGATIVE_INFINITY,POSITIVE_INFINITY,' +
    // ES2015 (in case, if modules with ES2015 Number statics required before):
    'EPSILON,MAX_SAFE_INTEGER,MIN_SAFE_INTEGER,isFinite,isInteger,isNaN,isSafeInteger,parseFloat,parseInt,' +
    // ESNext
    'fromString,range'
  ).split(','), j = 0, key; keys.length > j; j++) {
    if (hasOwn(source, key = keys[j]) && !hasOwn(target, key)) {
      defineProperty(target, key, getOwnPropertyDescriptor(source, key));
    }
  }
};

if (IS_PURE && PureNumberNamespace) copyConstructorProperties(path[NUMBER], PureNumberNamespace);
if (FORCED || IS_PURE) copyConstructorProperties(path[NUMBER], NativeNumber);


/***/ }),
/* 208 */
/***/ (function(module, exports, __webpack_require__) {

var isCallable = __webpack_require__(3);
var isObject = __webpack_require__(13);
var setPrototypeOf = __webpack_require__(73);

// makes subclassing work correct for wrapped built-ins
module.exports = function ($this, dummy, Wrapper) {
  var NewTarget, NewTargetPrototype;
  if (
    // it can work only with native `setPrototypeOf`
    setPrototypeOf &&
    // we haven't completely correct pre-ES6 way for getting `new.target`, so use this
    isCallable(NewTarget = dummy.constructor) &&
    NewTarget !== Wrapper &&
    isObject(NewTargetPrototype = NewTarget.prototype) &&
    NewTargetPrototype !== Wrapper.prototype
  ) setPrototypeOf($this, NewTargetPrototype);
  return $this;
};


/***/ }),
/* 209 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var requireObjectCoercible = __webpack_require__(21);
var toString = __webpack_require__(11);
var whitespaces = __webpack_require__(210);

var replace = uncurryThis(''.replace);
var ltrim = RegExp('^[' + whitespaces + ']+');
var rtrim = RegExp('(^|[^' + whitespaces + '])[' + whitespaces + ']+$');

// `String.prototype.{ trim, trimStart, trimEnd, trimLeft, trimRight }` methods implementation
var createMethod = function (TYPE) {
  return function ($this) {
    var string = toString(requireObjectCoercible($this));
    if (TYPE & 1) string = replace(string, ltrim, '');
    if (TYPE & 2) string = replace(string, rtrim, '$1');
    return string;
  };
};

module.exports = {
  // `String.prototype.{ trimLeft, trimStart }` methods
  // https://tc39.es/ecma262/#sec-string.prototype.trimstart
  start: createMethod(1),
  // `String.prototype.{ trimRight, trimEnd }` methods
  // https://tc39.es/ecma262/#sec-string.prototype.trimend
  end: createMethod(2),
  // `String.prototype.trim` method
  // https://tc39.es/ecma262/#sec-string.prototype.trim
  trim: createMethod(3)
};


/***/ }),
/* 210 */
/***/ (function(module, exports) {

// a string of all valid unicode whitespaces
module.exports = '\u0009\u000A\u000B\u000C\u000D\u0020\u00A0\u1680\u2000\u2001\u2002' +
  '\u2003\u2004\u2005\u2006\u2007\u2008\u2009\u200A\u202F\u205F\u3000\u2028\u2029\uFEFF';


/***/ }),
/* 211 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.MD5 = void 0;
__webpack_require__(10);
__webpack_require__(78);
__webpack_require__(43);
__webpack_require__(77);
/**
 * Class from https://github.com/Hipparch/Md5-typescript
 */
var MD5 = /** @class */function () {
  function MD5() {}
  /**
   * Hash string with MD5.
   * @returns the hashed string
   * @param str specifies the string to hash
   */
  MD5.init = function (str) {
    var temp;
    if (typeof str !== "string") {
      str = JSON.stringify(str);
    }
    this._pString = this._Utf8Encode(str);
    this._x = this._ConvertToWordArray(this._pString);
    this._a = 0x67452301;
    this._b = 0xefcdab89;
    this._c = 0x98badcfe;
    this._d = 0x10325476;
    for (this._k = 0; this._k < this._x.length; this._k += 16) {
      this._AA = this._a;
      this._BB = this._b;
      this._CC = this._c;
      this._DD = this._d;
      this._a = this._FF(this._a, this._b, this._c, this._d, this._x[this._k], this._S11, 0xd76aa478);
      this._d = this._FF(this._d, this._a, this._b, this._c, this._x[this._k + 1], this._S12, 0xe8c7b756);
      this._c = this._FF(this._c, this._d, this._a, this._b, this._x[this._k + 2], this._S13, 0x242070db);
      this._b = this._FF(this._b, this._c, this._d, this._a, this._x[this._k + 3], this._S14, 0xc1bdceee);
      this._a = this._FF(this._a, this._b, this._c, this._d, this._x[this._k + 4], this._S11, 0xf57c0faf);
      this._d = this._FF(this._d, this._a, this._b, this._c, this._x[this._k + 5], this._S12, 0x4787c62a);
      this._c = this._FF(this._c, this._d, this._a, this._b, this._x[this._k + 6], this._S13, 0xa8304613);
      this._b = this._FF(this._b, this._c, this._d, this._a, this._x[this._k + 7], this._S14, 0xfd469501);
      this._a = this._FF(this._a, this._b, this._c, this._d, this._x[this._k + 8], this._S11, 0x698098d8);
      this._d = this._FF(this._d, this._a, this._b, this._c, this._x[this._k + 9], this._S12, 0x8b44f7af);
      this._c = this._FF(this._c, this._d, this._a, this._b, this._x[this._k + 10], this._S13, 0xffff5bb1);
      this._b = this._FF(this._b, this._c, this._d, this._a, this._x[this._k + 11], this._S14, 0x895cd7be);
      this._a = this._FF(this._a, this._b, this._c, this._d, this._x[this._k + 12], this._S11, 0x6b901122);
      this._d = this._FF(this._d, this._a, this._b, this._c, this._x[this._k + 13], this._S12, 0xfd987193);
      this._c = this._FF(this._c, this._d, this._a, this._b, this._x[this._k + 14], this._S13, 0xa679438e);
      this._b = this._FF(this._b, this._c, this._d, this._a, this._x[this._k + 15], this._S14, 0x49b40821);
      this._a = this._GG(this._a, this._b, this._c, this._d, this._x[this._k + 1], this._S21, 0xf61e2562);
      this._d = this._GG(this._d, this._a, this._b, this._c, this._x[this._k + 6], this._S22, 0xc040b340);
      this._c = this._GG(this._c, this._d, this._a, this._b, this._x[this._k + 11], this._S23, 0x265e5a51);
      this._b = this._GG(this._b, this._c, this._d, this._a, this._x[this._k], this._S24, 0xe9b6c7aa);
      this._a = this._GG(this._a, this._b, this._c, this._d, this._x[this._k + 5], this._S21, 0xd62f105d);
      this._d = this._GG(this._d, this._a, this._b, this._c, this._x[this._k + 10], this._S22, 0x2441453);
      this._c = this._GG(this._c, this._d, this._a, this._b, this._x[this._k + 15], this._S23, 0xd8a1e681);
      this._b = this._GG(this._b, this._c, this._d, this._a, this._x[this._k + 4], this._S24, 0xe7d3fbc8);
      this._a = this._GG(this._a, this._b, this._c, this._d, this._x[this._k + 9], this._S21, 0x21e1cde6);
      this._d = this._GG(this._d, this._a, this._b, this._c, this._x[this._k + 14], this._S22, 0xc33707d6);
      this._c = this._GG(this._c, this._d, this._a, this._b, this._x[this._k + 3], this._S23, 0xf4d50d87);
      this._b = this._GG(this._b, this._c, this._d, this._a, this._x[this._k + 8], this._S24, 0x455a14ed);
      this._a = this._GG(this._a, this._b, this._c, this._d, this._x[this._k + 13], this._S21, 0xa9e3e905);
      this._d = this._GG(this._d, this._a, this._b, this._c, this._x[this._k + 2], this._S22, 0xfcefa3f8);
      this._c = this._GG(this._c, this._d, this._a, this._b, this._x[this._k + 7], this._S23, 0x676f02d9);
      this._b = this._GG(this._b, this._c, this._d, this._a, this._x[this._k + 12], this._S24, 0x8d2a4c8a);
      this._a = this._HH(this._a, this._b, this._c, this._d, this._x[this._k + 5], this._S31, 0xfffa3942);
      this._d = this._HH(this._d, this._a, this._b, this._c, this._x[this._k + 8], this._S32, 0x8771f681);
      this._c = this._HH(this._c, this._d, this._a, this._b, this._x[this._k + 11], this._S33, 0x6d9d6122);
      this._b = this._HH(this._b, this._c, this._d, this._a, this._x[this._k + 14], this._S34, 0xfde5380c);
      this._a = this._HH(this._a, this._b, this._c, this._d, this._x[this._k + 1], this._S31, 0xa4beea44);
      this._d = this._HH(this._d, this._a, this._b, this._c, this._x[this._k + 4], this._S32, 0x4bdecfa9);
      this._c = this._HH(this._c, this._d, this._a, this._b, this._x[this._k + 7], this._S33, 0xf6bb4b60);
      this._b = this._HH(this._b, this._c, this._d, this._a, this._x[this._k + 10], this._S34, 0xbebfbc70);
      this._a = this._HH(this._a, this._b, this._c, this._d, this._x[this._k + 13], this._S31, 0x289b7ec6);
      this._d = this._HH(this._d, this._a, this._b, this._c, this._x[this._k], this._S32, 0xeaa127fa);
      this._c = this._HH(this._c, this._d, this._a, this._b, this._x[this._k + 3], this._S33, 0xd4ef3085);
      this._b = this._HH(this._b, this._c, this._d, this._a, this._x[this._k + 6], this._S34, 0x4881d05);
      this._a = this._HH(this._a, this._b, this._c, this._d, this._x[this._k + 9], this._S31, 0xd9d4d039);
      this._d = this._HH(this._d, this._a, this._b, this._c, this._x[this._k + 12], this._S32, 0xe6db99e5);
      this._c = this._HH(this._c, this._d, this._a, this._b, this._x[this._k + 15], this._S33, 0x1fa27cf8);
      this._b = this._HH(this._b, this._c, this._d, this._a, this._x[this._k + 2], this._S34, 0xc4ac5665);
      this._a = this._II(this._a, this._b, this._c, this._d, this._x[this._k], this._S41, 0xf4292244);
      this._d = this._II(this._d, this._a, this._b, this._c, this._x[this._k + 7], this._S42, 0x432aff97);
      this._c = this._II(this._c, this._d, this._a, this._b, this._x[this._k + 14], this._S43, 0xab9423a7);
      this._b = this._II(this._b, this._c, this._d, this._a, this._x[this._k + 5], this._S44, 0xfc93a039);
      this._a = this._II(this._a, this._b, this._c, this._d, this._x[this._k + 12], this._S41, 0x655b59c3);
      this._d = this._II(this._d, this._a, this._b, this._c, this._x[this._k + 3], this._S42, 0x8f0ccc92);
      this._c = this._II(this._c, this._d, this._a, this._b, this._x[this._k + 10], this._S43, 0xffeff47d);
      this._b = this._II(this._b, this._c, this._d, this._a, this._x[this._k + 1], this._S44, 0x85845dd1);
      this._a = this._II(this._a, this._b, this._c, this._d, this._x[this._k + 8], this._S41, 0x6fa87e4f);
      this._d = this._II(this._d, this._a, this._b, this._c, this._x[this._k + 15], this._S42, 0xfe2ce6e0);
      this._c = this._II(this._c, this._d, this._a, this._b, this._x[this._k + 6], this._S43, 0xa3014314);
      this._b = this._II(this._b, this._c, this._d, this._a, this._x[this._k + 13], this._S44, 0x4e0811a1);
      this._a = this._II(this._a, this._b, this._c, this._d, this._x[this._k + 4], this._S41, 0xf7537e82);
      this._d = this._II(this._d, this._a, this._b, this._c, this._x[this._k + 11], this._S42, 0xbd3af235);
      this._c = this._II(this._c, this._d, this._a, this._b, this._x[this._k + 2], this._S43, 0x2ad7d2bb);
      this._b = this._II(this._b, this._c, this._d, this._a, this._x[this._k + 9], this._S44, 0xeb86d391);
      this._a = this._AddUnsigned(this._a, this._AA);
      this._b = this._AddUnsigned(this._b, this._BB);
      this._c = this._AddUnsigned(this._c, this._CC);
      this._d = this._AddUnsigned(this._d, this._DD);
    }
    temp = this._WordToHex(this._a) + this._WordToHex(this._b) + this._WordToHex(this._c) + this._WordToHex(this._d);
    return temp.toLowerCase();
  };
  MD5._RotateLeft = function (lValue, iShiftBits) {
    return lValue << iShiftBits | lValue >>> 32 - iShiftBits;
  };
  MD5._AddUnsigned = function (lX, lY) {
    var lX4;
    var lY4;
    var lX8;
    var lY8;
    var lResult;
    lX8 = lX & 0x80000000;
    lY8 = lY & 0x80000000;
    lX4 = lX & 0x40000000;
    lY4 = lY & 0x40000000;
    lResult = (lX & 0x3fffffff) + (lY & 0x3fffffff);
    if (lX4 & lY4) {
      return lResult ^ 0x80000000 ^ lX8 ^ lY8;
    }
    if (lX4 | lY4) {
      if (lResult & 0x40000000) {
        return lResult ^ 0xc0000000 ^ lX8 ^ lY8;
      } else {
        return lResult ^ 0x40000000 ^ lX8 ^ lY8;
      }
    } else {
      return lResult ^ lX8 ^ lY8;
    }
  };
  MD5._F = function (x, y, z) {
    return x & y | ~x & z;
  };
  MD5._G = function (x, y, z) {
    return x & z | y & ~z;
  };
  MD5._H = function (x, y, z) {
    return x ^ y ^ z;
  };
  MD5._I = function (x, y, z) {
    return y ^ (x | ~z);
  };
  MD5._FF = function (a, b, c, d, x, s, ac) {
    a = this._AddUnsigned(a, this._AddUnsigned(this._AddUnsigned(this._F(b, c, d), x), ac));
    return this._AddUnsigned(this._RotateLeft(a, s), b);
  };
  MD5._GG = function (a, b, c, d, x, s, ac) {
    a = this._AddUnsigned(a, this._AddUnsigned(this._AddUnsigned(this._G(b, c, d), x), ac));
    return this._AddUnsigned(this._RotateLeft(a, s), b);
  };
  MD5._HH = function (a, b, c, d, x, s, ac) {
    a = this._AddUnsigned(a, this._AddUnsigned(this._AddUnsigned(this._H(b, c, d), x), ac));
    return this._AddUnsigned(this._RotateLeft(a, s), b);
  };
  MD5._II = function (a, b, c, d, x, s, ac) {
    a = this._AddUnsigned(a, this._AddUnsigned(this._AddUnsigned(this._I(b, c, d), x), ac));
    return this._AddUnsigned(this._RotateLeft(a, s), b);
  };
  MD5._ConvertToWordArray = function (str) {
    var lWordCount;
    var lMessageLength = str.length;
    var lNumberOfWordsTemp1 = lMessageLength + 8;
    var lNumberOfWordsTemp2 = (lNumberOfWordsTemp1 - lNumberOfWordsTemp1 % 64) / 64;
    var lNumberOfWords = (lNumberOfWordsTemp2 + 1) * 16;
    var lWordArray = Array(lNumberOfWords - 1);
    var lBytePosition = 0;
    var lByteCount = 0;
    while (lByteCount < lMessageLength) {
      lWordCount = (lByteCount - lByteCount % 4) / 4;
      lBytePosition = lByteCount % 4 * 8;
      lWordArray[lWordCount] = lWordArray[lWordCount] | str.charCodeAt(lByteCount) << lBytePosition;
      lByteCount++;
    }
    lWordCount = (lByteCount - lByteCount % 4) / 4;
    lBytePosition = lByteCount % 4 * 8;
    lWordArray[lWordCount] = lWordArray[lWordCount] | 0x80 << lBytePosition;
    lWordArray[lNumberOfWords - 2] = lMessageLength << 3;
    lWordArray[lNumberOfWords - 1] = lMessageLength >>> 29;
    return lWordArray;
  };
  MD5._WordToHex = function (lValue) {
    var wordToHexValue = "";
    var wordToHexValueTemp = "";
    var lByte;
    var lCount;
    for (lCount = 0; lCount <= 3; lCount++) {
      lByte = lValue >>> lCount * 8 & 255;
      wordToHexValueTemp = "0".concat(lByte.toString(16));
      wordToHexValue = wordToHexValue + wordToHexValueTemp.substr(wordToHexValueTemp.length - 2, 2);
    }
    return wordToHexValue;
  };
  MD5._Utf8Encode = function (str) {
    var utftext = "";
    var c;
    str = str.replace(/\r\n/g, "\n");
    for (var n = 0; n < str.length; n++) {
      c = str.charCodeAt(n);
      if (c < 128) {
        utftext += String.fromCharCode(c);
      } else if (c > 127 && c < 2048) {
        utftext += String.fromCharCode(c >> 6 | 192);
        utftext += String.fromCharCode(c & 63 | 128);
      } else {
        utftext += String.fromCharCode(c >> 12 | 224);
        utftext += String.fromCharCode(c >> 6 & 63 | 128);
        utftext += String.fromCharCode(c & 63 | 128);
      }
    }
    return utftext;
  };
  MD5._x = [];
  MD5._S11 = 7;
  MD5._S12 = 12;
  MD5._S13 = 17;
  MD5._S14 = 22;
  MD5._S21 = 5;
  MD5._S22 = 9;
  MD5._S23 = 14;
  MD5._S24 = 20;
  MD5._S31 = 4;
  MD5._S32 = 11;
  MD5._S33 = 16;
  MD5._S34 = 23;
  MD5._S41 = 6;
  MD5._S42 = 10;
  MD5._S43 = 15;
  MD5._S44 = 21;
  return MD5;
}();
exports.MD5 = MD5;

/***/ }),
/* 212 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(38);
__webpack_require__(39);
__webpack_require__(40);
__webpack_require__(29);
__webpack_require__(41);
__webpack_require__(42);
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.deterministicStringify = deterministicStringify;
__webpack_require__(95);
__webpack_require__(213);
__webpack_require__(10);
__webpack_require__(78);
__webpack_require__(30);
__webpack_require__(214);
__webpack_require__(19);
__webpack_require__(219);
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
/**
 * Function from https://github.com/substack/json-stable-stringify
 * @param obj
 * @param opts
 */
function deterministicStringify(obj, opts) {
  if (opts === void 0) {
    opts = {};
  }
  if (typeof opts === "function") {
    opts = {
      cmp: opts
    };
  }
  var space = opts.space || "";
  if (typeof space === "number") {
    space = Array(space + 1).join(" ");
  }
  var cycles = typeof opts.cycles === "boolean" ? opts.cycles : false;
  var replacer = opts.replacer || function (key, value) {
    return value;
  };
  var cmp = opts.cmp && function (f) {
    return function (node) {
      return function (a, b) {
        var aobj = {
          key: a,
          value: node[a]
        };
        var bobj = {
          key: b,
          value: node[b]
        };
        return f(aobj, bobj);
      };
    };
  }(opts.cmp);
  var seen = [];
  return function stringify(parent, key, node, level) {
    var indent = space ? "\n".concat(new Array(level + 1).join(space)) : ";";
    var colonSeparator = space ? ": " : ":";
    if (node && node.toJSON && typeof node.toJSON === "function") {
      node = node.toJSON();
    }
    node = replacer.call(parent, key, node);
    if (node === undefined) {
      return;
    }
    if (_typeof(node) !== "object" || node === null) {
      return JSON.stringify(node);
    }
    if (Array.isArray(node)) {
      var out = [];
      for (var i = 0; i < node.length; i++) {
        var item = stringify(node, i.toString(), node[i], level + 1) || JSON.stringify(null);
        out.push(indent + space + item);
      }
      return "[".concat(out.join(",")).concat(indent, "]");
    } else {
      if (seen.indexOf(node) !== -1) {
        if (cycles) {
          return JSON.stringify("__cycle__");
        }
        throw new TypeError("Converting circular structure to JSON");
      } else {
        seen.push(node);
      }
      var keys = Object.keys(node).sort(cmp && cmp(node));
      var out = [];
      for (var _i = 0, keys_1 = keys; _i < keys_1.length; _i++) {
        var eachKey = keys_1[_i];
        var value = stringify(node, eachKey, node[eachKey], level + 1);
        if (!value) {
          continue;
        }
        var keyValue = JSON.stringify(eachKey) + colonSeparator + value;
        out.push(indent + space + keyValue);
      }
      seen.splice(seen.indexOf(node), 1);
      return "{".concat(out.join(",")).concat(indent, "}");
    }
  }({
    "": obj
  }, "", obj, 0);
}

/***/ }),
/* 213 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var call = __webpack_require__(6);

// `URL.prototype.toJSON` method
// https://url.spec.whatwg.org/#dom-url-tojson
$({ target: 'URL', proto: true, enumerable: true }, {
  toJSON: function toJSON() {
    return call(URL.prototype.toString, this);
  }
});


/***/ }),
/* 214 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var uncurryThis = __webpack_require__(0);
var aCallable = __webpack_require__(18);
var toObject = __webpack_require__(15);
var lengthOfArrayLike = __webpack_require__(23);
var deletePropertyOrThrow = __webpack_require__(137);
var toString = __webpack_require__(11);
var fails = __webpack_require__(1);
var internalSort = __webpack_require__(215);
var arrayMethodIsStrict = __webpack_require__(87);
var FF = __webpack_require__(216);
var IE_OR_EDGE = __webpack_require__(217);
var V8 = __webpack_require__(45);
var WEBKIT = __webpack_require__(218);

var test = [];
var nativeSort = uncurryThis(test.sort);
var push = uncurryThis(test.push);

// IE8-
var FAILS_ON_UNDEFINED = fails(function () {
  test.sort(undefined);
});
// V8 bug
var FAILS_ON_NULL = fails(function () {
  test.sort(null);
});
// Old WebKit
var STRICT_METHOD = arrayMethodIsStrict('sort');

var STABLE_SORT = !fails(function () {
  // feature detection can be too slow, so check engines versions
  if (V8) return V8 < 70;
  if (FF && FF > 3) return;
  if (IE_OR_EDGE) return true;
  if (WEBKIT) return WEBKIT < 603;

  var result = '';
  var code, chr, value, index;

  // generate an array with more 512 elements (Chakra and old V8 fails only in this case)
  for (code = 65; code < 76; code++) {
    chr = String.fromCharCode(code);

    switch (code) {
      case 66: case 69: case 70: case 72: value = 3; break;
      case 68: case 71: value = 4; break;
      default: value = 2;
    }

    for (index = 0; index < 47; index++) {
      test.push({ k: chr + index, v: value });
    }
  }

  test.sort(function (a, b) { return b.v - a.v; });

  for (index = 0; index < test.length; index++) {
    chr = test[index].k.charAt(0);
    if (result.charAt(result.length - 1) !== chr) result += chr;
  }

  return result !== 'DGBEFHACIJK';
});

var FORCED = FAILS_ON_UNDEFINED || !FAILS_ON_NULL || !STRICT_METHOD || !STABLE_SORT;

var getSortCompare = function (comparefn) {
  return function (x, y) {
    if (y === undefined) return -1;
    if (x === undefined) return 1;
    if (comparefn !== undefined) return +comparefn(x, y) || 0;
    return toString(x) > toString(y) ? 1 : -1;
  };
};

// `Array.prototype.sort` method
// https://tc39.es/ecma262/#sec-array.prototype.sort
$({ target: 'Array', proto: true, forced: FORCED }, {
  sort: function sort(comparefn) {
    if (comparefn !== undefined) aCallable(comparefn);

    var array = toObject(this);

    if (STABLE_SORT) return comparefn === undefined ? nativeSort(array) : nativeSort(array, comparefn);

    var items = [];
    var arrayLength = lengthOfArrayLike(array);
    var itemsLength, index;

    for (index = 0; index < arrayLength; index++) {
      if (index in array) push(items, array[index]);
    }

    internalSort(items, getSortCompare(comparefn));

    itemsLength = lengthOfArrayLike(items);
    index = 0;

    while (index < itemsLength) array[index] = items[index++];
    while (index < arrayLength) deletePropertyOrThrow(array, index++);

    return array;
  }
});


/***/ }),
/* 215 */
/***/ (function(module, exports, __webpack_require__) {

var arraySlice = __webpack_require__(118);

var floor = Math.floor;

var mergeSort = function (array, comparefn) {
  var length = array.length;
  var middle = floor(length / 2);
  return length < 8 ? insertionSort(array, comparefn) : merge(
    array,
    mergeSort(arraySlice(array, 0, middle), comparefn),
    mergeSort(arraySlice(array, middle), comparefn),
    comparefn
  );
};

var insertionSort = function (array, comparefn) {
  var length = array.length;
  var i = 1;
  var element, j;

  while (i < length) {
    j = i;
    element = array[i];
    while (j && comparefn(array[j - 1], element) > 0) {
      array[j] = array[--j];
    }
    if (j !== i++) array[j] = element;
  } return array;
};

var merge = function (array, left, right, comparefn) {
  var llength = left.length;
  var rlength = right.length;
  var lindex = 0;
  var rindex = 0;

  while (lindex < llength || rindex < rlength) {
    array[lindex + rindex] = (lindex < llength && rindex < rlength)
      ? comparefn(left[lindex], right[rindex]) <= 0 ? left[lindex++] : right[rindex++]
      : lindex < llength ? left[lindex++] : right[rindex++];
  } return array;
};

module.exports = mergeSort;


/***/ }),
/* 216 */
/***/ (function(module, exports, __webpack_require__) {

var userAgent = __webpack_require__(25);

var firefox = userAgent.match(/firefox\/(\d+)/i);

module.exports = !!firefox && +firefox[1];


/***/ }),
/* 217 */
/***/ (function(module, exports, __webpack_require__) {

var UA = __webpack_require__(25);

module.exports = /MSIE|Trident/.test(UA);


/***/ }),
/* 218 */
/***/ (function(module, exports, __webpack_require__) {

var userAgent = __webpack_require__(25);

var webkit = userAgent.match(/AppleWebKit\/(\d+)\./);

module.exports = !!webkit && +webkit[1];


/***/ }),
/* 219 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var toObject = __webpack_require__(15);
var toAbsoluteIndex = __webpack_require__(69);
var toIntegerOrInfinity = __webpack_require__(28);
var lengthOfArrayLike = __webpack_require__(23);
var setArrayLength = __webpack_require__(220);
var doesNotExceedSafeInteger = __webpack_require__(135);
var arraySpeciesCreate = __webpack_require__(85);
var createProperty = __webpack_require__(76);
var deletePropertyOrThrow = __webpack_require__(137);
var arrayMethodHasSpeciesSupport = __webpack_require__(58);

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('splice');

var max = Math.max;
var min = Math.min;

// `Array.prototype.splice` method
// https://tc39.es/ecma262/#sec-array.prototype.splice
// with adding support of @@species
$({ target: 'Array', proto: true, forced: !HAS_SPECIES_SUPPORT }, {
  splice: function splice(start, deleteCount /* , ...items */) {
    var O = toObject(this);
    var len = lengthOfArrayLike(O);
    var actualStart = toAbsoluteIndex(start, len);
    var argumentsLength = arguments.length;
    var insertCount, actualDeleteCount, A, k, from, to;
    if (argumentsLength === 0) {
      insertCount = actualDeleteCount = 0;
    } else if (argumentsLength === 1) {
      insertCount = 0;
      actualDeleteCount = len - actualStart;
    } else {
      insertCount = argumentsLength - 2;
      actualDeleteCount = min(max(toIntegerOrInfinity(deleteCount), 0), len - actualStart);
    }
    doesNotExceedSafeInteger(len + insertCount - actualDeleteCount);
    A = arraySpeciesCreate(O, actualDeleteCount);
    for (k = 0; k < actualDeleteCount; k++) {
      from = actualStart + k;
      if (from in O) createProperty(A, k, O[from]);
    }
    A.length = actualDeleteCount;
    if (insertCount < actualDeleteCount) {
      for (k = actualStart; k < len - actualDeleteCount; k++) {
        from = k + actualDeleteCount;
        to = k + insertCount;
        if (from in O) O[to] = O[from];
        else deletePropertyOrThrow(O, to);
      }
      for (k = len; k > len - actualDeleteCount + insertCount; k--) deletePropertyOrThrow(O, k - 1);
    } else if (insertCount > actualDeleteCount) {
      for (k = len - actualDeleteCount; k > actualStart; k--) {
        from = k + actualDeleteCount - 1;
        to = k + insertCount - 1;
        if (from in O) O[to] = O[from];
        else deletePropertyOrThrow(O, to);
      }
    }
    for (k = 0; k < insertCount; k++) {
      O[k + actualStart] = arguments[k + 2];
    }
    setArrayLength(O, len - actualDeleteCount + insertCount);
    return A;
  }
});


/***/ }),
/* 220 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var DESCRIPTORS = __webpack_require__(8);
var isArray = __webpack_require__(50);

var $TypeError = TypeError;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

// Safari < 13 does not throw an error in this case
var SILENT_ON_NON_WRITABLE_LENGTH_SET = DESCRIPTORS && !function () {
  // makes no sense without proper strict mode support
  if (this !== undefined) return true;
  try {
    // eslint-disable-next-line es/no-object-defineproperty -- safe
    Object.defineProperty([], 'length', { writable: false }).length = 1;
  } catch (error) {
    return error instanceof TypeError;
  }
}();

module.exports = SILENT_ON_NON_WRITABLE_LENGTH_SET ? function (O, length) {
  if (isArray(O) && !getOwnPropertyDescriptor(O, 'length').writable) {
    throw $TypeError('Cannot set read only .length');
  } return O.length = length;
} : function (O, length) {
  return O.length = length;
};


/***/ }),
/* 221 */
/***/ (function(module, exports) {

module.exports = require("i18next");

/***/ }),
/* 222 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.State = exports.EStateEvents = void 0;
__webpack_require__(10);
__webpack_require__(37);
__webpack_require__(38);
__webpack_require__(39);
__webpack_require__(40);
__webpack_require__(29);
__webpack_require__(41);
__webpack_require__(42);
__webpack_require__(19);
__webpack_require__(223);
__webpack_require__(96);
__webpack_require__(30);
var _tracking = __webpack_require__(143);
var __awaiter = void 0 && (void 0).__awaiter || function (thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function (resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function (resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator = void 0 && (void 0).__generator || function (thisArg, body) {
  var _ = {
      label: 0,
      sent: function sent() {
        if (t[0] & 1) throw t[1];
        return t[1];
      },
      trys: [],
      ops: []
    },
    f,
    y,
    t,
    g;
  return g = {
    next: verb(0),
    "throw": verb(1),
    "return": verb(2)
  }, typeof Symbol === "function" && (g[Symbol.iterator] = function () {
    return this;
  }), g;
  function verb(n) {
    return function (v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return {
            value: op[1],
            done: false
          };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return {
      value: op[0] ? op[1] : void 0,
      done: true
    };
  }
};
var EStateEvents;
exports.EStateEvents = EStateEvents;
(function (EStateEvents) {
  EStateEvents["Loaded"] = "stateloaded";
  EStateEvents["Change"] = "statechange";
  EStateEvents["Changed"] = "statechanged";
})(EStateEvents || (exports.EStateEvents = EStateEvents = {}));
var State = /** @class */function () {
  /**
   * Constructor
   * @param states: the states instance
   */
  function State(states) {
    this._previous = null;
    this._currentView = null;
    this._attrs = {};
    this._busyOn = null;
    this.states = states;
    this._keys = Object.keys(states);
    this._current = this._keys[0];
  }
  Object.defineProperty(State.prototype, "eventEmitter", {
    /**
     * Get event emitter for state event notification.
     */
    get: function get() {
      return this._eventEmitter;
    },
    /**
     * Set event emitter for state event notification.
     * @param eventEmitter The event emitter.
     */
    set: function set(eventEmitter) {
      this._eventEmitter = eventEmitter;
    },
    enumerable: false,
    configurable: true
  });
  /**
   * Loads the views calling .onLoad
   * @param view: specifies the first view to make appear
   */
  State.prototype.load = function (view) {
    return __awaiter(this, void 0, void 0, function () {
      var _this = this;
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            return [4 /*yield*/, Promise.all(this._keys.map(function (k) {
              var viewInstance = _this.states[k].view;
              return _this._executeViewLifecycleHook(viewInstance.onLoad, viewInstance);
            }))];
          case 1:
            _a.sent();
            if (this.onLoaded) {
              this.onLoaded();
            }
            if (this._eventEmitter) {
              this._eventEmitter.emit(EStateEvents.Loaded);
            }
            return [4 /*yield*/, this.to(view || this._current)];
          case 2:
            _a.sent();
            return [2 /*return*/];
        }
      });
    });
  };
  /**
   * Returns the current view name
   */
  State.prototype.current = function () {
    return this._current;
  };
  /**
   * Returns the current view instance
   */
  State.prototype.currentView = function () {
    return this.states[this._current].view;
  };
  /**
   * Returns the previous view instance
   * Note: returns null if no transition was done
   */
  State.prototype.previousView = function () {
    return this.states[this._previous] ? this.states[this._previous].view : null;
  };
  Object.defineProperty(State.prototype, "busy", {
    /**
     * Returns true if state is currently changing view (waiting for onWillAppear/onDidAppear)
     * @returns boolean
     */
    get: function get() {
      return this._busyOn !== null;
    },
    enumerable: false,
    configurable: true
  });
  /**
   * Switches to the given view
   * @param view: the given view name
   */
  State.prototype.to = function (view, next) {
    return __awaiter(this, void 0, void 0, function () {
      var stepIndice;
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            stepIndice = this._keys.indexOf(view);
            if (stepIndice === -1) {
              throw new Error("View '".concat(view, "' does not exists"));
            }
            this._busyOn = view;
            if (next) {
              this.states[view].next = next;
            }
            if (this.onStateChange) {
              this.onStateChange(view);
            }
            if (this._eventEmitter) {
              this._eventEmitter.emit(EStateEvents.Change, view);
            }
            return [4 /*yield*/, this._switchCurrentView(this.states[view].view, view)];
          case 1:
            _a.sent();
            if (this._busyOn === view) {
              this._busyOn = null;
            }
            return [2 /*return*/];
        }
      });
    });
  };
  /**
   * Switches to the next view
   */
  State.prototype.next = function () {
    return __awaiter(this, void 0, void 0, function () {
      var currentState, currentIndice, key;
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            currentState = this.states[this._current];
            if (!currentState.next) return [3 /*break*/, 2];
            return [4 /*yield*/, this.to(currentState.next)];
          case 1:
            _a.sent();
            return [2 /*return*/, currentState.next];
          case 2:
            currentIndice = this._keys.indexOf(this._current);
            if (currentIndice >= this._keys.length - 1) {
              currentIndice = 0;
            } else {
              currentIndice++;
            }
            key = this._keys[currentIndice];
            return [4 /*yield*/, this.to(key)];
          case 3:
            _a.sent();
            return [2 /*return*/, key];
        }
      });
    });
  };
  /**
   * Executes onWillAppear and onDidAppear of the given view
   * @param view the view to make appear
   */
  State.prototype.executeAppear = function (view, done) {
    return __awaiter(this, void 0, void 0, function () {
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            return [4 /*yield*/, this._executeCallback("onWillAppear", view)];
          case 1:
            _a.sent();
            return [4 /*yield*/, this._executeCallback("onDidAppear", view)];
          case 2:
            _a.sent();
            if (done) {
              done();
            }
            return [2 /*return*/];
        }
      });
    });
  };
  /**
   * Executes onWillDisappear and onDidDisappear of the given view
   * @param view the view to make disappear
   */
  State.prototype.executeDisappear = function (view, done) {
    return __awaiter(this, void 0, void 0, function () {
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            return [4 /*yield*/, this._executeCallback("onWillDisappear", view)];
          case 1:
            _a.sent();
            return [4 /*yield*/, this._executeCallback("onDidDisappear", view)];
          case 2:
            _a.sent();
            if (done) {
              done();
            }
            return [2 /*return*/];
        }
      });
    });
  };
  /**
   * Get the value of the given key in attrs
   * @param key the key to pick in attrs
   * @param value the value to set
   */
  State.prototype.attrs = function (key, value) {
    if (key && value !== null && value !== undefined) {
      this._attrs[key] = value;
      return this._attrs[key];
    } else if (key !== null && key !== undefined) {
      return this._attrs[key];
    }
    return this._attrs;
  };
  /**
   * Returns the states keys
   */
  State.prototype.stateKeys = function () {
    return this._keys.slice(0, this._keys.length);
  };
  /**
   * Return the view associated to the given state
   * @param key the state's key
   */
  State.prototype.getViewFromState = function (key) {
    for (var _i = 0, _a = this._keys; _i < _a.length; _i++) {
      var k = _a[_i];
      if (k === key) {
        return this.states[k].view;
      }
    }
    return null;
  };
  /**
   * Return the state key associated to the given view
   * @param view the view associated to a state
   */
  State.prototype.getStateFromView = function (view) {
    for (var _i = 0, _a = this._keys; _i < _a.length; _i++) {
      var k = _a[_i];
      if (this.states[k].view === view) {
        return k;
      }
    }
    return null;
  };
  // Switches the the new view calling the lifecycle old -> new
  State.prototype._switchCurrentView = function (to, key) {
    return __awaiter(this, void 0, void 0, function () {
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            if (!!this._currentView) return [3 /*break*/, 2];
            return [4 /*yield*/, this._drawNewView(to, key)];
          case 1:
            return [2 /*return*/, _a.sent()];
          case 2:
            // Close current view
            return [4 /*yield*/, this.executeDisappear(this._currentView)];
          case 3:
            // Close current view
            _a.sent();
            return [4 /*yield*/, this._drawNewView(to, key)];
          case 4:
            _a.sent();
            return [2 /*return*/];
        }
      });
    });
  };
  // Calls the lifecycle of the new view to switch on
  State.prototype._drawNewView = function (view, key) {
    return __awaiter(this, void 0, void 0, function () {
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            if (this._currentView) {
              this._previous = this._current;
            }
            this._current = key;
            this._currentView = view;
            return [4 /*yield*/, this.executeAppear(view)];
          case 1:
            _a.sent();
            if (this.onStateChanged) {
              this.onStateChanged(key);
            }
            if (this._eventEmitter) {
              this._eventEmitter.emit(EStateEvents.Changed, key);
            }
            return [2 /*return*/];
        }
      });
    });
  };
  // Executes the lifecycle synchronously or asynchronously
  State.prototype._executeCallback = function (callbackName, context) {
    var callback = context[callbackName];
    this._trackCall(callbackName); // track the state even if the view has not an overridden function
    if (!callback) {
      return Promise.resolve();
    }
    return this._executeViewLifecycleHook(callback, context);
  };
  /** Track an event if the callback match a state change */
  State.prototype._trackCall = function (callbackName) {
    var eventName = "".concat(callbackName.charAt(2).toLowerCase()).concat(callbackName.substring(3, callbackName.length));
    for (var _i = 0, _a = Object.keys(_tracking.EViewState); _i < _a.length; _i++) {
      var event_1 = _a[_i];
      if (eventName === _tracking.EViewState[event_1]) {
        _tracking.tracker.track(new _tracking.ViewStateEvent(this._currentView.trackingName || this._current, eventName));
        return;
      }
    }
  };
  /**
   * Executes lifecycle hook synchronously or asynchronously
   * @param hook ViewLifecycleHook.
   * @param context context of the hook.
   * @returns a promise resolved when the hook is done.
   */
  State.prototype._executeViewLifecycleHook = function (hook, context) {
    var usesCallback = hook.length >= 1;
    if (usesCallback) {
      return new Promise(function (resolve) {
        return hook.call(context, resolve);
      });
    }
    var promise = hook.call(context);
    return promise instanceof Promise ? promise : Promise.resolve();
  };
  return State;
}();
exports.State = State;

/***/ }),
/* 223 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var $map = __webpack_require__(49).map;
var arrayMethodHasSpeciesSupport = __webpack_require__(58);

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('map');

// `Array.prototype.map` method
// https://tc39.es/ecma262/#sec-array.prototype.map
// with adding support of @@species
$({ target: 'Array', proto: true, forced: !HAS_SPECIES_SUPPORT }, {
  map: function map(callbackfn /* , thisArg */) {
    return $map(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
  }
});


/***/ }),
/* 224 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Tracker = void 0;
var _tools = __webpack_require__(225);
var _tools2 = __webpack_require__(57);
var _event = __webpack_require__(24);
var _game = __webpack_require__(144);
var _view = __webpack_require__(145);
// import Com from "@gdk/com";

/** Tracker object allows to send tracked info to com layer. */
var Tracker = /** @class */function () {
  function Tracker() {
    this._seqNum = 0;
  }
  /** To be called once, at game initialization */
  Tracker.prototype.init = function (_a) {
    var com = _a.com,
      logger = _a.logger;
    this._com = com;
    this._logger = logger;
    this._seqNum = 0;
    this._logger.debug("[TRACK]", "Tracking initialized");
    this._gameInfo = undefined;
    this._viewInfo = undefined;
    this._uuid = _tools2.Tools.uuidv4();
  };
  /**
   * track an event
   * @param event specifies event to track
   */
  Tracker.prototype.track = function (event) {
    switch (event.eventType) {
      case _event.EEventType.Game:
        this._gameEvent(event);
        break;
      case _event.EEventType.View:
        this._viewEvent(event);
        break;
      default:
        var data = this._getFullInfo(event);
        this._actualTrack(data);
        break;
    }
  };
  /** To be called when applet is loaded or quit */
  Tracker.prototype._gameEvent = function (gameEvent) {
    var data;
    if (gameEvent.event === _game.EGameEvent.Loaded) {
      this._gameInfo = (0, _tools.clone)(gameEvent.gameInfo);
      data = (0, _tools.clone)(gameEvent);
    } else {
      data = this._getFullInfo(gameEvent);
    }
    this._actualTrack(data);
  };
  /** To be called each time a view state change */
  Tracker.prototype._viewEvent = function (viewEvent) {
    if (viewEvent.event === _view.EViewEvent.StateChange) {
      this._viewInfo = (0, _tools.clone)(viewEvent.viewInfo);
    }
    var data = (0, _tools.clone)(viewEvent);
    data.gameInfo = this._gameInfo;
    this._actualTrack(data);
  };
  /** Actual tracking */
  Tracker.prototype._actualTrack = function (data) {
    if (this._com) {
      this._com.trigger("TAG", data);
    }
    this._seqNum++;
  };
  /* Add fields most events have. MUST NOT be used for Game and view events */
  Tracker.prototype._getFullInfo = function (event) {
    var data = (0, _tools.clone)(event);
    return (0, _tools.mergeFields)({
      gameInfo: this._gameInfo,
      viewInfo: this._viewInfo
    }, data);
  };
  return Tracker;
}();
exports.Tracker = Tracker;

/***/ }),
/* 225 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.clone = clone;
exports.mergeFields = mergeFields;
var _tools = __webpack_require__(57);
/** Returns a new object that is a merge of object and fields.
 *  if object already contains fields, they are overided.
 */
function mergeFields(fields, object) {
  var cloned = clone(object);
  return _tools.Tools.merge(fields, cloned);
}
/** Clone a given object */
function clone(object) {
  return _tools.Tools.merge(object, {});
}

/***/ }),
/* 226 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.LinkClick = exports.ClickEvent = exports.CLICK_EVENT = exports.ButtonClick = void 0;
__webpack_require__(141);
__webpack_require__(227);
__webpack_require__(44);
var _event = __webpack_require__(24);
var __extends = void 0 && (void 0).__extends || function () {
  var _extendStatics = function extendStatics(d, b) {
    _extendStatics = Object.setPrototypeOf || {
      __proto__: []
    } instanceof Array && function (d, b) {
      d.__proto__ = b;
    } || function (d, b) {
      for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
    };
    return _extendStatics(d, b);
  };
  return function (d, b) {
    if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    _extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var CLICK_EVENT;
exports.CLICK_EVENT = CLICK_EVENT;
(function (CLICK_EVENT) {
  CLICK_EVENT["click"] = "click";
  CLICK_EVENT["button"] = "buttonClick";
  CLICK_EVENT["link"] = "linkClick";
})(CLICK_EVENT || (exports.CLICK_EVENT = CLICK_EVENT = {}));
/** Generic click event */
var ClickEvent = /** @class */function () {
  function ClickEvent(name, event) {
    if (event === void 0) {
      event = CLICK_EVENT.click;
    }
    this.name = name;
    this.event = event;
    this.eventType = _event.EEventType.Click;
  }
  return ClickEvent;
}();
exports.ClickEvent = ClickEvent;
/** To Track click on a button */
var ButtonClick = /** @class */function (_super) {
  __extends(ButtonClick, _super);
  function ButtonClick(name) {
    var _this = _super.call(this, name, CLICK_EVENT.button) || this;
    _this.eventType = _event.EEventType.Click;
    return _this;
  }
  return ButtonClick;
}(ClickEvent);
exports.ButtonClick = ButtonClick;
/** To Track link on a button */
var LinkClick = /** @class */function (_super) {
  __extends(LinkClick, _super);
  function LinkClick(name, url) {
    var _this = _super.call(this, name, CLICK_EVENT.link) || this;
    _this.url = url;
    return _this;
  }
  return LinkClick;
}(ClickEvent);
exports.LinkClick = LinkClick;

/***/ }),
/* 227 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var createHTML = __webpack_require__(228);
var forcedStringHTMLMethod = __webpack_require__(229);

// `String.prototype.link` method
// https://tc39.es/ecma262/#sec-string.prototype.link
$({ target: 'String', proto: true, forced: forcedStringHTMLMethod('link') }, {
  link: function link(url) {
    return createHTML(this, 'a', 'href', url);
  }
});


/***/ }),
/* 228 */
/***/ (function(module, exports, __webpack_require__) {

var uncurryThis = __webpack_require__(0);
var requireObjectCoercible = __webpack_require__(21);
var toString = __webpack_require__(11);

var quot = /"/g;
var replace = uncurryThis(''.replace);

// `CreateHTML` abstract operation
// https://tc39.es/ecma262/#sec-createhtml
module.exports = function (string, tag, attribute, value) {
  var S = toString(requireObjectCoercible(string));
  var p1 = '<' + tag;
  if (attribute !== '') p1 += ' ' + attribute + '="' + replace(toString(value), quot, '&quot;') + '"';
  return p1 + '>' + S + '</' + tag + '>';
};


/***/ }),
/* 229 */
/***/ (function(module, exports, __webpack_require__) {

var fails = __webpack_require__(1);

// check the existence of a method, lowercase
// of a tag and escaping quotes in arguments
module.exports = function (METHOD_NAME) {
  return fails(function () {
    var test = ''[METHOD_NAME]('"');
    return test !== test.toLowerCase() || test.split('"').length > 3;
  });
};


/***/ }),
/* 230 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ErrorEvent = void 0;
var _event = __webpack_require__(24);
var ErrorEvent = /** @class */function () {
  function ErrorEvent(errorMessage) {
    this.errorMessage = errorMessage;
    this.eventType = _event.EEventType.Error;
    this.event = "error";
  }
  return ErrorEvent;
}();
exports.ErrorEvent = ErrorEvent;

/***/ }),
/* 231 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SlideEvent = exports.ESlideEvent = void 0;
__webpack_require__(44);
var _event = __webpack_require__(24);
var ESlideEvent;
exports.ESlideEvent = ESlideEvent;
(function (ESlideEvent) {
  ESlideEvent["Show"] = "show";
  ESlideEvent["Hide"] = "hide";
})(ESlideEvent || (exports.ESlideEvent = ESlideEvent = {}));
var SlideEvent = /** @class */function () {
  function SlideEvent(name, event) {
    this.name = name;
    this.event = event;
    this.eventType = _event.EEventType.Slide;
  }
  return SlideEvent;
}();
exports.SlideEvent = SlideEvent;

/***/ }),
/* 232 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SettingsEvent = exports.ESettingsEvent = void 0;
var _event = __webpack_require__(24);
var ESettingsEvent;
exports.ESettingsEvent = ESettingsEvent;
(function (ESettingsEvent) {
  ESettingsEvent["Show"] = "show";
  ESettingsEvent["Hide"] = "hide";
})(ESettingsEvent || (exports.ESettingsEvent = ESettingsEvent = {}));
var SettingsEvent = /** @class */function () {
  function SettingsEvent(event) {
    this.event = event;
    this.eventType = _event.EEventType.Settings;
  }
  return SettingsEvent;
}();
exports.SettingsEvent = SettingsEvent;

/***/ }),
/* 233 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EEngine = exports.EBetMode = void 0;
var EEngine;
exports.EEngine = EEngine;
(function (EEngine) {
  EEngine["ITG"] = "ITG";
  EEngine["MTR"] = "MTR";
  EEngine["MTP"] = "MTP";
})(EEngine || (exports.EEngine = EEngine = {}));
var EBetMode;
exports.EBetMode = EBetMode;
(function (EBetMode) {
  EBetMode["AUTO"] = "BET_AUTO_CLAIM";
  EBetMode["MANUAL"] = "BET_MANUAL_CLAIM";
})(EBetMode || (exports.EBetMode = EBetMode = {}));

/***/ }),
/* 234 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
Object.defineProperty(exports, "ELevel", {
  enumerable: true,
  get: function get() {
    return _constants.ELevel;
  }
});
Object.defineProperty(exports, "EOutput", {
  enumerable: true,
  get: function get() {
    return _constants.EOutput;
  }
});
Object.defineProperty(exports, "Logger", {
  enumerable: true,
  get: function get() {
    return _logger.Logger;
  }
});
var _constants = __webpack_require__(97);
var _logger = __webpack_require__(235);

/***/ }),
/* 235 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Logger = void 0;
__webpack_require__(96);
__webpack_require__(30);
__webpack_require__(236);
__webpack_require__(10);
var _constants = __webpack_require__(97);
var _consoleAdapter = __webpack_require__(237);
var __spreadArray = void 0 && (void 0).__spreadArray || function (to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
};
/**
 * Logs library for game:
 *
 * @see Logger.init and Logger.getLogger for creating/getting instance
 *
 * Example:
 * In Game initialisation:
 *
 *   import { Logger, LEVEL, OUTPUT, TAG } from "../src/index";
 *
 *   Logger.init(LEVEL.info, OUTPUT.console);
 *
 * Wherever logger is needed:
 *    const logger = Logger.getLogger();
 *
 *    logger.log(LEVEL.info, "message");
 *
 *    logger.debug("warn");
 *
 *    logger.info("hello");
 *    logger.info(TAG, {}); // add TAG info in {}
 *
 *    logger.info("warn");
 *
 *    logger.error("error");
 *    logger.error(TAG, {}); // add TAG error params in {}
 *
 */
var Logger = /** @class */function () {
  function Logger(level) {
    if (level === void 0) {
      level = _constants.ELevel.Info;
    }
    var outputs = [];
    for (var _i = 1; _i < arguments.length; _i++) {
      outputs[_i - 1] = arguments[_i];
    }
    this._adapters = [];
    if (!outputs.length) {
      outputs = [_constants.EOutput.Console];
    }
    for (var _a = 0, outputs_1 = outputs; _a < outputs_1.length; _a++) {
      var output = outputs_1[_a];
      switch (output) {
        case _constants.EOutput.Console:
          this.addLogger(new _consoleAdapter.ConsoleAdapter(level));
          break;
        default:
          throw new Error("Don't know this output ".concat(output));
      }
    }
  }
  /** Returns current logger (init must be called before)
   * To log a message:
   * logger.info('msg');
   * To lag a TAG:
   * logger.info(TAG, { TAG data })
   */
  Logger.getLogger = function () {
    return Logger._logger;
  };
  /** Must be called when soft start to initiate the logger
   * @param level: minimum level to log (default: LEVEL.info)
   * @param outputs: where log will be send
   */
  Logger.init = function (level) {
    if (level === void 0) {
      level = _constants.ELevel.Info;
    }
    var outputs = [];
    for (var _i = 1; _i < arguments.length; _i++) {
      outputs[_i - 1] = arguments[_i];
    }
    if (Logger._logger) {
      throw new Error("Logger is already instanciated.");
    }
    Logger._logger = new (Logger.bind.apply(Logger, __spreadArray([void 0, level], outputs, false)))();
    return Logger._logger;
  };
  /**
   * Add logger
   * @param logger specifies logger to add
   */
  Logger.prototype.addLogger = function (logger) {
    if (this._hasLogger(logger.constructor)) {
      return;
    }
    this._adapters.push(logger);
  };
  /**
   * Log
   * @param level specifies the level of log (debug/ingo/warn/error)
   * @param data specifies the data to log
   */
  Logger.prototype.log = function (level) {
    var data = [];
    for (var _i = 1; _i < arguments.length; _i++) {
      data[_i - 1] = arguments[_i];
    }
    for (var _a = 0, _b = this._adapters; _a < _b.length; _a++) {
      var adapter = _b[_a];
      adapter.write.apply(adapter, __spreadArray([level], data, false));
    }
  };
  /**
   * Alias for log(ELevel.Debug, ...data);
   * @param data specifies the data to log
   */
  Logger.prototype.debug = function () {
    var data = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      data[_i] = arguments[_i];
    }
    this.log.apply(this, __spreadArray([_constants.ELevel.Debug], data, false));
  };
  /**
   * Alias for log(ELevel.Info, ...data);
   * @param data specifies the data to log
   */
  Logger.prototype.info = function () {
    var data = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      data[_i] = arguments[_i];
    }
    this.log.apply(this, __spreadArray([_constants.ELevel.Info], data, false));
  };
  /**
   * Alias for log(ELevel.Warn, ...data);
   * @param data specifies the data to log
   */
  Logger.prototype.warn = function () {
    var data = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      data[_i] = arguments[_i];
    }
    this.log.apply(this, __spreadArray([_constants.ELevel.Warn], data, false));
  };
  /**
   * Alias for log(ELevel.Error, ...data);
   * @param data specifies the data to log
   */
  Logger.prototype.error = function () {
    var data = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      data[_i] = arguments[_i];
    }
    this.log.apply(this, __spreadArray([_constants.ELevel.Error], data, false));
  };
  Logger.prototype._hasLogger = function (type) {
    var filtered = this._adapters.filter(function (adapter) {
      return adapter instanceof type;
    });
    return filtered.length !== 0;
  };
  Logger.LEVEL = _constants.ELevel;
  Logger.OUTPUT = _constants.EOutput;
  return Logger;
}();
exports.Logger = Logger;

/***/ }),
/* 236 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var $filter = __webpack_require__(49).filter;
var arrayMethodHasSpeciesSupport = __webpack_require__(58);

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('filter');

// `Array.prototype.filter` method
// https://tc39.es/ecma262/#sec-array.prototype.filter
// with adding support of @@species
$({ target: 'Array', proto: true, forced: !HAS_SPECIES_SUPPORT }, {
  filter: function filter(callbackfn /* , thisArg */) {
    return $filter(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
  }
});


/***/ }),
/* 237 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ConsoleAdapter = void 0;
var _constants = __webpack_require__(97);
var ConsoleAdapter = /** @class */function () {
  function ConsoleAdapter(_level) {
    this._level = _level;
  }
  /**
   * Log
   * @param level specifies the level of log (debug/ingo/warn/error)
   * @param data specifies the data to log
   */
  ConsoleAdapter.prototype.write = function (level) {
    var data = [];
    for (var _i = 1; _i < arguments.length; _i++) {
      data[_i - 1] = arguments[_i];
    }
    var func = this._getFunc(level);
    if (!func) {
      return;
    }
    func.apply(console, data);
  };
  ConsoleAdapter.prototype._getFunc = function (level) {
    if (level < this._level) {
      return undefined;
    }
    switch (level) {
      case _constants.ELevel.Debug:
        return console.log;
      case _constants.ELevel.Info:
        return console.info;
      case _constants.ELevel.Warn:
        return console.warn;
      case _constants.ELevel.Error:
        return console.error;
    }
  };
  return ConsoleAdapter;
}();
exports.ConsoleAdapter = ConsoleAdapter;

/***/ }),
/* 238 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
Object.defineProperty(exports, "HowlerLoader", {
  enumerable: true,
  get: function get() {
    return _howlerLoader.HowlerLoader;
  }
});
Object.defineProperty(exports, "SoundLayer", {
  enumerable: true,
  get: function get() {
    return _layer.SoundLayer;
  }
});
var _howlerLoader = __webpack_require__(146);
var _layer = __webpack_require__(240);

/***/ }),
/* 239 */
/***/ (function(module, exports) {

module.exports = require("howler");

/***/ }),
/* 240 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SoundLayer = void 0;
var _howlerLoader = __webpack_require__(146);
var SoundLayer = /** @class */function () {
  function SoundLayer() {}
  /**
   * Returns all the sounds available in the given layer.
   * @param layer the layer to get its sounds.
   */
  SoundLayer.getSoundsFromLayer = function (layer) {
    var result = [];
    for (var s in _howlerLoader.HowlerLoader.sounds) {
      if (!_howlerLoader.HowlerLoader.sounds.hasOwnProperty(s)) {
        continue;
      }
      var ref = _howlerLoader.HowlerLoader.sounds[s];
      if (ref.layer === layer) {
        result.push(ref.data);
      }
    }
    return result;
  };
  /**
   * Attach the given sound to the given layer.
   * @param sound the sound reference to configure.
   * @param layer the layer where to add the sound.
   */
  SoundLayer.setSoundLayer = function (sound, layer) {
    for (var s in _howlerLoader.HowlerLoader.sounds) {
      if (!_howlerLoader.HowlerLoader.sounds.hasOwnProperty(s)) {
        continue;
      }
      var ref = _howlerLoader.HowlerLoader.sounds[s];
      if (ref.data === sound) {
        ref.layer = layer;
        return;
      }
    }
  };
  /**
   * Sets the given layer muted or unmuted.
   * @param layer the layer to mute or unmute.
   * @param muted wether or not the layer should be muted.
   */
  SoundLayer.muteLayer = function (layer, muted) {
    for (var s in _howlerLoader.HowlerLoader.sounds) {
      if (!_howlerLoader.HowlerLoader.sounds.hasOwnProperty(s)) {
        continue;
      }
      var ref = _howlerLoader.HowlerLoader.sounds[s];
      if (ref.layer === layer) {
        ref.data.mute(muted);
      }
    }
  };
  /**
   * Sets the given layer paused or resumed.
   * @param layer the layer to pause or resume.
   * @param muted wether or not the layer should be paused or resumed.
   */
  SoundLayer.pauseLayer = function (layer, paused) {
    for (var s in _howlerLoader.HowlerLoader.sounds) {
      if (!_howlerLoader.HowlerLoader.sounds.hasOwnProperty(s)) {
        continue;
      }
      var ref = _howlerLoader.HowlerLoader.sounds[s];
      if (ref.layer !== layer) {
        continue;
      }
      if (paused && ref.data.playing()) {
        ref.data.pause();
      } else if (!paused && !ref.data.playing()) {
        ref.data.play();
      }
    }
  };
  return SoundLayer;
}();
exports.SoundLayer = SoundLayer;

/***/ }),
/* 241 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(10);
__webpack_require__(27);
__webpack_require__(19);
Object.defineProperty(exports, "__esModule", {
  value: true
});
var _serialize = __webpack_require__(242);
Object.keys(_serialize).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _serialize[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function get() {
      return _serialize[key];
    }
  });
});

/***/ }),
/* 242 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.generateSerializableMember = generateSerializableMember;
exports.initialStore = void 0;
exports.resetInitialStore = resetInitialStore;
exports.serializeAsBoolean = serializeAsBoolean;
exports.serializeAsNumber = serializeAsNumber;
exports.serializeAsString = serializeAsString;
__webpack_require__(19);
__webpack_require__(10);
__webpack_require__(27);
__webpack_require__(243);
var _tools = __webpack_require__(136);
/**
 * Defines the store that handles the serializable members.
 */
var initialStore = {};
/**
 * Resets the initial store by removing all available keys.
 * @warning use with care.
 */
exports.initialStore = initialStore;
function resetInitialStore() {
  var keys = Object.keys(initialStore);
  keys.forEach(function (k) {
    return delete initialStore[k];
  });
}
/**
 * Serializes the property which has a number.
 * @param sourceName optional name to retrieve when serializing/deserializing.
 */
function serializeAsNumber(sourceName) {
  return generateSerializableMember("number", sourceName);
}
/**
 * Serializes the property which has a number.
 * @param sourceName optional name to retrieve when serializing/deserializing.
 */
function serializeAsString(sourceName) {
  return generateSerializableMember("string", sourceName);
}
/**
 * Serializes the property which has a number.
 * @param sourceName optional name to retrieve when serializing/deserializing.
 */
function serializeAsBoolean(sourceName) {
  return generateSerializableMember("boolean", sourceName);
}
/**
 * Generates a serialization member (decorator function).
 * @param type the type of the property to serialize.
 * @param sourceName the optional custom name of the property to serialize.
 */
function generateSerializableMember(type, sourceName) {
  return function (target, propertyKey) {
    var ctorName = _tools.Tools.getConstructorName(target);
    if (!initialStore[ctorName]) {
      initialStore[ctorName] = [];
    }
    var store = initialStore[ctorName].find(function (s) {
      return s.sourceName === sourceName || s.sourceName === propertyKey;
    });
    if (!store) {
      initialStore[ctorName].push({
        sourceName: sourceName || propertyKey,
        type: type
      });
    }
  };
}

/***/ }),
/* 243 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $ = __webpack_require__(2);
var $find = __webpack_require__(49).find;
var addToUnscopables = __webpack_require__(123);

var FIND = 'find';
var SKIPS_HOLES = true;

// Shouldn't skip holes
// eslint-disable-next-line es/no-array-prototype-find -- testing
if (FIND in []) Array(1)[FIND](function () { SKIPS_HOLES = false; });

// `Array.prototype.find` method
// https://tc39.es/ecma262/#sec-array.prototype.find
$({ target: 'Array', proto: true, forced: SKIPS_HOLES }, {
  find: function find(callbackfn /* , that = undefined */) {
    return $find(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
  }
});

// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables
addToUnscopables(FIND);


/***/ })
/******/ ]);
//# sourceMappingURL=core.bundle.js.map