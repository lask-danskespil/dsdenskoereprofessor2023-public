import type { ICroupierConfig } from "@gdk/backutils";
export declare class CroupierImporter {
    private _id;
    private _stakes;
    croupierConf: ICroupierConfig;
    /**
     * Import archive file
     * @param file
     * @param serviceName
     * @param version
     */
    import(file: string): Promise<ICroupierConfig>;
    /**
     * Loads a sql croupier file and translates into a JSON format
     */
    importSQLCroupier(content: string): void;
    /**
     * Loads a sql usr file and translates into a JSON format
     */
    importUsr(content: string): void;
    /**
     * extract game info
     */
    private parseItgGame;
    /**
     * Add a stake to the stakes list
     */
    private parseGameStake;
    /**
     * Add a rank to the rank list
     */
    private parseWin;
    /**
     * Add ticket to the matching rank
     */
    private parseSymbols;
    private pickValueOf;
    private _nextId;
}
