import type { ICroupier, ICroupierConfig, ATicketClass, ICroupierUsrInfos } from "@gdk/backutils";
export declare class Generator<T extends ATicketClass> {
    private conf;
    private packageJson;
    private symbolClass?;
    private nextId;
    private usedSymbols;
    private usedIds;
    constructor(conf: ICroupierConfig, packageJson: any, symbolClass?: any);
    /**
     * Run the generation
     */
    run(): ICroupier;
    private getTicketFromShape;
    private nextTicketId;
    private _prefix;
    private _formatAmount;
    private _symbolToString;
    private _stringToSymbol;
}
export declare function generateUsrSql(infos: ICroupierUsrInfos): string;
