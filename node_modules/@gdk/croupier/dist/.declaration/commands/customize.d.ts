import type { ICroupierConfig } from "@gdk/backutils";
export interface ICustomizationConfig {
    path: string;
    code: string;
    masterGameCode?: string;
    description?: string;
    renewal?: number;
    baseStake?: number;
    ranks?: Array<{
        id: number;
        quantity?: number;
        renewal?: number;
        prizeType?: string;
        prizeDescription?: string;
    }>;
}
export declare const customize: (conf: ICustomizationConfig) => Promise<ICroupierConfig>;
