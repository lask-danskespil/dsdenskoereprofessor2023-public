import type { ICroupierConfig } from "@gdk/backutils";
export declare function validate(conf: ICroupierConfig): void;
export interface IInitOptions {
    masterGameCode?: string;
    code?: string;
    lotteryCode?: string;
    sourceFile?: string;
}
export declare function init(options: IInitOptions): ICroupierConfig;
export declare function initFromSourceFile(config: ICroupierConfig, lotteryCode: string, sourceFile: string): void;
/**
 * Merge the config with requirements, requirements override data in config
 * @param conf
 * @param requirements
 */
export declare function mergeWithRequirements(conf: ICroupierConfig, requirements: object): ICroupierConfig;
