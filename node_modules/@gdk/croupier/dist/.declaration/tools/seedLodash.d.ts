import * as _ from "lodash";
export declare class SeedLodash {
    private static _instance;
    static get instance(): _.LoDashStatic;
    /**
     * Seed the random and return the lodash instance.
     * @param seed
     */
    static seed(seed: string): _.LoDashStatic;
}
