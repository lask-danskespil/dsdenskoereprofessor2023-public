export function listToString(values: any[], maxValues = 3): string {
	const remaining = values.length - maxValues;
	if (remaining > 0) {
		values = [...values.slice(0, maxValues), `+ ${remaining}...`];
	}

	return `[${values.join(", ")}]`;
}
