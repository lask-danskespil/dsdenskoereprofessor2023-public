import { IClientEnvironment } from "../clientEnvironment/interface";
import { IList } from "./interface";
/**
 * Tests if environment is allowed by black/white list.
 * When blacklist provided, returns false if environment in blacklist.
 * When whitelist provided, returns false if environment not in whitelist.
 * Returns true otherwise.
 * @param list: black/white list.
 * @param env: browser environment.
 */
export declare function isAllowedByList(list: IList, env: IClientEnvironment): boolean;
