import type Hapi from "@hapi/hapi";
import type { GameConfig, IAmount, IJackpotsConfiguration } from "@gdk/backutils";
import { Jackpot } from "../models/jackpot";
export interface IJackpotMock {
    /**
     * externalId of the jackpot to control
     * Every jackpot not in the mock will be reseted to default behaviour
     */
    externalId: string;
    /**
     * This jackpot will be won if true
     * Lost if false
     * Random if absent
     */
    win?: boolean;
    /**
     * If present, it will set this jackpot amount
     * This amount will be used for banner in case of a progressive jackpot
     * This amount will be used for the win amount in case of an eventual win
     * If absent, the amount will be reseted to initialAmount
     */
    amount?: IAmount;
}
export declare class JackpotsRoutes {
    private _gameConfig;
    jackpots: Record<string, Jackpot>;
    /**
     * Constructor
     * @param _gameConfig
     */
    constructor(_gameConfig: GameConfig);
    setJackpots(config: IJackpotsConfiguration): void;
    /**
     * Retrieve routes
     */
    getRoutes(): Hapi.ServerRoute[];
    /**
     * PUT /jackpots/admin/mock/{serviceName}
     */
    private handleMock;
}
