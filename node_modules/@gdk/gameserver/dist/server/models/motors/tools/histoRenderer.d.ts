import GameModel from "./gameModel";
import type { IGameSession } from "../gameSession";
export default class HistoRenderer {
    /**
     * Render game session.
     */
    static render(gameSession: IGameSession): Promise<string>;
    private readonly xmlSnippet;
    private readonly constants;
    constructor(gameModel: GameModel);
    /**
     * Execute HistoRenderFunction.
     * @param gameSession
     */
    execute(gameSession: IGameSession): Promise<string>;
}
