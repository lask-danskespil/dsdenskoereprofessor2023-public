import type GameModel from "./gameModel";
import type { IGameSession } from "../gameSession";
import type { IGamifSession } from "../../gamif/gamifSession";
export default class State {
    private xmlSnippet;
    private gameModel;
    constructor(xmlSnippet: any, gameModel: GameModel);
    get name(): string;
    /** Process an event and returns the new state */
    on(event: string, session: IGameSession, gamifSession: IGamifSession, params: any): Promise<string>;
    private getParams;
}
