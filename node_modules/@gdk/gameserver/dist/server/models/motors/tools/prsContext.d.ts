export interface IPrsContext {
    /**
     * A string representation of the physical ticket winning amount
     * @exemple "1000 E"
     * @deprecated
     */
    masterPrizeDescription?: string;
    /**
     * ISO 4217 currency code of the physical ticket winning amount
     */
    creditCurrency: string;
    /**
     * Physical ticket winning amount in the currency fraction digit (for euro 100 means 1€)
     */
    creditAmount: number;
    /**
     * String representing the physical ticket context
     * This field is a free game dependant string
     * This context can be retrieved on script side & on game client side
     */
    contextElements: string;
}
export interface IPrsTicketInfos {
    token: string;
    status: "UNPLAYED" | "LOCKED" | "DRAWN" | "ERROR" | "INVALID" | "RETRY";
    info: IPrsContext;
}
export default class PrsContext {
    private static contexts;
    private static create;
    /**
     * Get game prsContext.
     * @param manifestFilePath
     * @param token
     */
    static get(manifestFilePath: string, token: string): Promise<IPrsTicketInfos | null>;
}
