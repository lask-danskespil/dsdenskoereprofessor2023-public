import type { ICroupier } from "@gdk/backutils";
export declare class CroupierLoader {
    /**
     * Loads a croupier.
     * @param filepath
     * @param serviceName
     */
    static load(filepath: string, serviceName: string): Promise<ICroupier>;
    /**
     * Loads a croupier from a json file.
     * @param filepath of the croupier.
     * @returns a promise resolving with a ICroupierJSON when loaded.
     */
    private static _importJson;
}
