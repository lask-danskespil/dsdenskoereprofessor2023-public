import type { IBetValidResponse } from "./types";
export declare class TicketStore extends Array<{
    playerId: string;
    ticket: IBetValidResponse;
}> {
    /**
     * Stores a ticket for a given player
     */
    store(playerId: string, ticket: IBetValidResponse): void;
    /**
     * Find a stored ticket by ticket Id
     */
    findById(ticketId: number): IBetValidResponse;
}
