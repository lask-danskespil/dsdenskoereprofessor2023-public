export interface IMockObject {
    rank?: number;
    symbol?: string;
    symbolPattern?: string;
    winAmount?: number;
    force?: boolean;
}
export declare class MocksStore {
    private _data;
    /**
     * Retrive next mock values for a given service and a given player
     * @param serviceName
     * @param player
     */
    getNext(serviceName: string, playerId: string): IMockObject;
    /**
     * Retrive all mocks values registered for a given service and a given player
     * @param serviceName
     * @param player
     */
    get(serviceName: string, playerId: string): IMockObject[];
    /**
     * Add some mocks values for future bets for a given service and a given player
     * @param serviceName
     * @param player
     * @param mocks
     */
    add(serviceName: string, playerId: string, mocks: IMockObject[]): void;
    /**
     * Erase all mocks values for a given service and a given player
     * @param serviceName
     * @param player
     */
    remove(serviceName: string, playerId: string): void;
}
