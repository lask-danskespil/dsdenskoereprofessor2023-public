import type { IJackpotResults } from "./jackpot";
export declare class JackpotResultsStore extends Array<{
    playerId: string;
    betId: number | string;
    result: IJackpotResults;
}> {
    private static instance;
    private constructor();
    /**
     * @returns the JackpotResultsStore singleton instance
     */
    static getInstance(): JackpotResultsStore;
    /**
     * Stores a ticket for a given player
     */
    store(playerId: string, betId: number | string, result: IJackpotResults): void;
    /**
     * Find a stored ticket by ticket Id
     */
    findById(betId: number | string): IJackpotResults;
}
