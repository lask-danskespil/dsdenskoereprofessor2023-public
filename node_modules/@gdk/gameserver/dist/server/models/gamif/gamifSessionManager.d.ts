import type { IGamifSession } from "./gamifSession";
export interface IGamifSessionFile {
    serviceName: string;
    dataFilePath: string;
}
export declare class GamifSessionManager {
    private _gamifSessionFiles;
    /**
     * Add gamif session file.
     * @param serviceName
     * @param dataFilePath
     */
    add(serviceName: string, dataFilePath: string): void;
    /**
     * Method to get a IGamifSession.
     * @param serviceName of the requested IGamifSession.
     * @returns the IGamifSession requested, {} if not found.
     * @todo should we store the session once loaded ?
     */
    get(serviceName: string): IGamifSession;
}
