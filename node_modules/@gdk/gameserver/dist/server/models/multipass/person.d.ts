import type { Room } from "./room";
import type { Player } from "./player";
import type { SSEChannel } from "./sseChannel";
import type { IBet, IPlayerShort } from "./models";
export declare class Person {
    readonly id: string;
    room: Room;
    channel: SSEChannel;
    nbInactiveSession: number;
    /**
     * Constructor
     * @param id the id of the person
     * @param room the room that the person joined
     * @param channel the notification channel
     */
    constructor(id: string, room: Room, channel: SSEChannel);
    /**
     * Returns the JSON data of the person
     */
    toShortJson(): IPlayerShort;
    /**
     * The person leaves the room
     */
    leaveRoom(): void;
    /**
     * The person bets and becomes a new player
     * @param bet the bet amount with data
     */
    bet<T>(bet: IBet<T>): Player;
}
