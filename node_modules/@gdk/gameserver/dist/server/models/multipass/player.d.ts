import type { Request } from "@hapi/hapi";
import { Person } from "./person";
import type { IPlayerFull, IPlayerPublicData, IPlayerPrivateData, IPlayerProtectedData } from "./models";
export declare class Player extends Person {
    protected: IPlayerProtectedData;
    public: IPlayerPublicData;
    private: IPlayerPrivateData;
    /**
     * Constructor
     * @param person person reference being a new player
     */
    constructor(person: Person);
    /**
     * The player executes an action
     * @param eventName the name of the event
     * @param data the data in the execute request
     */
    execute(eventName: string, data: any): void;
    /**
     * The player leaves the room
     */
    cancelBet(): void;
    /**
     * Returns full json of the player
     * @param playerId the id of the player
     */
    toFullJson(playerId?: string): IPlayerFull;
    /**
     * Returns the player"s id from the request (referrer or whatever)
     * @param request the request containing the player Id
     */
    static getPlayerId(request: Request): string;
}
